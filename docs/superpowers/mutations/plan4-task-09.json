[
 {"id": "M1", "desc": "축2 축분리 — refused의 두 사유 문장을 맞바꾼다",
  "file": "plugins/claude-sync/skills/sync-backup/scripts/prune_mcp.py",
  "old": "            refused[name] = \"로컬에 있는 서버는 지우지 않는다 — 사용자의 실제 설정이다\"\n        elif mc.restorable(name, repo[name]):\n            refused[name] = \"복원 가능한 항목은 지우지 않는다 — 정리 대상이 아니다\"",
  "new": "            refused[name] = \"복원 가능한 항목은 지우지 않는다 — 정리 대상이 아니다\"\n        elif mc.restorable(name, repo[name]):\n            refused[name] = \"로컬에 있는 서버는 지우지 않는다 — 사용자의 실제 설정이다\""},

 {"id": "M2", "desc": "축1 훅 호출 계약 — 로컬 가드를 없앤다(사용자의 실제 설정이 지워진다)",
  "file": "plugins/claude-sync/skills/sync-backup/scripts/prune_mcp.py",
  "old": "        elif name in local:\n            refused[name] = \"로컬에 있는 서버는 지우지 않는다 — 사용자의 실제 설정이다\"\n",
  "new": ""},

 {"id": "M3", "desc": "축3 {} vs None — 레포에 없는 이름을 not_found가 아니라 pruned로 센다",
  "file": "plugins/claude-sync/skills/sync-backup/scripts/prune_mcp.py",
  "old": "        if name not in repo:\n            not_found.append(name)",
  "new": "        if name not in repo:\n            pruned.append(name)"},

 {"id": "M4", "desc": "축4 I/O 층 — 아무것도 지우지 않아도 레포 파일을 다시 쓴다",
  "file": "plugins/claude-sync/skills/sync-backup/scripts/prune_mcp.py",
  "old": "    if pruned:\n        mc.dump_backup(",
  "new": "    if True:\n        mc.dump_backup("},

 {"id": "M5", "desc": "축5 입력 축 — 고정점 회차를 둘에서 하나로 줄인다(2회차==3회차 단정이 죽는지)",
  "file": "plugins/claude-sync/tests/test_mcp_cycle.py",
  "old": "    snapshots = []\n    for _ in range(2):\n        report = dev.backup()",
  "new": "    snapshots = []\n    for _ in range(1):\n        report = dev.backup()"}
]
