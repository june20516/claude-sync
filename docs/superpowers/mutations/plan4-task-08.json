[
 {
  "id": "M1",
  "desc": "축1 훅 호출 계약 — collect가 병합 결과 대신 repo_ahead에서 뽑는다(degrade 갈래를 놓친다)",
  "file": "plugins/claude-sync/skills/sync-backup/scripts/collect_mcp.py",
  "old": "        **mc.unrestorable_report([n for n in servers if n not in local], servers),",
  "new": "        **mc.unrestorable_report(result[\"repo_ahead\"], servers),"
 },
 {
  "id": "M2",
  "desc": "축2 축분리 — \"로컬에 없다\" 조건을 없앤다(사용자의 실제 설정을 쓰레기로 보고)",
  "file": "plugins/claude-sync/skills/sync-backup/scripts/collect_mcp.py",
  "old": "        **mc.unrestorable_report([n for n in servers if n not in local], servers),",
  "new": "        **mc.unrestorable_report(list(servers), servers),"
 },
 {
  "id": "M3",
  "desc": "축3 {} vs None — compare가 route_new_keys 대신 only_local을 훑는다",
  "file": "plugins/claude-sync/skills/sync-status/scripts/compare_mcp.py",
  "old": "    out.update(mc.unrestorable_report(mc.route_new_keys(local, repo), repo))",
  "new": "    out.update(mc.unrestorable_report(out[\"only_local\"], repo))"
 },
 {
  "id": "M4",
  "desc": "축5 입력 축 — 픽스처에서 복원 가능한 레포 전용 대조군을 뺀다",
  "file": "plugins/claude-sync/tests/test_mcp_scripts.py",
  "old": "    repo = write_repo(tmp_path, {\"claude.ai Slack\": GARBAGE, \"theirs\": B})\n    out = compare_mcp.compare(os.path.join(repo, mc.BACKUP_RELPATH), claude_json_path=local)\n    assert out[\"only_repo\"] == [\"claude.ai Slack\", \"theirs\"]",
  "new": "    repo = write_repo(tmp_path, {\"claude.ai Slack\": GARBAGE})\n    out = compare_mcp.compare(os.path.join(repo, mc.BACKUP_RELPATH), claude_json_path=local)\n    assert out[\"only_repo\"] == [\"claude.ai Slack\", \"theirs\"]"
 },
 {
  "id": "M5",
  "desc": "축4 I/O 층 — status 산문이 정리의 주체를 restore로 되돌린다",
  "file": "plugins/claude-sync/skills/sync-status/SKILL.md",
  "old": "\"어느 기기도 복원할 수 없는 항목입니다. `/sync-backup`이 레포에서 정리할지 묻습니다.\"",
  "new": "\"restore가 이 기기에 설치합니다.\""
 }
]
