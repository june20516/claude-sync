# 3.0.0 이후 결함 일곱 Implementation Plan (plan ④, 3.1.0)

> **agentic worker에게:** REQUIRED SUB-SKILL: 이 plan을 task 단위로 구현하려면 suberpower:subagent-driven-development(권장) 또는 suberpower:executing-plans를 사용하세요. Step은 추적을 위해 checkbox(`- [ ]`) 문법을 사용합니다.

**Goal:** 첫 실기기 백업이 드러낸 거짓 문구·유실 경로 다섯(①②③④⑤)과 3.0.0이 이월한 둘(언어 스위치·복원 계획 층 통일)을 닫아 **3.1.0**을 배포 가능한 상태로 만든다. 스키마는 바뀌지 않는다.

**Architecture:** 판정은 스크립트(`lib/`·`skills/*/scripts/`)가, 대화는 세 `SKILL.md`가, 둘의 결속은 `tests/test_skill_wiring.py`·`test_restore_prose.py`·`test_user_docs.py`의 가드가 맡는다 — 3.0.0의 구조 그대로다. 새로 들어오는 것은 셋이다: `reject`·`conflict`의 **기준선 유무 갈래**(A), 복원 불가 항목의 **정리 경로**(`prune_mcp.py`, B), 구문 손상의 **복구 경로**(`detect_downgrade.py` 확장, C). 레포에 쓰는 자리는 `/sync-backup` 하나이고 restore·status는 진단해 그곳을 가리킨다.

**Tech Stack:** Python 3.13 표준 라이브러리만. 테스트는 pytest (`uv run --with pytest pytest plugins/claude-sync/tests -q`). 기준선 **1211 passed**(`main` `e51c929`, 2026-09-02 실측).

**Spec:** `docs/superpowers/specs/2026-09-02-post-3.0.0-fixes-design.md` (승인됨). 각 task 머리의 `**근거:** spec N.M`이 그 절을 가리킨다 — **spec의 그 절이 바뀌면 그 task는 무효다. 전제가 깨지면 plan이 아니라 spec부터 고친다.**

**브랜치:** `feat/post-3.0.0-fixes` (`main`에서 분기, spec 커밋 `10a51d6`). 흐름은 3.0.0과 같다 — `feat/post-3.0.0-fixes` → `release/3.1.0` → `main`. **푸시·머지·태그·배포는 사용자가 실행한다.**

---

## 이 plan에는 「다음으로 넘길 것」이 없다

plan ③의 규칙을 그대로 잇는다. **착수 시점에 보이던 일곱은 전부 이 plan이 처리한다**(사용자 결정 2026-09-02 — "일곱 전부"). **작업 중 새로 발견되는 것은 그 자리에서 판정한다** — 이 plan의 Goal에 방해가 되면 지금 하고, 방해가 되지 않으면 spec 11장 「닫은 것」에 이유를 적고 닫는다. 새 이월 항목을 만들지 않는다. **판단이 애매하면 미루지 말고 사용자에게 묻는다.**

## Task 순서가 spec의 장 순서와 다른 자리 하나

spec은 A(①②④·3.4) → B(③) → C(⑤) → D(언어·층)이지만 이 plan은 **D-2(층 통일, Task 6)를 B보다 먼저** 한다. B의 Task 8·9와 C의 Task 12가 `sync-restore/SKILL.md` §6에 문장을 더하는데, Task 6이 그 절을 통째로 다시 쓴다 — 뒤에 두면 앞 task의 문장을 옮겨 적는 일이 생기고, 그 옮겨 적기가 이 저장소가 반복해 만난 드리프트다. 결과 구조는 같다.

| Task | 갈래 | spec | 등급 |
|---|---|---|---|
| 1 | `reconcile_backup`의 `reject` 두 갈래 + 4단계 문구 | 3.1 | 중 |
| 2 | 10단계 `BASE_RELS` 두 경로 | 3.2 | 중 |
| 3 | `generate_metadata`가 레포 트리를 걷는다 | 3.3 | 경 |
| 4 | `check_status`의 `no_base` 묶음 | 3.4 | 중 |
| 5 | README 두 벌의 표식 문장 | 3.5 | 경 |
| 6 | `plan_mcp` 출력을 `sections` 층으로 + restore §6 | 7 | 경 |
| 7 | `mcp_config.unrestorable_reason` | 4.1 | 경 |
| 8 | 세 스크립트의 `unrestorable`·사유 + status·restore 문구 | 4.2 | 경 |
| 9 | `prune_mcp.py` + 고정점 | 4.3 · 4.6 | 중 |
| 10 | backup 6.5단계 + 표 예외 + 억제 | 4.4 · 4.5 | 중 |
| 11 | `detect_downgrade`의 `broken_syntax` + 세 스킬의 진단 갈래 | 5.1 · 5.2 · 5.4 | 중 |
| 12 | `broken_syntax` 문구 여섯 줄 | 5.3 | 중 |
| 13 | 언어 스위치 | 6 | 경 |
| 14 | 릴리즈 게이트 — 3.1.0 | 8 · 10 | 중 |

---

## 변조 확인은 각 task의 필수 스텝이다 (Step 4b)

plan ③의 규칙 그대로다. 각 task의 `Step 4b`에서 **그 task가 도입한 가드 절을 하나씩 뒤집고** 대응 테스트가 FAIL하는지 확인한다. 다섯 축 각각에 최소 하나 — **훅 호출 계약 · 축 분리 · `{}` vs `None` · I/O 층 · 입력 축**. 다섯이 전부 CAUGHT면 그 task의 변조 검증은 끝이다(커버리지 기준). 명세는 `docs/superpowers/mutations/plan4-task-NN.json`에 남긴다(형식은 그 디렉토리의 README).

```bash
python3 ~/.claude/suberpowers/tools/mutate.py \
  --repo /Users/bran/personal/claude-sync \
  --spec docs/superpowers/mutations/plan4-task-NN.json --jobs 4
```

**SURVIVE하면 구현이 아니라 테스트를 보강한다.** `APPLY_FAIL`을 `SURVIVED`로 읽지 않는다 — 앵커를 고쳐 다시 돌린다. 가드가 스스로 공허해지는 세 형태(`not in`의 바늘이 틀려도 초록 · 선택자가 비면 루프 0회 · 목록이 자기 축소를 탐지 못 함)를 매번 본다.

## 검증을 언제 멈추는가 · 검증 강도

plan ③의 세 기준(커버리지·깊이·라운드)과 세 등급(중·경·미)을 그대로 쓴다. **축은 「코드냐 문서냐」가 아니라 "여기서 조용히 틀리면 되돌릴 수 없는 행동이 나가는가"다** — `SKILL.md` 산문이 사용자에게 「제거」·「정리」·「복구」를 권하는 자리는 전부 **중**이다. 등급은 위 표에 있다.

## 코드를 고치면 docstring도 함께 점검한다 (모든 task의 필수 스텝)

`grep`으로 전수를 뜬다. 이 plan에서 특히 낡는 문장: *"`~/.claude`를 직접 걷는다"*(Task 3 — `syncignore.py`·`test_skill_wiring.py`·`check_status.py` 주석), *"복원 불가 판정에 사유가 없다"*(Task 7), *"`reject`가 하나다"*(Task 1), *"세 SKILL.md는 `newer_schema_seen`만 읽는다"*(`generate_metadata.py` docstring — Task 11 뒤에는 `broken_syntax`도 읽는다).

## 배포 전 확인

> **이 기기의 캐시는 `claude-sync/3.0.0`이다.** 세 스킬을 실행하면 **3.0.0 스크립트**가 돈다 — plan 실행 중의 스모크는 `claude plugin`을 임시 HOME에서 재거나 테스트의 서브프로세스 픽스처로 한다. **실기기 스모크(Task 14)는 3.1.0 설치 뒤에 한다.** 실기기 픽스처(임시 클론의 `claude.ai *` 7개, 파일 기준선 없음)는 그때까지 **손대지 않는다** — spec 1장.

---

### Task 1: `reconcile_backup.py`의 `reject`를 두 갈래로 가른다

**근거:** spec 3.1 — **등급: 중**(4단계 문구가 사용자를 restore의 「백업 채택」으로 이끌 수 있다)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-backup/scripts/reconcile_backup.py`
- Modify: `plugins/claude-sync/skills/sync-backup/SKILL.md` (4단계, 지금의 214~216행)
- Test: `plugins/claude-sync/tests/test_reconcile.py`, `plugins/claude-sync/tests/test_skill_wiring.py`

- [ ] **Step 1: 실패하는 테스트 — 판정 함수와 CLI 출력 모양**

`tests/test_reconcile.py`의 `test_backup_new_local_push` 바로 아래에 더한다:

```python
def test_backup_reject_bucket_is_decided_by_base_presence_alone():
    """spec 3.1 — backup_action의 세 반환값은 그대로고, reject의 갈래는 S 하나가 정한다.

    S 없음·R 있음·L≠R은 여전히 reject다(push하지 않는다). 다른 것은 **사용자에게 할
    말**이고, 그것을 정하는 사실은 기준선의 유무 하나다.
    """
    assert rb.backup_action("L", "R", None) == "reject"
    assert rb.reject_bucket(None) == "no_base"
    assert rb.reject_bucket("S") == "remote_ahead"
```

같은 파일의 `run_reconcile_backup` 아래(지금의 375행 앞)에 헬퍼와 CLI 테스트를 더한다:

```python
def every_rel(out):
    """reconcile_backup 출력의 모든 relpath. `reject`는 두 갈래의 dict다(spec 3.1)."""
    rels = list(out["push"]) + list(out["in_sync"])
    for bucket in out["reject"].values():
        rels += bucket
    return rels


def test_backup_reconcile_splits_reject_by_base_presence(tmp_path):
    """①(spec 3.1) — 기준선이 없는 L≠R은 `reject.no_base`, 기준선 뒤로 레포가 바뀐 것은
    `reject.remote_ahead`. 실측(2026-09-01)의 형태 그대로다 — 첫 백업의 유일한 reject는
    기준선이 없어서였고 로컬이 앞서 있었다.

    **대조군을 함께 둔다** — `in_sync` 하나가 없으면 "아무것도 push하지 않는다"로도 단정이
    참이 된다.
    """
    home, repo = status_fixture(tmp_path)
    (repo / "agents" / "keep.md").write_text("레포 판본")            # 로컬 "공개"와 다르다
    (repo / "agents" / "internal-secret.md").write_text("사내 URL")  # 로컬과 같다 — 대조군
    out = run_reconcile_backup(home, repo)
    assert out["reject"] == {"remote_ahead": [], "no_base": ["agents/keep.md"]}
    assert out["in_sync"] == ["agents/internal-secret.md"] and out["push"] == []

    base = home / ".claude" / ".sync-state" / "base" / "agents"
    base.mkdir(parents=True)
    (base / "keep.md").write_text("옛 레포 판본")     # S ≠ R, S ≠ L → 리모트가 앞섰다
    out = run_reconcile_backup(home, repo)
    assert out["reject"] == {"remote_ahead": ["agents/keep.md"], "no_base": []}
```

그리고 기존 `test_backup_reconcile_does_not_promise_to_push_an_excluded_file`의 마지막 단정을 새 모양에 맞춘다 — `reject`가 dict가 되면 `out.values()`를 도는 옛 줄은 갈래 **이름**을 relpath로 세어 공허해진다:

```python
    assert all("internal-secret" not in rel for rel in every_rel(out))
```

- [ ] **Step 2: 실패 확인**

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_reconcile.py -q -k "reject_bucket or splits_reject or excluded_file"`
기대: `reject_bucket`은 `AttributeError`, `splits_reject`는 `out["reject"]`가 list라 `==` 실패, `excluded_file`은 `every_rel` 미정의로 FAIL.

- [ ] **Step 3: 구현**

`reconcile_backup.py`의 module docstring 둘째 단락과 `backup_action`·`main`을 이렇게 바꾼다(`.syncignore` 단락은 그대로 둔다):

```python
"""백업(push) 방향 파일별 판정.

사용: reconcile_backup.py <repo_path>  (~/.claude 기준 로컬을 레포로 push)
JSON 출력: {"push": [...], "in_sync": [...],
            "reject": {"remote_ahead": [...], "no_base": [...]}}
실제 파일 복사/커밋은 SKILL.md 흐름에서 수행하며, push된 파일과 in_sync 파일의 base는
로컬 내용으로 갱신한다(10단계).

**`reject`는 두 갈래다.** 어느 쪽도 push하지 않지만 사용자에게 할 말이 다르다 —
`remote_ahead`는 기준선이 있고 레포가 그 뒤로 바뀐 것이고(restore 먼저), `no_base`는
기준선이 없어 **방향을 모르는** 것이다. 한 버킷으로 내면 4단계가 후자에도 "리모트가
앞섰다"고 단정하고, 그 안내를 따라 restore에서 「백업 채택」을 고르면 로컬 변경이
사라진다(실측 — 2026-09-01 첫 실기기 백업의 `agents/code-reviewer.md`는 로컬이 앞서
있었다). 갈래 이름은 reconcile_restore.py의 충돌 사유(`no_base`)와 같다.
```

```python
def backup_action(local_hash, repo_hash, seen_hash):
    """반환: in_sync | push | reject. reject의 갈래는 reject_bucket이 정한다."""
    if repo_hash is None:
        return "push"  # 레포에 없음 → 새로 추가
    if local_hash == repo_hash:
        return "in_sync"
    if repo_hash == seen_hash:
        return "push"  # 로컬만 변경(local ahead)
    return "reject"    # 기준선이 없거나(no_base) remote가 base 이후 변경됨(remote_ahead)


def reject_bucket(seen_hash):
    """reject의 갈래. 기준선(S)이 없으면 방향을 모르는 것이지 리모트가 앞선 것이 아니다.

    입력이 seen_hash 하나뿐인 것이 계약이다 — backup_action이 reject를 낸 뒤 S 하나가
    갈래를 정하므로, 두 판정이 어긋날 자리가 없다.
    """
    return "no_base" if seen_hash is None else "remote_ahead"


def main():
    repo_path = sys.argv[1]
    home = os.path.expanduser("~/.claude")
    out = {"push": [], "in_sync": [], "reject": {"remote_ahead": [], "no_base": []}}
    patterns = syncignore.load_patterns(syncignore.default_path(home))
    for rel in sorted(syncignore.filter_relpaths(ss.iter_synced_relpaths(home), patterns)):
        L = ss.file_hash(os.path.join(home, rel))
        R = ss.file_hash(os.path.join(repo_path, rel))
        S = ss.base_hash(rel)
        action = backup_action(L, R, S)
        if action == "reject":
            out["reject"][reject_bucket(S)].append(rel)
        else:
            out[action].append(rel)
    print(json.dumps(out, indent=2, ensure_ascii=False))
```

- [ ] **Step 4: 통과 확인**

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_reconcile.py -q`
기대: 전부 PASS.

- [ ] **Step 5: 4단계 문구 — 갈래마다 다른 문장**

`sync-backup/SKILL.md` 4단계의 이 두 줄을

```
출력 JSON의 세 키를 확인한다:
- `reject`가 하나라도 있으면: "리모트가 앞선 변경이 있습니다. 먼저 /sync-restore 하세요" 안내 후 그 파일은 건너뛴다(push하지 않는다).
```

이렇게 바꾼다(`- \`push\` 파일만 …` 줄은 그대로 그 아래 남는다):

```
출력 JSON의 세 키를 확인한다. `reject`는 **두 갈래**다 — 어느 쪽도 push하지 않지만 사용자에게 할 말이 다르다. 갈래를 정하는 사실은 **기준선의 유무** 하나다.
- `reject.remote_ahead`가 하나라도 있으면: 기준선이 있고 레포가 그 뒤로 바뀐 파일이다. "리모트가 앞선 변경이 있습니다. 먼저 /sync-restore 하세요" 안내 후 그 파일은 건너뛴다(push하지 않는다).
- `reject.no_base`가 하나라도 있으면: 기준선이 없어 **어느 쪽이 앞선지 모르는** 파일이다. **"리모트가 앞섰다"고 말하지 않는다** — 실측(2026-09-01)의 유일한 reject는 이 갈래였고 **로컬이 앞서** 있었다. 그 안내를 따라 restore에서 「백업 채택」을 고르면 로컬 변경이 사라진다. 파일마다 이렇게 안내하고 건너뛴다(push하지 않는다):
  > "이 파일은 비교 기준선이 없어 어느 쪽이 앞선 것인지 판단할 수 없습니다. `/sync-restore`에서 양쪽을 보고 고르세요. **로컬 변경이 있다면 「백업 채택」은 그것을 버립니다.**"
```

- [ ] **Step 6: 산문 가드**

`tests/test_skill_wiring.py`의 `test_backup_checks_the_missing_base_before_it_collects` 앞에 더한다:

```python
def test_backup_step4_never_calls_a_baseless_reject_remote_ahead():
    """①(spec 3.1) — `reject`의 두 갈래에 다른 문장을 쓰고, `no_base`에는 방향을 단정하지 않는다.

    실측(2026-09-01): 첫 실기기 백업의 유일한 reject는 기준선이 없어서였고 로컬이 앞서
    있었다. "리모트가 앞섰다 → restore 먼저"를 따라 「백업 채택」을 고르면 로컬 41줄이
    사라진다 — 안내가 파괴적 선택을 가리키는 자리다.
    """
    sec = section("sync-backup", "4. 파일별 reconcile")
    remote = sec.index("`reject.remote_ahead`")
    no_base = sec.index("`reject.no_base`")
    push = sec.index("`push` 파일만")
    assert remote < no_base < push, "갈래 둘이 push 안내보다 앞에, remote_ahead가 먼저"
    remote_branch, no_base_branch = sec[remote:no_base], sec[no_base:push]
    assert "리모트가 앞선 변경이 있습니다" in remote_branch
    assert "어느 쪽이 앞선 것인지 판단할 수 없습니다" in no_base_branch
    assert "「백업 채택」은 그것을 버립니다" in no_base_branch, "파괴적 선택의 위험을 말하지 않는다"
    # 인용문(사용자에게 나가는 문장) 안에는 방향을 단정하는 말이 없어야 한다.
    quote = no_base_branch[no_base_branch.index("> "):]
    assert "리모트가 앞선" not in quote, "no_base 인용문이 방향을 단정한다"
    for branch in (remote_branch, no_base_branch):
        assert "push하지 않는다" in branch
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_skill_wiring.py -q -k step4`
기대: PASS.

- [ ] **Step 4b: 변조 확인** — `docs/superpowers/mutations/plan4-task-01.json`

| id | 축 | 파일 · 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 축 분리 | `reconcile_backup.py`: `"no_base" if seen_hash is None else "remote_ahead"` → 갈래 맞바꿈 | `test_backup_reject_bucket_is_decided_by_base_presence_alone` |
| M2 | 훅 호출 계약 | `main`: `out["reject"][reject_bucket(S)]` → `out["reject"]["remote_ahead"]` | `test_backup_reconcile_splits_reject_by_base_presence` |
| M3 | `{}` vs `None` | `backup_action`: `if repo_hash == seen_hash: return "push"` → `if repo_hash == seen_hash or seen_hash is None:` (기준선 없음을 push로) | 같은 테스트(첫 절반) |
| M4 | 입력 축 | 테스트 픽스처에서 `internal-secret.md`의 레포 사본(대조군)을 뺀다 | 대조군 단정 `out["in_sync"] == [...]`이 죽는다 |
| M5 | I/O 층 | SKILL.md: `no_base` 인용문에 "리모트가 앞선 변경이 있습니다"를 다시 넣는다 | `test_backup_step4_never_calls_a_baseless_reject_remote_ahead` |

전부 CAUGHT여야 한다.

- [ ] **Step 7: docstring 점검**

`grep -rn "reject" plugins/claude-sync/lib plugins/claude-sync/skills` — 4단계 산문과 이 스크립트 말고 `reject`를 읽는 자리가 없는지 확인한다(spec 3.1의 실측 그대로여야 한다).

- [ ] **Step 8: Commit**

```bash
git add plugins/claude-sync/skills/sync-backup/scripts/reconcile_backup.py \
        plugins/claude-sync/skills/sync-backup/SKILL.md \
        plugins/claude-sync/tests/test_reconcile.py plugins/claude-sync/tests/test_skill_wiring.py \
        docs/superpowers/mutations/plan4-task-01.json
git commit -m "fix(backup): reject를 remote_ahead·no_base로 갈라 기준선 없는 파일에 방향을 단정하지 않는다"
```

---

### Task 2: 10단계 — `push` ∪ `in_sync`의 base를 두 경로 모두에서 갱신한다

**근거:** spec 3.2 — **등급: 중**(base 쓰기 경로. 잘못 넣으면 방향 모르는 파일의 base가 전진한다)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-backup/SKILL.md` (10단계 bash 블록 453~490행, 11단계 첫 문단 494행)
- Test: `plugins/claude-sync/tests/test_skill_wiring.py` (`COMPAT_WIRING` 앵커 + 새 가드)

- [ ] **Step 1: 실패하는 가드**

`tests/test_skill_wiring.py`의 `COMPAT_WIRING["sync-backup"]["before_calls"]`에서

```python
            'python3 "$SYNC_SCRIPTS/update_base.py" "$HOME/.claude" "${PUSHED_RELS[@]}"',
```

를

```python
            'python3 "$SYNC_SCRIPTS/update_base.py" "$HOME/.claude" "${BASE_RELS[@]}"',
```

로 바꾸고, `test_backup_base_gate_distinguishes_push_failure_from_staging_failure` 아래에 더한다:

```python
FILE_BASE_CALL = 'python3 "$SYNC_SCRIPTS/update_base.py" "$HOME/.claude" "${BASE_RELS[@]}"'


def test_backup_advances_file_bases_on_both_success_paths():
    """②(spec 3.2) — 파일 base 갱신이 "변경사항 없음" 경로와 푸시 성공 경로 **둘 다**에 있고,
    푸시 실패 경로에는 없다.

    실측(2026-09-01): push가 비고 in_sync만 8개인 백업은 정확히 첫 경로로 빠졌고, 거기에
    갱신이 없어 파일 기준선이 하나도 생기지 않았다. 그래서 ①이 백업만 하는 기기에서
    **계속** 재발한다.
    """
    block = section("sync-backup", "10. 커밋 & 푸시")
    sites = [m.start() for m in re.finditer(re.escape(FILE_BASE_CALL), block)]
    assert len(sites) == 2, "파일 base 갱신 호출이 %d번이다 — 두 경로에 하나씩이어야 한다" % len(sites)
    quiet = block.index("if git diff --cached --quiet; then")
    commit = block.index("elif git commit")
    fail = block.index("푸시에 실패했습니다")
    assert quiet < sites[0] < commit < sites[1] < fail
    # 대상은 push ∪ in_sync 이고 reject의 어느 갈래도 아니다.
    src = block[block.index("mapfile -t BASE_RELS"):quiet]
    assert "data.get('push', []) + data.get('in_sync', [])" in src
    assert "reject" not in src, "방향을 모르는 파일의 base가 전진한다"
    assert "in_sync 파일의 base ← 로컬 내용" in section("sync-backup", "11. base(.sync-state) 갱신 규칙")
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_skill_wiring.py -q -k "both_success_paths or before_calls"`
기대: FAIL(호출 0번, 앵커 없음).

- [ ] **Step 2: 10단계 bash 블록 교체**

`sync-backup/SKILL.md` 10단계의 두 번째 bash 블록(`SYNC_REPO=…`부터 `fi`까지, 지금의 453~475행)을 이것으로 바꾼다. 그 아래 `# base: 레포가 실제로 …`부터의 플러그인·MCP 게이트 블록은 **그대로** 둔다:

```bash
SYNC_REPO="${TMPDIR:-/tmp}/claude-sync-repo"
BASE_STAGING="${TMPDIR:-/tmp}/claude-sync-base-staging-backup"
cd "$SYNC_REPO"
REPO_HAS_CONTENT=0

# 파일 base 갱신 대상 = push ∪ in_sync. **reject의 두 갈래는 넣지 않는다** — remote_ahead는
# 로컬이 레포에 동의하지 않은 상태이고 no_base는 방향을 모르는 상태다.
# in_sync는 로컬 == 레포이므로 base ← 로컬이 곧 base ← 레포다. 이것을 넣지 않으면 백업만
# 하는 기기에는 파일 기준선이 영영 생기지 않는다(실측 — 2026-09-01: 파일 기준선 0개).
mapfile -t BASE_RELS < <(python3 -c "
import json
data = json.load(open('/tmp/claude-sync-reconcile.json'))
for r in data.get('push', []) + data.get('in_sync', []):
    print(r)
")

if git diff --cached --quiet; then
  echo "변경사항이 없습니다. 모든 설정이 최신 상태입니다."
  REPO_HAS_CONTENT=1          # 레포가 이미 이번 결과와 정합하다
  # **이 경로에도 있어야 한다.** push가 비고 in_sync만 있는 실행이 정확히 여기로 온다.
  if [ "${#BASE_RELS[@]}" -gt 0 ]; then
    python3 "$SYNC_SCRIPTS/update_base.py" "$HOME/.claude" "${BASE_RELS[@]}"
  fi
elif git commit -m "sync: backup claude settings ($(date '+%Y-%m-%d %H:%M'))" && git push; then
  REPO_HAS_CONTENT=1
  if [ "${#BASE_RELS[@]}" -gt 0 ]; then
    python3 "$SYNC_SCRIPTS/update_base.py" "$HOME/.claude" "${BASE_RELS[@]}"
  fi
else
  echo "푸시에 실패했습니다. base를 갱신하지 않습니다."
fi
```

- [ ] **Step 3: 11단계 규칙 문장**

11단계 첫 문단의

```
**파일**: 커밋 & 푸시에 성공한 경우에만 push된 각 파일의 base를 방금 올린 로컬 내용으로 갱신한다. **핵심 계약: push 성공 파일의 base ← 로컬 내용.**
```

를

```
**파일**: 레포가 이번 결과와 정합한 경우(푸시 성공, 또는 커밋할 변경 없음)에만 갱신한다. **핵심 계약: push 성공 파일과 in_sync 파일의 base ← 로컬 내용.** `in_sync`는 로컬 == 레포이므로 그것이 곧 레포 내용이다. `reject`의 두 갈래(`remote_ahead`·`no_base`)는 어느 경로에서도 갱신하지 않는다 — 앞의 것은 로컬이 레포에 동의하지 않았고, 뒤의 것은 방향을 모른다. 푸시 실패 경로는 `in_sync`도 갱신하지 않는다 — 옳더라도(레포가 안 바뀌었으니 L == R은 여전하다) "푸시 실패 = base 불변"이라는 한 줄 규칙을 지킨다.
```

로 바꾼다.

- [ ] **Step 4: 통과 확인**

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_skill_wiring.py -q`
기대: 전부 PASS — 특히 `test_each_anchor_witnesses_exactly_one_call_site`(같은 줄 둘은 지점 하나다)와 `test_before_calls_covers_every_gated_script_call`.

- [ ] **Step 4b: 변조 확인** — `plan4-task-02.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 입력 축 | `data.get('push', []) + data.get('in_sync', [])` → `data.get('push', [])` | `both_success_paths`의 `src` 단정 |
| M2 | 축 분리 | 첫 경로의 갱신 호출 삭제 | 호출 2번 단정 |
| M3 | `{}` vs `None` | `+ data.get('in_sync', [])` → `+ data.get('in_sync', []) + data['reject']['no_base']` | `"reject" not in src` |
| M4 | I/O 층 | 푸시 실패 `else`에 갱신 호출 추가 | 호출 3번 → 단정 실패 |
| M5 | 훅 호출 계약 | `"$HOME/.claude"` → `"$SYNC_REPO"` (base ← 레포 바이트) | `COMPAT_WIRING` 앵커 불일치 → `before_calls_covers…` |

- [ ] **Step 5: docstring 점검** — `update_base.py` docstring의 "push 성공 후 SKILL.md에서 호출한다 … push된 파일의 base ← 로컬 내용"을 "push된 파일과 in_sync 파일"로 고친다.

- [ ] **Step 6: Commit**

```bash
git add plugins/claude-sync/skills/sync-backup/SKILL.md plugins/claude-sync/skills/sync-backup/scripts/update_base.py \
        plugins/claude-sync/tests/test_skill_wiring.py docs/superpowers/mutations/plan4-task-02.json
git commit -m "fix(backup): in_sync 파일의 기준선을 두 경로 모두에서 쓴다 — 백업만 하는 기기에도 파일 base가 생긴다"
```

---

### Task 3: `generate_metadata.py`가 레포 작업 트리를 걷는다

**근거:** spec 3.3 — **등급: 경**(표식은 기록이지 판정 입력이 아니다 — 읽는 프로덕션 코드가 없다)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-backup/scripts/generate_metadata.py`
- Modify: `plugins/claude-sync/lib/sync_state.py` (`iter_synced_relpaths` docstring), `plugins/claude-sync/lib/syncignore.py` (module docstring 「왜 여기 있는가」)
- Modify: `plugins/claude-sync/skills/sync-backup/SKILL.md` (7단계 391~397행, 「보안」절 56행)
- Test: `plugins/claude-sync/tests/test_metadata.py`, `plugins/claude-sync/tests/test_skill_wiring.py`

- [ ] **Step 1: 실패하는 테스트**

`tests/test_metadata.py`: 상단 import에 `import subprocess`와 `import sync_state as ss`를 더하고, `fake_claude_dir`를 트리 이름을 받는 `fake_tree`로 바꾼다(호출 자리는 `sed -i '' 's/fake_claude_dir(tmp_path)/fake_tree(tmp_path)/g'`로 전부 교체):

```python
def fake_tree(tmp_path, name="claude"):
    """agents/skills/CLAUDE.md를 가진 트리. 레포 작업 트리 역할(spec 3.3) — `~/.claude`가 아니다."""
    d = tmp_path / name
    (d / "agents").mkdir(parents=True)
    (d / "skills" / "demo").mkdir(parents=True)
    (d / "agents" / "a.md").write_text("a", encoding="utf-8")
    (d / "skills" / "demo" / "SKILL.md").write_text("s", encoding="utf-8")
    (d / "CLAUDE.md").write_text("c", encoding="utf-8")
    return str(d)
```

`.syncignore` 블록(`# --- \`.syncignore\`: 제외한 파일이 표식에 남는가 ---`부터 `test_the_excluded_count_is_reported`까지, 지금의 272~413행)을 **통째로 지우고** 이것으로 바꾼다:

```python
# --- 표식은 레포 작업 트리를 걷는다 (spec 3.3) ---
#
# 앞 판은 `~/.claude`를 걸어 **이 기기의 로컬**을 적었다. 그래서 표식이 레포에 없는 내용
# (reject 파일의 로컬 해시)을 레포 것이라 말하고, 다른 기기가 올린 파일은 통째로 빠뜨렸다
# (실측 — 2026-09-01). 7단계 시점의 레포 트리가 정의상 "이 백업이 담는 내용"이다 — 4단계가
# push 복사와 `.syncignore` 삭제를 마쳤으므로 제외 파일은 트리에 없고 표식에도 없다.
# 제외의 근거가 필터에서 **구조**로 옮겨 갔다.

def test_files_map_is_exactly_the_synced_files_of_the_tree(tmp_path):
    """양방향 완전성 — reject 해시·타 기기 파일 누락·제외 파일 유출 셋을 한 단정으로 덮는다.

    ⊆만 걸면 파일을 하나씩 잃어도 초록이고, ⊇만 걸면 트리 밖의 것을 실어도 초록이다.
    걷는 집합의 정의는 sync_state.iter_synced_relpaths 하나다 — 세 소비자와 같다.
    """
    tree = fake_tree(tmp_path)
    meta = gm.build_metadata(tree, write_plugin_json(tmp_path, {"version": "3.0.0"}))
    assert set(meta["files"]) == set(ss.iter_synced_relpaths(tree))
    for rel, digest in meta["files"].items():
        assert digest == gm.file_sha256(os.path.join(tree, rel)), rel


def test_files_map_records_the_tree_not_some_other_directory(tmp_path):
    """트리 인자가 실제로 쓰인다 — 같은 relpath에 다른 내용을 가진 둘째 트리가 아니다.

    ④의 실측 형태 그대로다(레포의 code-reviewer.md ≠ 로컬의 code-reviewer.md).
    """
    repo = fake_tree(tmp_path, name="repo")
    local = fake_tree(tmp_path, name="local")
    with open(os.path.join(local, "agents", "a.md"), "w", encoding="utf-8") as f:
        f.write("로컬이 앞선 판본")
    meta = gm.build_metadata(repo, write_plugin_json(tmp_path, {"version": "3.0.0"}))
    assert meta["files"]["agents/a.md"] == gm.file_sha256(os.path.join(repo, "agents", "a.md"))
    assert meta["files"]["agents/a.md"] != gm.file_sha256(os.path.join(local, "agents", "a.md"))


def test_the_tree_root_is_required(tmp_path):
    """기본값을 두면 갱신되지 않은 호출자가 조용히 옛 동작(로컬을 적는다)으로 돌아간다."""
    with pytest.raises(TypeError):
        gm.build_metadata(write_plugin_json(tmp_path, {"version": "3.0.0"}))


def test_generate_metadata_does_not_read_syncignore():
    """제외의 근거는 필터가 아니라 구조다 — 4단계가 레포 트리에서 지웠다.

    다시 부르기 시작하면 여기가 빨개진다. 그때 고칠 것은 이 단정이 아니라
    lib/syncignore.py 정본의 소비자 목록과 sync-backup/SKILL.md 7단계다.
    """
    src = open(gm.__file__, encoding="utf-8").read()
    assert "import syncignore" not in src and "syncignore." not in src


def test_cli_takes_output_and_tree_and_refuses_anything_else(tmp_path):
    """SKILL.md가 넘기는 인자 모양 그대로 — 트리를 빠뜨린 호출은 exit 1로 서야 한다."""
    tree = fake_tree(tmp_path)
    out = str(tmp_path / "m.json")
    script = os.path.abspath(gm.__file__)
    proc = subprocess.run([sys.executable, script, out, tree], capture_output=True, text=True)
    assert proc.returncode == 0, proc.stderr
    with open(out, encoding="utf-8") as f:
        assert set(json.load(f)["files"]) == set(ss.iter_synced_relpaths(tree))
    proc = subprocess.run([sys.executable, script, out], capture_output=True, text=True)
    assert proc.returncode == 1 and "레포 작업 트리" in proc.stderr
```

`tests/test_skill_wiring.py`: `test_backup_step7_says_the_marker_honours_syncignore`를 **지우고** 그 자리에 더한다. 그리고 `COMPAT_WIRING["sync-backup"]["before_calls"]`의 `generate_metadata.py` 앵커를 `GENERATE_METADATA_CALL`로 바꾼다(상수는 `COLLECT_MCP_CALL` 아래에 정의):

```python
GENERATE_METADATA_CALL = ('python3 "$SYNC_SCRIPTS/generate_metadata.py"'
                          ' "$SYNC_REPO/sync-metadata.json" "$SYNC_REPO"')
# 4단계의 제외 삭제 블록. 표식은 이 뒤에서 걷어야 한다(spec 3.3).
SYNCIGNORE_DELETE = 'find "$SYNC_REPO" -path "$SYNC_REPO/.git" -prune -o -path "$SYNC_REPO/$pattern" -print'


def test_backup_step7_hashes_the_repo_tree_after_the_exclusions():
    """표식은 레포 작업 트리를 걷는다(spec 3.3) — 그 사실과 순서가 절 안에 적혀야 한다.

    앞 판은 `~/.claude`를 걸어 제외 파일을 필터로 걸렀다. 이제 근거는 구조다 — 4단계가
    레포 트리에서 지운 뒤에 걷는다. 호출이 그 삭제 블록보다 앞으로 옮겨지면 지워지기 전
    트리를 걷어 제외 파일의 이름·해시가 다시 표식에 실린다.
    """
    text = read_skill("sync-backup")
    sec = section("sync-backup", "7. sync-metadata.json 생성")
    assert GENERATE_METADATA_CALL in sec, "7단계가 레포 트리를 둘째 인자로 넘기지 않는다"
    assert "레포 작업 트리를 걷는다" in sec
    assert "`~/.claude`를 직접 걷" not in sec, "옛 서술(로컬을 걷는다)이 남아 있다"
    assert index_of(text, SYNCIGNORE_DELETE, "sync-backup") < index_of(
        text, GENERATE_METADATA_CALL, "sync-backup"), "표식이 제외 삭제보다 앞에서 걷는다"
    security = security_section()
    assert "sync-metadata.json" in security, "보안 절이 표식 적용을 말하지 않는다"
    assert "레포 작업 트리" in security
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_metadata.py plugins/claude-sync/tests/test_skill_wiring.py -q`
기대: 새 테스트 다섯과 `step7_hashes` FAIL.

- [ ] **Step 2: 구현 — 스크립트**

`generate_metadata.py`를 이렇게 바꾼다(`file_sha256`·`write_metadata`는 그대로):

```python
#!/usr/bin/env python3
"""백업 시점의 파일별 내용 해시(sha256)와 버전 표식을 기록한다. mtime 미사용.

**레포 작업 트리를 걷는다 — `~/.claude`가 아니다.** 7단계 시점의 레포 트리가 정의상
"이 백업이 담는 내용"이다: 4단계가 push 복사와 `.syncignore` 삭제를 마쳤고, reject 파일은
옛 레포 판본 그대로이며, 다른 기기가 올린 파일은 거기 있다. 앞 판은 `~/.claude`를 걸어
**이 기기의 로컬**을 적었고, 그래서 표식이 레포에 없는 내용(reject 파일의 로컬 해시)을
레포 것이라 말하고 다른 기기의 파일은 통째로 빠뜨렸다(실측 — 2026-09-01, spec 3.3).

그래서 `.syncignore`를 읽지 않는다. 제외 파일은 4단계가 레포 트리에서 이미 지웠으므로
여기서 다시 거를 것이 없다 — 제외의 근거가 필터에서 구조로 옮겨 갔다. 매칭 규칙의
소비자는 `~/.claude`를 걷는 둘(check_status.py·reconcile_backup.py)이다.

표식 세 필드의 성격이 다르다:
- written_by_version: 정보. 판정에 쓰지 않는다.
- min_reader_version: **판정 근거.** 이것 하나가 backup 게이트다.
- schema: 사람이 읽는 요약. 판정 근거가 아니다 — 항목별 보류는 각 파일 자체의
  version 필드로 한다(spec 결정 2). 이 맵을 읽는 코드도 산문도 없다. 세 SKILL.md가
  읽는 것은 detect_downgrade의 출력(`newer_schema_seen`·`broken_syntax`)이지 이 맵이
  아니다. 백업 문서 **둘 다** 싣는다.
"""
import hashlib
import os
import sys

sys.path.insert(
    0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "..", "..", "lib")
)
import compat  # noqa: E402
import keyed_sync as ks  # noqa: E402
import mcp_config as mc  # noqa: E402
import plugin_config as pc  # noqa: E402
import sync_state as ss  # noqa: E402


def file_sha256(path):
    (기존 그대로)


def build_metadata(tree_root, plugin_json_path):
    """표식이 붙은 메타데이터 dict. `files`는 tree_root의 동기화 대상 전부다.

    **tree_root는 기본값 없는 필수 인자다.** 기본값을 `~/.claude`로 두면 갱신되지 않은
    호출자가 조용히 옛 동작(로컬을 적는다)으로 돌아간다 — compat.shape_of의 relpath와
    같은 이유다. 걷는 집합은 sync_state.iter_synced_relpaths가 정한다(agents/·skills/·
    CLAUDE.md) — 세 소비자와 같은 정의라야 표식의 키 집합이 판정의 집합과 같다.
    끊어진 심볼릭 링크는 건너뛴다 — os.walk가 그것을 파일로 열거하고 file_sha256이 None을
    돌려준다. 표식 생성 전체가 죽는 것보다 파일 하나가 빠지는 것이 싸다.

    plugin.json을 못 읽으면 written_by_version을 생략한다 — 자기 버전을 모르는 것이
    파일 해시를 못 쓸 이유는 아니다. min_reader_version은 상수이므로 이 경우에도
    정상 기록된다.
    """
    files = {}
    for rel in sorted(ss.iter_synced_relpaths(tree_root)):
        digest = file_sha256(os.path.join(tree_root, rel))
        if digest is None:
            print("건너뜀(읽을 수 없음): %s" % rel, file=sys.stderr)
            continue
        files[rel] = digest
    metadata = {"files": files}
    written_by = compat.read_plugin_version(plugin_json_path)
    if written_by is not None:
        metadata["written_by_version"] = written_by
    metadata["min_reader_version"] = compat.MIN_READER_VERSION
    # **백업 문서 둘 다 싣는다**(version-compat spec 5.3의 약속). 상수를 각 모듈에서
    # 뽑는다 — 리터럴을 적으면 SCHEMA_VERSION이 올라가도 표식만 조용히 옛 값을 말한다.
    metadata["schema"] = {mc.BACKUP_RELPATH: mc.SCHEMA_VERSION,
                          pc.BACKUP_RELPATH: pc.SCHEMA_VERSION}
    return metadata


def main():
    if len(sys.argv) != 3:
        # 파일명을 리터럴로 다시 쓰지 않는다 — 쓰는 쪽과 읽는 쪽이 다른 파일을 보면
        # 표식이 있는데도 없는 것으로 판정되는 무증상 고장이 된다.
        print("사용: generate_metadata.py <출력 경로 (예: $SYNC_REPO/%s)> <레포 작업 트리>"
              % compat.METADATA_RELPATH, file=sys.stderr)
        sys.exit(1)
    output_path, tree_root = sys.argv[1], sys.argv[2]
    metadata = build_metadata(tree_root, compat.default_plugin_json_path())
    if "written_by_version" not in metadata:
        print(
            "경고: plugin.json에서 버전을 읽지 못해 written_by_version을 생략했습니다.",
            file=sys.stderr,
        )
    write_metadata(output_path, metadata)
```

`sync_state.iter_synced_relpaths` docstring의 소비자 목록에 한 줄을 더한다:

```
    - generate_metadata.py는 **레포 작업 트리**를 걷는다 — 제외는 4단계가 그 트리에서
      이미 적용했으므로 걸지 않는다(spec 3.3).
```

- [ ] **Step 3: 구현 — 산문 셋**

`sync-backup/SKILL.md` 7단계의 첫 문단과 bash를

```
백업 시점의 파일 해시와 **버전 표식**을 기록한다. 이 스크립트는 **레포 작업 트리를 걷는다** — 7단계 시점의 그 트리가 정의상 "이 백업이 담는 내용"이다. 4단계가 push 복사와 `.syncignore` 삭제를 이미 마쳤으므로 제외 파일은 트리에 없고 표식에도 없으며, reject된 파일은 레포에 남은 판본의 해시가 실리고, 다른 기기가 올린 파일도 함께 실린다. 로컬을 걷지 않는다 — 그러면 표식이 레포에 없는 내용을 레포 것이라 말한다(실측 — 2026-09-01).

```bash
python3 "$SYNC_SCRIPTS/generate_metadata.py" "$SYNC_REPO/sync-metadata.json" "$SYNC_REPO"
```
```

로 바꾼다. 「보안」절의 `- **제외는 7단계의 …` 불릿을

```
- **제외는 7단계의 `sync-metadata.json`에도 미친다.** 그 표식은 4단계가 정리를 마친 **레포 작업 트리**를 걷으므로, 지워진 파일의 이름도 sha256도 실리지 않는다 — 별도의 필터가 아니라 순서가 그것을 보장한다. `~/.claude`를 걷는 두 스크립트(status의 `check_status.py`, backup 4단계의 `reconcile_backup.py`)만 `lib/syncignore.py`의 매칭을 스스로 적용한다.
```

로 바꾼다. `lib/syncignore.py` docstring의 `**왜 여기 있는가.**` 문단을

```
**왜 여기 있는가.** 4단계는 레포 작업 트리에서 `find … | rm -rf`로 파일을 지우는데,
`~/.claude`를 직접 걷는 스크립트 둘(check_status.py·reconcile_backup.py)은 그 삭제를
보지 못하므로 제외 목록을 스스로 적용해야 한다. 두 곳이 각자 매칭을 만들면 그 어긋남이
조용해지므로 규칙을 이 파일 하나에 둔다. 7단계의 generate_metadata.py는 **소비자가
아니다** — 레포 작업 트리를 걷으므로 4단계의 삭제가 곧 표식의 제외다(spec 3.3. 앞 판은
`~/.claude`를 걸어 이 필터에 기댔다).
```

로 바꾼다(`**README 두 벌이 말해야 하는 것**` 앵커와 그 번호 목록은 손대지 않는다 — `test_user_docs.py`가 그 사이를 자른다). `tests/test_skill_wiring.py` 1483행부터의 주석("7단계의 generate_metadata.py는 `~/.claude`를 직접 걷는다 …")을 "check_status.py·reconcile_backup.py는 `~/.claude`를 직접 걷는다"로 고친다.

- [ ] **Step 4: 통과 확인**

실행: `uv run --with pytest pytest plugins/claude-sync/tests -q`
기대: 전부 PASS. `test_default_output_name_matches_compat_constant`(상수 참조가 usage로 옮겨 갔다)와 `test_skill_writes_the_filename_compat_reads`(첫 인자는 그대로다)가 초록인지 본다.

- [ ] **Step 4b: 변조 확인** — `plan4-task-03.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 훅 호출 계약 | `ss.iter_synced_relpaths(tree_root)` → `ss.iter_synced_relpaths(os.path.expanduser("~/.claude"))` | `records_the_tree_not_some_other_directory`·`is_exactly_the_synced_files` |
| M2 | 축 분리 | `for rel in sorted(...)` 루프에서 `files[rel] = digest` → `if rel != "CLAUDE.md": files[rel] = digest` | 양방향 완전성 |
| M3 | `{}` vs `None` | `if digest is None: continue` → `files[rel] = None; continue` | 해시 단정 |
| M4 | 입력 축 | `test_files_map_records_the_tree_not_some_other_directory`에서 둘째 트리의 덮어쓰기를 뺀다 | `!=` 단정이 죽는다(등가 검사 아님을 확인) |
| M5 | I/O 층 | SKILL.md 7단계 호출을 4단계 `.syncignore` 블록 앞으로 옮긴다 | `step7_hashes_the_repo_tree_after_the_exclusions` |

- [ ] **Step 5: docstring 점검**

`grep -rn "직접 걷" plugins/claude-sync` — 남는 것은 `check_status.py`·`reconcile_backup.py`·`syncignore.py`·status SKILL.md의 것뿐이어야 한다(그 넷은 여전히 참이다).

- [ ] **Step 6: Commit**

```bash
git add plugins/claude-sync/skills/sync-backup/scripts/generate_metadata.py plugins/claude-sync/lib/sync_state.py \
        plugins/claude-sync/lib/syncignore.py plugins/claude-sync/skills/sync-backup/SKILL.md \
        plugins/claude-sync/tests/test_metadata.py plugins/claude-sync/tests/test_skill_wiring.py \
        docs/superpowers/mutations/plan4-task-03.json
git commit -m "fix(backup): 표식이 로컬이 아니라 레포 작업 트리를 걷는다 — 레포에 없는 내용을 레포 것이라 적지 않는다"
```

---

### Task 4: `/sync-status`가 기준선 없는 파일을 "양쪽 변경"이라 부르지 않는다

**근거:** spec 3.4 (사용자 수용 2026-09-02) — **등급: 중**(같은 거짓 문장이 status에서 restore의 「백업 채택」을 가리킨다)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-status/scripts/check_status.py`
- Modify: `plugins/claude-sync/skills/sync-status/SKILL.md` (2단계 "아직 base가 없는 파일은 …" 문단, 3단계 상태 분류 목록)
- Test: `plugins/claude-sync/tests/test_reconcile.py`, `plugins/claude-sync/tests/test_skill_wiring.py`

- [ ] **Step 1: 실패하는 테스트**

`tests/test_reconcile.py`의 `test_without_syncignore_the_same_file_is_reported` 아래에 더한다:

```python
def test_status_does_not_call_a_baseless_difference_a_two_sided_change(tmp_path):
    """spec 3.4 — 기준선 없는 L≠R은 "양쪽 변경"이 아니라 "기준선 없음"이다.

    `classify`는 그것을 conflict로 낸다(판정으로는 옳다 — 어느 쪽이 앞선지 모르니 restore가
    선택을 받아야 한다). 거짓인 것은 머리말이다 — 아무도 양쪽을 바꾸지 않았을 수 있다.
    대조군: 기준선을 두면 같은 파일이 진짜 충돌 머리말 아래로 간다.
    """
    home, repo = status_fixture(tmp_path)
    (repo / "agents" / "keep.md").write_text("레포 판본")
    out = run_check_status(home, repo).stdout
    assert section_of(out, "agents/keep.md").startswith("⚠ 기준선 없음")
    assert "양쪽 변경" not in out

    base = home / ".claude" / ".sync-state" / "base" / "agents"
    base.mkdir(parents=True)
    (base / "keep.md").write_text("옛 판본")           # L ≠ S, R ≠ S → 진짜 충돌
    out = run_check_status(home, repo).stdout
    assert section_of(out, "agents/keep.md").startswith("⚠ 충돌 — 양쪽 변경")
    assert "기준선 없음" not in out
```

`tests/test_skill_wiring.py`의 `test_status_step2_does_not_promise_a_metadata_branch` 아래에 더한다:

```python
def test_status_names_the_no_base_bucket_with_the_scripts_own_heading():
    """spec 3.4 — 2단계가 `no_base` 묶음의 머리말을 스크립트에서 뽑은 그대로 싣고, 3단계의
    어휘에도 그 갈래가 있다. 머리말을 손으로 적지 않는다 — excluded_in_repo와 같은 처방."""
    with open(os.path.join(SKILLS_DIR, "sync-status", "scripts", "check_status.py"),
              encoding="utf-8") as f:
        label = re.search(r'\("no_base", "(.+?)"\)', f.read())
    assert label, "check_status.py에서 no_base 머리말을 뽑지 못했다"
    step2 = section("sync-status", "2. 로컬과 레포의 차이 분석")
    assert label.group(1) in step2, "2단계가 그 묶음의 머리말을 그대로 싣지 않는다"
    assert "`no_base`" in step2 and "양쪽 변경\"이 아니다" in step2
    assert "- **no_base**" in section("sync-status", "3. 결과 요약")
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_reconcile.py plugins/claude-sync/tests/test_skill_wiring.py -q -k "no_base"`
기대: 둘 다 FAIL.

- [ ] **Step 2: 구현 — 스크립트**

`check_status.py`의 `buckets` dict와 분류 루프, `labels`를 이렇게 고친다:

```python
buckets = {
    "in_sync": [],
    "repo_only": [],      # restore 시 추가
    "local_only": [],     # backup 시 push
    "local_ahead": [],    # backup 시 push
    "fast_forward": [],   # restore 시 업데이트
    "conflict": [],       # 양쪽 변경 (기준선 있음)
    "no_base": [],        # 기준선 없음 — 방향을 모른다 (spec 3.4)
    "excluded_in_repo": [],   # backup 시 레포에서 삭제
}
```

```python
    cls = ss.classify(L, R, S, local_exists=L is not None, repo_exists=R is not None)
    # **보고 시점 분할.** classify는 기준선 없는 L≠R을 conflict로 낸다 — 판정으로는 옳다
    # (어느 쪽이 앞선지 모르니 restore가 선택을 받아야 한다). 그러나 "양쪽 변경"이라는
    # 머리말은 그 파일에 대해 거짓이다 — 아무도 양쪽을 바꾸지 않았을 수 있다(spec 3.4.
    # backup 4단계의 reject.no_base와 같은 사실, 같은 이름).
    # classify 자체에 값을 더하지 않는다: restore_action의 완전성 단정과 소비자 둘이
    # 걸려 있고, 보고 시점 분할이 같은 결과를 더 작게 낸다(spec 11장).
    if cls == "conflict" and S is None:
        cls = "no_base"
    buckets[cls].append(rel)
```

```python
labels = [
    ("conflict", "⚠ 충돌 — 양쪽 변경 (restore 시 해소 필요)"),
    ("no_base", "⚠ 기준선 없음 — 어느 쪽이 앞선지 알 수 없음 (restore 시 선택 필요)"),
    ("fast_forward", "↓ 업데이트 가능 — 레포가 앞섬 (restore 시 적용)"),
    ("repo_only", "+ 새 파일 — 레포에만 있음 (restore 시 추가)"),
    ("local_ahead", "↑ 로컬 앞섬 (backup 시 push)"),
    ("local_only", "+ 로컬 전용 (backup 시 push)"),
    ("excluded_in_repo", "⊘ .syncignore 제외인데 레포에 남아 있음 (backup 시 레포에서 삭제)"),
    ("in_sync", "✓ 동일"),
]
```

- [ ] **Step 3: 구현 — 산문**

`sync-status/SKILL.md` 2단계의

```
아직 base가 없는 파일은 로컬과 레포가 다르면 `conflict`로 분류된다. 어느 쪽이 앞선 것인지 판단할 근거가 없기 때문이며, 이것도 분기가 아니라 같은 3-way 분류의 결과다.
```

를

```
아직 base가 없는 파일은 로컬과 레포가 다르면 **`no_base`**로 따로 묶인다 — 어느 쪽이 앞선 것인지 판단할 근거가 없기 때문이다. 3-way 분류로는 `conflict`와 같은 자리이지만 **"양쪽 변경"이 아니다** — 아무도 양쪽을 바꾸지 않았을 수 있다(실측 — 2026-09-01의 `agents/code-reviewer.md`는 로컬만 앞서 있었다). 스크립트가 찍는 머리말은 `⚠ 기준선 없음 — 어느 쪽이 앞선지 알 수 없음 (restore 시 선택 필요)`이다. 이것도 분기가 아니라 같은 3-way 분류의 결과를 기준선 유무로 가른 것이다.
```

로 바꾼다. 3단계 "상태 분류" 목록의 `- **conflict**: …` 줄 아래에

```
- **no_base**: 기준선이 없어 방향을 알 수 없음 → restore 시 양쪽을 보고 선택. 「백업 채택」은 로컬 변경을 버린다
```

를 더한다.

- [ ] **Step 4: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS.

- [ ] **Step 4b: 변조 확인** — `plan4-task-04.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 축 분리 | `if cls == "conflict" and S is None` → `and S is not None` | `two_sided_change` 양쪽 절반 |
| M2 | `{}` vs `None` | 분할 줄 삭제 | 첫 절반(`기준선 없음` 머리말 없음) |
| M3 | I/O 층 | `labels`에서 `no_base` 행 삭제 (버킷은 채워지되 찍히지 않는다) | `section_of`가 None → `startswith` 실패 |
| M4 | 입력 축 | 테스트에서 대조군(기준선 쓰기) 절반 삭제 | 대조군 단정이 사라진 것을 리뷰가 잡는다 — **등가 변조**이므로 SURVIVED 허용, 명세에 그렇게 적는다 |
| M5 | 훅 호출 계약 | SKILL 2단계 머리말 문장에서 "기준선 없음"을 다른 말로 바꾼다 | `names_the_no_base_bucket_with_the_scripts_own_heading` |

- [ ] **Step 5: docstring 점검** — `lib/sync_state.classify` docstring에 "기준선 없는 L≠R도 conflict다 — 보고 시점 분할은 check_status.py가 한다(spec 3.4)" 한 줄을 더한다.

- [ ] **Step 6: Commit**

```bash
git add plugins/claude-sync/skills/sync-status/scripts/check_status.py plugins/claude-sync/skills/sync-status/SKILL.md \
        plugins/claude-sync/lib/sync_state.py plugins/claude-sync/tests/test_reconcile.py \
        plugins/claude-sync/tests/test_skill_wiring.py docs/superpowers/mutations/plan4-task-04.json
git commit -m "fix(status): 기준선 없는 파일을 '양쪽 변경'이 아니라 '기준선 없음'으로 찍는다"
```

---

### Task 5: README 두 벌 — 표식은 기록이지 판정 입력이 아니다

**근거:** spec 3.5 — **등급: 경**

**Files:**
- Modify: `README.md` (122행), `README.ko.md` (122행)
- Test: `plugins/claude-sync/tests/test_user_docs.py`

- [ ] **Step 1: 실패하는 가드**

`tests/test_user_docs.py`의 `SYNCIGNORE_WORDING` 블록 아래에 더한다(같은 짝 형태 — 바늘을 정정 문안과 짝짓는다):

```python
# (문서, 옛 문구, 정정 문안). CORRECTIONS에 넣지 않는다 — 그 표의 개수는 2026-08-24 spec
# 13장에서 뽑는데 이 정정의 원천은 2026-09-02 spec 3.5다. 개수는 아래에서 함께 건다.
METADATA_WORDING = [
    ("README.md",
     "Each backup records a content-hash base snapshot for accurate 3-way conflict detection",
     "It is a record, not an input"),
    ("README.ko.md",
     "백업마다 내용 해시 기반 base 스냅샷을 기록하여 정확한 3-way 충돌 판단에 활용",
     "기록일 뿐 판정 입력이 아닙니다"),
]


@pytest.mark.parametrize("name,stale,fixed", METADATA_WORDING,
                         ids=[n for n, _, _ in METADATA_WORDING])
def test_readme_does_not_call_the_metadata_a_conflict_input(name, stale, fixed):
    """spec 3.5 — `files` 맵을 읽는 프로덕션 코드가 없다(위 test_no_production_code_reads_
    the_metadata_files_map). "3-way 충돌 감지에 활용"은 거짓이고, 그 거짓을 backup-readme는
    이미 고쳤는데 README 두 벌만 남아 있었다."""
    assert len(METADATA_WORDING) == 2
    text = read_doc(name)
    assert fixed in text, "%s: 정정 문안이 없다 — %r" % (name, fixed)
    assert stale not in text, "%s: 옛 서술이 남아 있다 — %r" % (name, stale)
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_user_docs.py -q -k metadata_a_conflict` → FAIL 둘.

- [ ] **Step 2: 문서 정정**

`README.md` 122행을

```
- **Metadata tracking**: `sync-metadata.json` lists a content hash of every file this backup contains. It is a record, not an input — conflict detection compares against each machine's own `.sync-state/` base, never against this file
```

로, `README.ko.md` 122행을

```
- **메타데이터**: `sync-metadata.json`에 이 백업이 담은 파일별 내용 해시를 적습니다. 기록일 뿐 판정 입력이 아닙니다 — 충돌 판정은 이 파일이 아니라 기기마다 자기 `.sync-state/` base와 대조합니다
```

로 바꾼다.

- [ ] **Step 3: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests/test_user_docs.py -q` → 전부 PASS.

- [ ] **Step 4b: 변조 확인** — `plan4-task-05.json`: 정정 문안을 무의미한 값으로(CAUGHT), 옛 문구 되돌리기(CAUGHT), 한 문서만 되돌리기(CAUGHT — 파라미터화). 세 축이면 충분하다(경 등급).

- [ ] **Step 5: Commit**

```bash
git add README.md README.ko.md plugins/claude-sync/tests/test_user_docs.py docs/superpowers/mutations/plan4-task-05.json
git commit -m "docs(readme): 표식은 기록이지 3-way 판정 입력이 아니다"
```

---

### Task 6: `plan_mcp.py` 출력을 `sections` 층으로 감싸고 restore §6을 그 위에 다시 쓴다

**근거:** spec 7 — **등급: 경**(휘발성 JSON. 사용자 가치는 없고 산문의 혼동 경고 셋을 없앤다). **B·C보다 먼저** — 이 plan 머리의 「Task 순서」 참조.

**Files:**
- Modify: `plugins/claude-sync/skills/sync-restore/scripts/plan_mcp.py` (`build_plan`)
- Modify: `plugins/claude-sync/skills/sync-restore/SKILL.md` (§5 표 머리말 260행, 5-5 첫 문단 361행, §6 446~468행, 6-4의 1번 526행)
- Test: `plugins/claude-sync/tests/test_mcp_scripts.py`, `plugins/claude-sync/tests/test_mcp_cycle.py`, `plugins/claude-sync/tests/test_restore_prose.py`

- [ ] **Step 1: 실패하는 테스트 — 출력 모양**

`tests/test_mcp_scripts.py`의 `staged_servers` 아래에 헬퍼를, `test_plan_emits_buckets_and_configs` 앞에 테스트를 더한다:

```python
def bucket(plan, name):
    """계획의 버킷 — `sections[<섹션>]` 안이다(spec 7). 섹션 이름은 어댑터에서 뽑는다."""
    (section,) = mc.SECTIONS
    return plan["sections"][section][name]
```

```python
def test_plan_is_two_layered_like_the_plugin_plan(tmp_path):
    """spec 7 — 버킷은 `sections[<섹션>]` 안, 실행 재료(`configs`·`secret_keys`)는 최상위.

    플러그인 계획과 같은 층 구조라야 sync-restore/SKILL.md가 두 표의 층 차이를 세 군데서
    경고할 필요가 없다. 섹션 이름은 리터럴이 아니라 `mc.SECTIONS`에서 온다.
    """
    local = write_local(tmp_path, {"mine": A})
    repo = write_repo(tmp_path, {"mine": A, "theirs": B})
    out = plan_mcp.build_plan(os.path.join(repo, mc.BACKUP_RELPATH),
                              claude_json_path=local, base_dir=write_base_blob(tmp_path, None))
    assert set(out) == {"status", "sections", "configs", "secret_keys"}
    assert set(out["sections"]) == set(mc.SECTIONS)
    assert set(bucket(out, "add")) == {"theirs"}
    assert set(out["sections"][mc.SECTIONS[0]]) == set(mc.restore_plan({}, {}, None))
    assert out["configs"] == {"theirs": B}
```

그리고 같은 파일에서 최상위 버킷을 읽는 단정 여섯을 `bucket(...)`으로 바꾼다 — `plan["local_stale"]`·`plan["add"]`(201~202행), `out["add"]`·`out["needs_secret"]`(321행), `out["unrestorable"]`(343행), `out["local_ahead"]`·`out["repo_ahead"]`·`out["both_changed"]`(353~355행). 예: `assert bucket(out, "add") == ["pw"] and bucket(out, "needs_secret") == ["c7"]`.

`tests/test_mcp_cycle.py`: 같은 `bucket` 헬퍼를 `repo_servers` 아래에 두고, `Device.restore`의 `plan["add"]`를 `bucket(plan, "add")`로, 테스트의 `plan["repo_ahead"]`(113행)·`plan["local_ahead"]`(181행)·`plan["local_stale"]`(192행)·`plan["unrestorable"]`·`plan["add"]`·`plan["needs_secret"]`(241~242행)를 `bucket(plan, …)`으로 바꾼다. `test_two_cycles_reach_fixed_point`의 스냅샷은 `{k: v for k, v in plan.items() if isinstance(v, list) and v}`를 `{k: v for k, v in plan["sections"][mc.SECTIONS[0]].items() if isinstance(v, list) and v}`로 바꾼다.

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_mcp_scripts.py plugins/claude-sync/tests/test_mcp_cycle.py -q` → `KeyError: 'sections'`로 다수 FAIL.

- [ ] **Step 2: 구현**

`plan_mcp.py`의 `build_plan`을:

```python
def build_plan(backup_path, claude_json_path=None, base_dir=ss.BASE_DIR):
    """restore_plan 결과를 `sections` 층으로 감싸고 등록용 레포 config(마스킹됨)를 덧붙인다.

    **두 층이다 — plan_plugins와 같은 구조**(spec 7). 버킷은 `sections[<섹션>]` 안(판정),
    `configs`·`secret_keys`는 최상위(실행 재료). 섹션 이름은 `mc.SECTIONS`에서 뽑고
    언패킹으로 "섹션이 하나"라는 전제를 함께 건다 — detect_downgrade._mcp_buckets와 같다.
    앞 판은 버킷을 최상위에 두어 sync-restore/SKILL.md가 두 표의 층 차이를 세 군데서
    경고해야 했다. 휘발성 JSON이라 마이그레이션은 없다.
    """
    local = mc.read_local_servers(claude_json_path)
    repo = mc.load_backup(backup_path)
    base = mc.parse_base(ss.read_base(mc.BACKUP_RELPATH, base_dir=base_dir))
    plan = mc.restore_plan(local, repo, base)
    masked = mc.redact(repo)
    names = sorted({n for bucket in NEEDS_CONFIG for n in plan[bucket]})
    (section,) = mc.SECTIONS
    return {
        "status": "ok",
        "sections": {section: dict(plan)},
        "configs": {n: masked[n] for n in names},
        "secret_keys": {
            n: mc.secret_keys(masked[n]) for n in names if mc.secret_keys(masked[n])
        },
    }
```

로 바꾼다. module docstring의 "복원 계획 JSON을 stdout에 낸다 (버킷 9개 + configs + secret_keys)"를 "(`sections[<섹션>]`의 버킷 9개 + 최상위 configs·secret_keys)"로 고친다.

- [ ] **Step 3: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests/test_mcp_scripts.py plugins/claude-sync/tests/test_mcp_cycle.py -q` → 전부 PASS.

- [ ] **Step 4: restore §6 다시 쓰기**

`sync-restore/SKILL.md`에서 넷을 고친다.

(a) §6의 `cat /tmp/claude-sync-mcp-plan.json` 블록 바로 뒤, `status`가 `"skipped"`면 … 문단 **앞**에 문단을 넣는다:

```
**계획 JSON은 두 층이다 — 5절의 플러그인 계획과 같은 구조다.** 버킷은 `sections["servers"]` 안에 있고(섹션이 플러그인은 셋, MCP는 하나 — 이름은 `mcp_config.SECTIONS`), 실행 재료인 `configs`·`secret_keys`는 **최상위**다. 최상위에서 `add`나 `local_stale`을 찾으면 없고, 그러면 아래 표의 어느 버킷도 처리되지 않는다.
```

(b) §6 표의 머리 행 `| 버킷 | 처방 |`을 `| 버킷 (`sections["servers"]` 안) | 처방 |`로.

(c) §5 표 머리말의 "바로 뒤 6절의 MCP 표와 이름이 겹치지만 층도 처방도 다르다 — 그 표를 이 절에 옮겨 쓰지 않는다."를 "바로 뒤 6절의 MCP 표와 **층은 같고 처방이 다르다** — 그 표를 이 절에 옮겨 쓰지 않는다."로.

(d) 5-5 첫 문단의 "바로 뒤 6단계의 MCP 표는 같은 이름들이 최상위인 계획(`plan_mcp`)을 다루므로, 두 표가 나란히 있어도 층이 다르다."를 "바로 뒤 6단계의 MCP 계획(`plan_mcp`)도 같은 두 층이다 — 버킷은 `sections["servers"]` 안, `configs`·`secret_keys`는 최상위."로.

(e) 6-4의 "1. 그 이름이 unrestorable 목록에 있으면"을 "1. 그 이름이 `sections["servers"]["unrestorable"]`에 있으면"으로.

- [ ] **Step 5: 산문 가드**

`tests/test_restore_prose.py`의 `test_the_two_bucket_tables_are_not_copies_of_each_other` 아래에 더한다:

```python
def test_the_mcp_plan_prose_reads_buckets_from_the_section_layer():
    """spec 7 — 6절이 버킷을 `sections["servers"]` 아래서, `configs`·`secret_keys`를 최상위에서
    읽는다. 옛 경고 셋("최상위인 계획"·"층도 처방도 다르다")은 거짓이 됐으므로 없어야 하고,
    `not in`만으로는 바늘이 틀려도 초록이라 정정 문안과 짝짓는다."""
    (section,) = mc.SECTIONS
    head = mcp_bucket_table()
    assert 'sections["%s"]' % section in head
    assert "`configs`·`secret_keys`는 **최상위**" in head
    text = read_skill()
    for stale in ("같은 이름들이 최상위인 계획", "층도 처방도 다르다"):
        assert stale not in text, stale
    assert "층은 같고 처방이 다르다" in plugin_bucket_table()
    assert 'sections["%s"]["unrestorable"]' % section in clauses_mcp()["6-4"]
```

그리고 `clauses()` 아래에 6절용 자르기를 더한다(5절의 `clauses`와 같은 형태 — 손으로 나열하지 않는다):

```python
@functools.lru_cache(maxsize=None)
def clauses_mcp():
    """6절의 `#### 6-N` 소절 본문 {절 번호: 본문}."""
    text = read_skill()
    sec = text[text.index(MCP_HEADING):text.index(MCP_END_HEADING)]
    marks = list(CLAUSE_HEAD.finditer(sec))
    assert marks, "6절에서 `#### 6-N` 소절을 하나도 찾지 못했다"
    out = {}
    for i, m in enumerate(marks):
        end = marks[i + 1].start() if i + 1 < len(marks) else len(sec)
        out[m.group(1)] = sec[m.start():end]
    assert all(name.startswith("6-") for name in out), sorted(out)
    return out
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS.

- [ ] **Step 4b: 변조 확인** — `plan4-task-06.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 훅 호출 계약 | `"sections": {section: dict(plan)}` → `"sections": {"mcpServers": dict(plan)}` (BACKLOG가 적은 틀린 이름) | `set(out["sections"]) == set(mc.SECTIONS)` |
| M2 | 축 분리 | `configs`를 섹션 안으로 옮긴다 | `set(out) == {...}` |
| M3 | `{}` vs `None` | `dict(plan)` → `{k: v for k, v in plan.items() if v}` (빈 버킷 생략) | `set(...) == set(mc.restore_plan(...))` |
| M4 | 입력 축 | 픽스처에서 `theirs`를 뺀다 | `add` 단정 |
| M5 | I/O 층 | SKILL §6에 "같은 이름들이 최상위인 계획"을 되살린다 | `reads_buckets_from_the_section_layer` |

- [ ] **Step 6: docstring 점검** — `mcp_config.restore_plan` docstring의 "plan_mcp.py가 이 dict를 사용자 JSON에 통째로 펼치므로"는 여전히 참이다(펼치는 자리가 섹션 안으로 옮겨졌을 뿐). `grep -rn "최상위" plugins/claude-sync/skills/sync-restore/SKILL.md`로 층을 말하는 문장 전부를 다시 읽는다.

- [ ] **Step 7: Commit**

```bash
git add plugins/claude-sync/skills/sync-restore/scripts/plan_mcp.py plugins/claude-sync/skills/sync-restore/SKILL.md \
        plugins/claude-sync/tests/test_mcp_scripts.py plugins/claude-sync/tests/test_mcp_cycle.py \
        plugins/claude-sync/tests/test_restore_prose.py docs/superpowers/mutations/plan4-task-06.json
git commit -m "refactor(restore): MCP 복원 계획을 플러그인 계획과 같은 sections 층으로 — 경고 세 곳이 사라진다"
```

---

### Task 7: `mcp_config.unrestorable_reason` — 판정 하나에서 사유가 나온다

**근거:** spec 4.1 — **등급: 경**

**Files:**
- Modify: `plugins/claude-sync/lib/mcp_config.py`
- Test: `plugins/claude-sync/tests/test_mcp_config.py`

- [ ] **Step 1: 실패하는 테스트**

`tests/test_mcp_config.py`의 `test_restore_plan_unrestorable_for_non_dict_config` 아래에 더한다(`import inspect`를 상단에 더한다):

```python
@pytest.mark.parametrize("name,cfg,ok", [
    ("ok", {"command": "npx"}, True),
    ("sse-one", {"type": "sse", "url": "https://x/sse"}, True),
    ("claude.ai Notion", {"command": "npx"}, False),          # 이름 규칙 위반
    ("legacy", {"url": "npx x", "type": "stdio"}, False),      # v1 승격
    ("broken", None, False),                                   # 객체가 아님
])
def test_unrestorable_reason_is_none_exactly_when_restorable(name, cfg, ok):
    """spec 4.1 — `unrestorable_reason(n, c) is None ⟺ restorable(n, c)`.

    두 판정이 갈리면 양쪽 다 무증상이다 — 복원 가능한데 사유가 붙거나, 복원 불가인데
    사유가 None이 되어 보고에서 빠진다. 정상 케이스 둘이 없으면 "항상 사유가 있다"로도
    이 단정이 참이 된다(입력 축).
    """
    assert mc.restorable(name, cfg) is ok
    reason = mc.unrestorable_reason(name, cfg)
    assert (reason is None) is ok
    if not ok:
        assert isinstance(reason, str) and reason


def test_restorable_is_derived_from_the_reason_function():
    """동치를 테스트 픽스처에만 맡기지 않는다 — restorable이 사유 함수로 정의된다."""
    assert "unrestorable_reason(" in inspect.getsource(mc.restorable)


def test_unrestorable_report_pairs_every_name_with_a_reason():
    """spec 4.2 — 목록과 사유 맵의 키가 같은 집합이고, 복원 가능한 이름은 둘 다에 없다."""
    mapping = {"ok": {"command": "npx"}, "claude.ai Slack": {"url": "u", "type": "HTTP"},
               "legacy": {"url": "x", "type": "stdio"}}
    out = mc.unrestorable_report(["claude.ai Slack", "ok", "legacy"], mapping)
    assert out["unrestorable"] == ["claude.ai Slack", "legacy"]           # 정렬
    assert set(out["unrestorable_reasons"]) == set(out["unrestorable"])
    assert "이름 규칙" in out["unrestorable_reasons"]["claude.ai Slack"]
    assert "v1" in out["unrestorable_reasons"]["legacy"]
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_mcp_config.py -q -k "unrestorable_reason or derived or report_pairs"` → `AttributeError`로 FAIL.

- [ ] **Step 2: 구현**

`mcp_config.py`의 `restorable`을 이것으로 바꾸고 아래 셋을 더한다(module docstring의 "복원 가능성(restorable)"에 "·사유(unrestorable_reason)"를 덧붙인다):

```python
def unrestorable_reason(name, cfg):
    """복원 불가의 **갈래**를 문장으로. 복원 가능하면 None.

    `restorable`이 이 함수로 정의되므로(아래) 두 판정이 갈릴 자리가 없다 — plugin_config의
    같은 이름 함수는 조건을 두 벌 두고 "같아야 한다"고 적었는데, 여기서는 구조가 그것을
    대신한다. 사유는 status(compare_mcp)·backup(collect_mcp)·restore(plan_mcp) 셋이 같은
    함수에서 받는다 — 한쪽이 문장을 따로 쓰면 그것이 결함 B(파서 두 벌)의 형태다.

    두 갈래의 사용자 할 일은 같다(어느 기기도 재현할 수 없다 → 백업 6.5단계의 정리).
    그래도 사유를 싣는 것은 플러그인 보고와 형태를 맞추고 "왜"를 보이기 위해서다.
    v1 배열에서 승격된 항목이 정확히 둘째 갈래다(10장). type은 소문자만 인정한다 —
    v1이 저장하던 "HTTP"는 add-json 스키마와 맞지 않는다.
    """
    if not VALID_NAME.match(name):
        return ("이름 '%s'이(가) claude mcp add-json의 이름 규칙(영숫자·하이픈·언더스코어)에"
                " 맞지 않는다 — 계정 커넥터(claude.ai *)를 2.x가 긁어 넣은 항목이 이 형태다"
                % name)
    if isinstance(cfg, dict):
        if isinstance(cfg.get("command"), str) and cfg["command"]:
            return None
        if isinstance(cfg.get("url"), str) and cfg.get("type") in ("http", "sse"):
            return None
    return ("command도 url+type(http/sse)도 없어 등록 인자를 만들 수 없다"
            " — 옛 v1 형식에서 승격된 항목이 이 형태다")


def restorable(name, cfg):
    """claude mcp add-json으로 재현할 수 있는 항목인가. 판정은 unrestorable_reason 하나가 한다."""
    return unrestorable_reason(name, cfg) is None


def route_new_keys(local, repo):
    """restore가 "레포에만 있는 새 항목"으로 훑는 키 — plugin_config.route_new_for의 MCP 판.

    복원 가능성을 restore 밖에서 묻는 소비자(compare_mcp)가 이것을 훑는다. 코어의
    route_new_keys가 restore_plan과 같은 _route_new_names를 부르므로 정의상 같은 집합이다.
    MCP에는 보류가 없어 diff의 only_repo와도 같지만, **같은 함수를 부른다** — 두 어댑터가
    같은 자리에서 다른 집합을 보는 일을 만들지 않는다.
    """
    return ks.route_new_keys(local, repo, normalize=redact, hold=ks.no_hold)


def unrestorable_report(names, mapping):
    """names 중 복원 불가인 것과 그 사유 — 소비자 셋이 같은 한 벌을 쓴다(spec 4.2).

    {"unrestorable": [정렬된 이름], "unrestorable_reasons": {이름: 사유}}. 두 필드를 한 곳에서
    만든다 — 따로 만들면 갈리고, 갈려도 증상이 없다(사유 없는 항목이 조용히 생긴다).
    mapping은 마스킹 여부와 무관하다 — 판정이 보는 것은 이름·command·url·type뿐이다.
    """
    bad = sorted(n for n in names if not restorable(n, mapping.get(n)))
    return {"unrestorable": bad,
            "unrestorable_reasons": {n: unrestorable_reason(n, mapping.get(n)) for n in bad}}
```

- [ ] **Step 3: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests/test_mcp_config.py -q` → 전부 PASS(기존 `restorable` 테스트 넷 포함).

- [ ] **Step 4b: 변조 확인** — `plan4-task-07.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 축 분리 | `cfg.get("type") in ("http", "sse")` → `("http", "sse", "stdio")` | `legacy` 케이스 |
| M2 | 훅 호출 계약 | `restorable`을 옛 독립 구현으로 되돌린다 | `derived_from_the_reason_function` |
| M3 | `{}` vs `None` | `if isinstance(cfg, dict):` 가드 제거 | `broken` 케이스가 `AttributeError` |
| M4 | 입력 축 | 파라미터에서 `ok` 둘을 뺀다 | 리뷰가 잡는다 — 등가 변조로 명세에 적는다 |
| M5 | I/O 층 | `unrestorable_report`의 `sorted(...)` 제거 + 사유 맵을 `names` 전체로 | `report_pairs` |

- [ ] **Step 5: Commit**

```bash
git add plugins/claude-sync/lib/mcp_config.py plugins/claude-sync/tests/test_mcp_config.py docs/superpowers/mutations/plan4-task-07.json
git commit -m "feat(mcp): 복원 불가 사유를 판정 함수 하나에서 낸다"
```

---

### Task 8: 세 스크립트가 `unrestorable`·사유를 싣고, status·restore가 그것을 말한다

**근거:** spec 4.2 — **등급: 경**(보고. 삭제를 권하는 자리는 Task 10이다)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-status/scripts/compare_mcp.py`, `plugins/claude-sync/skills/sync-backup/scripts/collect_mcp.py`, `plugins/claude-sync/skills/sync-restore/scripts/plan_mcp.py`
- Modify: `plugins/claude-sync/skills/sync-status/SKILL.md` (2단계 MCP 절, 169행 문단 뒤), `plugins/claude-sync/skills/sync-restore/SKILL.md` (6-3)
- Test: `plugins/claude-sync/tests/test_mcp_scripts.py`, `plugins/claude-sync/tests/test_skill_wiring.py`

- [ ] **Step 1: 실패하는 테스트**

`tests/test_mcp_scripts.py`에 더한다(`test_compare_reports_three_buckets` 아래). 그리고 `test_compare_converges_when_local_secret_is_plaintext`의 `out == {...}` 정확 비교에 `"unrestorable": [], "unrestorable_reasons": {}`를 더한다:

```python
GARBAGE = {"url": "https://mcp.example/mcp", "type": "HTTP"}    # 2.x가 긁어 넣은 형태


def test_compare_reports_unrestorable_with_reasons(tmp_path):
    """spec 4.2 — status가 `only_repo` 중 어느 기기도 복원할 수 없는 것을 가른다."""
    local = write_local(tmp_path, {"mine": A})
    repo = write_repo(tmp_path, {"claude.ai Slack": GARBAGE, "theirs": B})
    out = compare_mcp.compare(os.path.join(repo, mc.BACKUP_RELPATH), claude_json_path=local)
    assert out["only_repo"] == ["claude.ai Slack", "theirs"]
    assert out["unrestorable"] == ["claude.ai Slack"]
    assert set(out["unrestorable_reasons"]) == {"claude.ai Slack"}
    assert "이름 규칙" in out["unrestorable_reasons"]["claude.ai Slack"]


@pytest.mark.parametrize("base", [None, {"mine": A}], ids=["no_base", "with_base"])
def test_collect_lists_unrestorable_repo_only_entries_with_or_without_base(tmp_path, base):
    """spec 4.2 — backup은 **병합 결과**에서 뽑는다. 기준선이 없으면 레포 전용 항목이
    `repo_ahead`에 실리지 않으므로(합집합 degrade) 거기서 뽑으면 놓친다.

    로컬에 있는 이름은 이름이 이상해도 오지 않는다 — 사용자의 실제 설정이다.
    """
    local = write_local(tmp_path, {"mine": A, "odd name": {"command": "x"}})
    repo = write_repo(tmp_path, {"mine": A, "claude.ai Slack": GARBAGE, "theirs": B})
    out = collect_mcp.collect(repo, str(tmp_path / "staging"), claude_json_path=local,
                              base_dir=write_base_blob(tmp_path, base))
    assert out["unrestorable"] == ["claude.ai Slack"]
    assert list(out["unrestorable_reasons"]) == ["claude.ai Slack"]
    if base is None:
        assert out["repo_ahead"] == {"present": [], "absent": []}     # degrade — 여기엔 없다
    else:
        assert "claude.ai Slack" in out["repo_ahead"]["absent"]


def test_plan_reasons_cover_exactly_the_unrestorable_bucket(tmp_path):
    local = write_local(tmp_path, {})
    repo = write_repo(tmp_path, {"claude.ai Notion": GARBAGE, "pw": {"command": "npx"}})
    out = plan_mcp.build_plan(os.path.join(repo, mc.BACKUP_RELPATH), claude_json_path=local,
                              base_dir=write_base_blob(tmp_path, None))
    assert bucket(out, "unrestorable") == ["claude.ai Notion"]
    assert set(bucket(out, "unrestorable_reasons")) == {"claude.ai Notion"}
    assert out["configs"] == {"pw": {"command": "npx"}}


def test_status_and_restore_give_the_same_reason_for_the_same_name(tmp_path):
    """플러그인 쪽과 같은 규칙(test_plugin_scripts) — 두 스킬이 같은 키에 같은 사유를 말한다."""
    local = write_local(tmp_path, {})
    repo = write_repo(tmp_path, {"claude.ai Notion": GARBAGE})
    path = os.path.join(repo, mc.BACKUP_RELPATH)
    status = compare_mcp.compare(path, claude_json_path=local)
    plan = plan_mcp.build_plan(path, claude_json_path=local, base_dir=write_base_blob(tmp_path, None))
    assert status["unrestorable_reasons"] == bucket(plan, "unrestorable_reasons")
```

`tests/test_mcp_scripts.py`의 `test_plan_is_two_layered_like_the_plugin_plan`(Task 6)에서 `set(out["sections"][...]) == set(mc.restore_plan(...))`을 `== set(mc.restore_plan({}, {}, None)) | {"unrestorable_reasons"}`로 바꾼다.

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_mcp_scripts.py -q -k "unrestorable or same_reason or converges or two_layered"` → FAIL.

- [ ] **Step 2: 구현 — 스크립트 셋**

`compare_mcp.compare`:

```python
    local = mc.read_local_servers(claude_json_path)
    repo = mc.load_backup(backup_path)
    out = {"status": "ok"}
    out.update(mc.diff(local, repo))
    # **restore가 새 항목으로 훑는 집합**에서 뽑는다(spec 4.2) — MCP에는 보류가 없어
    # only_repo와 같지만 플러그인 쪽(compare_plugins의 route_new_for)과 같은 함수를 부른다.
    # 어느 기기도 복원할 수 없는 항목에 "restore가 설치합니다"라고 말하지 않기 위한 축이다.
    out.update(mc.unrestorable_report(mc.route_new_keys(local, repo), repo))
    return out
```

`collect_mcp.collect`의 `out = {...}` dict에 두 키를 더한다(`"repo_ahead": ...` 줄 뒤):

```python
        # **병합 결과에서 뽑는다 — repo_ahead가 아니다**(spec 4.2). 기준선이 없으면 레포
        # 전용 항목이 repo_ahead에 실리지 않으므로(합집합 degrade) 거기서 뽑으면 놓친다.
        # "로컬에 없다"(이 기기 것이 아니다) ∩ "복원 불가"(어느 기기도 재현할 수 없다)가
        # 6.5단계의 정리 후보다. 순수 계산이라 쓰기 뒤에 있어도 된다.
        **mc.unrestorable_report([n for n in servers if n not in local], servers),
```

`plan_mcp.build_plan`의 `"sections": {section: dict(plan)}`을:

```python
    buckets = dict(plan)
    # 사유는 버킷과 같은 층(섹션 안)에 둔다 — plan_plugins가 unrestorable_reasons를 섹션 안에
    # 두는 것과 맞춘다. unrestorable_report가 목록을 다시 내지만 restore_plan의 버킷과 같은
    # 집합이다(아래 테스트가 건다) — 사유만 따로 만들면 갈릴 자리가 생긴다.
    buckets.update(mc.unrestorable_report(plan["unrestorable"], masked))
    ...
        "sections": {section: buckets},
```

- [ ] **Step 3: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests/test_mcp_scripts.py plugins/claude-sync/tests/test_mcp_cycle.py -q` → 전부 PASS.

- [ ] **Step 4: 산문 — status 2단계 MCP 절, restore 6-3**

`sync-status/SKILL.md` 2단계 MCP 절의 "… 세 목록이 모두 비어 있으면 "MCP 서버: 동일"이라고 보고한다." 문단 **뒤**에 문단을 더한다:

```
`only_repo`의 항목 중 **`unrestorable`에 있는 것**에는 "restore가 설치합니다"라고 말하지 않는다 — 어느 기기도 복원할 수 없는 항목이다(이름이 CLI 규칙을 어겼거나 `command`도 `url`+`type`도 없다. 2.x가 `claude mcp list` 출력을 긁어 넣은 계정 커넥터와 v1 승격 항목이 그 형태다). `unrestorable_reasons[<이름>]`의 사유 문장을 그대로 보여주고 이렇게 말한다: "어느 기기도 복원할 수 없는 항목입니다. `/sync-backup`이 레포에서 정리할지 묻습니다." 이 맵의 키는 `unrestorable`과 **같은 집합**이다 — 사유 없는 항목은 결함이지 침묵할 자리가 아니다. "동일"의 판정은 다섯 필드가 전부 빈 것이다.
```

같은 절의 "세 목록이 모두 비어 있으면"을 "다섯 필드가 모두 비어 있으면"으로 고친다.

`sync-restore/SKILL.md` 6-3의 문단 끝에 더한다:

```
사유는 `sections["servers"]["unrestorable_reasons"]`에 있다 — 항목마다 그 문장을 그대로 보여준다. **레포에서 정리하려면 `/sync-backup`이 6.5단계에서 제안한다** — restore는 레포에 쓰지 않는다.
```

- [ ] **Step 5: 산문 가드**

`tests/test_skill_wiring.py`에 더한다(`status_only_repo_guidance` 근처):

```python
def status_mcp_section():
    """status 2단계의 **MCP 반쪽**. status_plugin_section의 짝이다."""
    text = read_skill("sync-status")
    start = text.index("MCP 서버 비교:")
    return text[start:text.index("### 3. 결과 요약", start)]


def test_status_and_restore_say_who_prunes_unrestorable_mcp_entries():
    """spec 4.2 — status는 `unrestorable`에 "restore가 설치합니다"를 붙이지 않고, 둘 다
    정리의 주체(`/sync-backup`)를 가리킨다. restore는 레포에 쓰지 않는다고 말한다."""
    mcp = status_mcp_section()
    assert "`unrestorable`" in mcp and "`unrestorable_reasons`" in mcp
    assert "`/sync-backup`이 레포에서 정리할지 묻습니다" in mcp
    assert "같은 집합" in mcp, "사유 맵의 키가 목록과 같다는 계약이 없다"
    sub = subsection("sync-restore", "6-3.")
    assert '`sections["%s"]["unrestorable_reasons"]`' % mc.SECTIONS[0] in sub
    assert "`/sync-backup`이 6.5단계에서 제안한다" in sub
    assert "restore는 레포에 쓰지 않는다" in sub
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS.

- [ ] **Step 4b: 변조 확인** — `plan4-task-08.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 훅 호출 계약 | `collect_mcp`: `[n for n in servers if n not in local]` → `result["repo_ahead"]` | `with_or_without_base[no_base]` |
| M2 | 축 분리 | `collect_mcp`: `if n not in local` 조건 제거 | `"odd name"`이 실려 `== ["claude.ai Slack"]` 실패 |
| M3 | `{}` vs `None` | `compare_mcp`: `mc.route_new_keys(local, repo)` → `out["only_local"]` | `compare_reports_unrestorable_with_reasons` |
| M4 | 입력 축 | 픽스처에서 `theirs`(복원 가능한 레포 전용 대조군)를 뺀다 | `only_repo` 단정 |
| M5 | I/O 층 | status 산문에서 "`/sync-backup`이 레포에서 정리할지 묻습니다"를 "restore가 설치합니다"로 | `say_who_prunes` |

- [ ] **Step 6: docstring 점검** — `mcp_config.diff` docstring("코어의 held 키는 걸러낸다 …")에 "`unrestorable`은 이 함수가 아니라 소비자가 `unrestorable_report`로 얹는다"를 한 줄 더한다. `compare_mcp.py` module docstring의 "판정은 mcp_config.diff 하나만 쓴다"를 "diff와 unrestorable_report 둘"로.

- [ ] **Step 7: Commit**

```bash
git add plugins/claude-sync/skills/sync-status/scripts/compare_mcp.py plugins/claude-sync/skills/sync-backup/scripts/collect_mcp.py \
        plugins/claude-sync/skills/sync-restore/scripts/plan_mcp.py plugins/claude-sync/lib/mcp_config.py \
        plugins/claude-sync/skills/sync-status/SKILL.md plugins/claude-sync/skills/sync-restore/SKILL.md \
        plugins/claude-sync/tests/test_mcp_scripts.py plugins/claude-sync/tests/test_skill_wiring.py \
        docs/superpowers/mutations/plan4-task-08.json
git commit -m "feat(mcp): 세 스크립트가 복원 불가 항목과 사유를 싣고 status·restore가 정리의 주체를 가리킨다"
```

---

### Task 9: `prune_mcp.py` — 레포에서 지우는 유일한 경로, 그리고 고정점

**근거:** spec 4.3 · 4.6 — **등급: 중**(레포 파일을 지운다)

**Files:**
- Create: `plugins/claude-sync/skills/sync-backup/scripts/prune_mcp.py`
- Test: `plugins/claude-sync/tests/test_mcp_scripts.py`, `plugins/claude-sync/tests/test_mcp_cycle.py`

- [ ] **Step 1: 실패하는 테스트 — 스크립트 계약**

`tests/test_mcp_scripts.py` 상단 import에 `import prune_mcp  # noqa: E402`를 더하고(경로는 이미 `sync-backup/scripts`가 들어 있다), 파일 끝에 더한다:

```python
# --------------------------------------------------------- prune_mcp (spec 4.3)

def test_prune_removes_only_the_named_unrestorable_repo_only_entries(tmp_path):
    local = write_local(tmp_path, {"mine": A})
    repo = write_repo(tmp_path, {"mine": A, "claude.ai Slack": GARBAGE, "claude.ai Gmail": GARBAGE})
    out = prune_mcp.prune(repo, ["claude.ai Slack"], claude_json_path=local)
    assert out == {"status": "ok", "pruned": ["claude.ai Slack"], "not_found": [], "refused": {}}
    assert repo_servers(repo) == {"mine": A, "claude.ai Gmail": GARBAGE}


def test_prune_refuses_local_and_restorable_names_instead_of_deleting_them(tmp_path):
    """잘못 배선된 이름을 조용히 지우지 않는다 — 거부는 예외가 아니라 출력이다."""
    local = write_local(tmp_path, {"mine": A, "odd name": {"command": "x"}})
    repo = write_repo(tmp_path, {"mine": A, "odd name": {"command": "x"},
                                 "theirs": B, "claude.ai Slack": GARBAGE})
    before = open(os.path.join(repo, mc.BACKUP_RELPATH), encoding="utf-8").read()
    out = prune_mcp.prune(repo, ["odd name", "theirs", "ghost"], claude_json_path=local)
    assert out["pruned"] == [] and out["not_found"] == ["ghost"]
    assert set(out["refused"]) == {"odd name", "theirs"}
    assert "로컬" in out["refused"]["odd name"] and "복원 가능" in out["refused"]["theirs"]
    assert open(os.path.join(repo, mc.BACKUP_RELPATH), encoding="utf-8").read() == before


def test_prune_touches_neither_base_nor_staging(tmp_path):
    local = write_local(tmp_path, {"mine": A})
    repo = write_repo(tmp_path, {"mine": A, "claude.ai Slack": GARBAGE})
    base_dir = write_base_blob(tmp_path, {"mine": A})
    before = open(os.path.join(base_dir, mc.BACKUP_RELPATH), "rb").read()
    prune_mcp.prune(repo, ["claude.ai Slack"], claude_json_path=local)
    assert open(os.path.join(base_dir, mc.BACKUP_RELPATH), "rb").read() == before
    assert not os.path.exists(str(tmp_path / "staging"))


def test_prune_is_a_document_level_skip_on_a_broken_repo_document(tmp_path):
    repo = write_broken_repo(tmp_path, {"mine": A, "claude.ai Slack": GARBAGE})
    path = os.path.join(repo, mc.BACKUP_RELPATH)
    before = open(path, encoding="utf-8").read()
    with pytest.raises(mc.BrokenBackupSyntax):
        prune_mcp.prune(repo, ["claude.ai Slack"], claude_json_path=write_local(tmp_path, {}))
    assert open(path, encoding="utf-8").read() == before


def test_prune_cli_exits_zero_and_reports_skipped_on_a_broken_document(tmp_path):
    repo = write_broken_repo(tmp_path, {"mine": A})
    home = tmp_path / "home"
    home.mkdir()
    (home / ".claude.json").write_text(json.dumps({"mcpServers": {}}), encoding="utf-8")
    script = os.path.join(SCRIPTS_DIR, "sync-backup", "scripts", "prune_mcp.py")
    proc = subprocess.run([sys.executable, script, repo, "claude.ai Slack"],
                          capture_output=True, text=True, env=dict(os.environ, HOME=str(home)))
    assert proc.returncode == 0, proc.stderr
    out = json.loads(proc.stdout)
    assert out["status"] == "skipped" and out["reason_kind"] == "broken_syntax"
```

- [ ] **Step 2: 실패하는 테스트 — 고정점(spec 4.6)**

`tests/test_mcp_cycle.py`: 상수 `PRUNE = os.path.abspath(os.path.join(SKILLS, "sync-backup", "scripts", "prune_mcp.py"))`를 `PLAN` 아래에, `Device`에 메서드를, 파일 끝에 테스트 둘을 더한다:

```python
    def prune(self, names):
        """SKILL.md 6.5단계 「레포에서 정리한다」."""
        return json.loads(self._run(PRUNE, self.repo, *names))
```

```python
GARBAGE = {"url": "https://mcp.example/mcp", "type": "HTTP"}    # 2.x가 긁어 넣은 형태


def test_pruned_garbage_does_not_come_back(tmp_path):
    """spec 4.6 #1·#2 — 지운 뒤 두 번 더 백업해도 되살아나지 않고 보고가 비어 있다.

    3회차와 2회차가 바이트 동일이어야 고정점이다. 기준선이 없는 첫 회차(합집합 degrade)에서도
    목록이 나오는 것이 이 시나리오의 첫 단정이다.
    """
    dev = make_device(tmp_path, {"a": A}, repo_init={"a": A, "claude.ai G": GARBAGE})
    assert dev.backup()["unrestorable"] == ["claude.ai G"]          # base 없음 — 그래도 실린다
    assert dev.prune(["claude.ai G"])["pruned"] == ["claude.ai G"]
    snapshots = []
    for _ in range(2):
        report = dev.backup()
        assert report["unrestorable"] == [] and report["deleted"] == []
        assert report["repo_ahead"] == {"present": [], "absent": []}
        snapshots.append((repo_servers(dev.repo), dev.base()))
    assert snapshots[0] == snapshots[1] == ({"a": A}, {"a": A})


def test_a_device_that_really_has_the_server_is_asked_and_brings_it_back(tmp_path):
    """spec 4.6 #3 — 다른 기기가 실제로 쓰는 서버를 정리했더라도 조용히 사라지지 않는다.

    그 기기에서는 케이스 4(`local_stale`)로 **묻고**, 「유지」를 고르면 다음 백업이 진짜 값을
    레포에 되돌리며, 되살아난 값은 복원 가능하다. 그 기기의 백업이 레포의 쓰레기 값을 만났을
    때 `unrestorable`에 싣지 않는 것(로컬에 있다)도 여기서 함께 건다.
    """
    real = {"command": "g"}
    dev = make_device(tmp_path, {"a": A, "g": real}, repo_init={"a": A, "g": real})
    dev.backup()                                      # base {a, g(real)}
    set_repo(dev.repo, {"a": A, "g": GARBAGE})        # 2.x가 덮어쓴 흔적
    report = dev.backup()
    assert report["repo_ahead"]["present"] == ["g"]   # 케이스 8 — 레포 값이 남는다
    assert report["unrestorable"] == []               # 이 기기의 로컬에 있으므로 정리 후보가 아니다
    set_repo(dev.repo, {"a": A})                      # 다른 기기가 6.5단계에서 정리했다
    plan = dev.restore(keep_stale=["g"])
    assert bucket(plan, "local_stale") == ["g"]       # 묻는 자리 — 조용한 삭제가 아니다
    assert dev.local()["g"] == real
    report = dev.backup()
    assert repo_servers(dev.repo)["g"] == real and report["unrestorable"] == []
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_mcp_scripts.py plugins/claude-sync/tests/test_mcp_cycle.py -q -k "prune or come_back or brings_it_back"` → `ImportError`/FAIL.

- [ ] **Step 3: 구현 — `prune_mcp.py`**

```python
#!/usr/bin/env python3
"""레포의 mcp-servers.json에서 복원 불가 항목을 지운다 — 백업 6.5단계의 「레포에서 정리한다」.

사용: prune_mcp.py <레포 경로> <이름> [<이름> ...]
출력: {"status": "ok", "pruned": [...], "not_found": [...], "refused": {이름: 사유}}

**레포에서 그 항목이 나갈 수 있는 유일한 경로다**(spec 4.3). 어느 기기의 로컬에도 없는
항목은 base에 실릴 수 없고, 삭제 전파는 "base에 있고 로컬에서 사라짐"이라야 일어나므로
병합으로는 영원히 남는다(실측 — 2026-09-01, 2.x가 긁어 넣은 계정 커넥터 7개).

**거부 둘** — 잘못 배선된 이름을 조용히 지우지 않는다. 거부는 예외가 아니라 출력이다.
로컬에 있는 이름(사용자의 실제 설정이다)과 복원 가능한 이름(정리 대상이 아니다)은 지우지
않고 `refused`에 사유와 함께 싣는다. 이름은 6.5단계가 6단계 출력의 `unrestorable`을 그대로
넘기므로 정상 흐름에서 거부는 비어 있다.

**base·스테이징은 건드리지 않는다.** 지워지는 이름은 로컬에 없어 next_base에 애초에 실리지
않는다(keyed_sync._next_base_normalized — merged에 있고 local에 없으면 이전 base에 있을
때만 유지되는데, 로컬이 동의한 적 없는 이름은 base에 없다). test_mcp_cycle의 고정점이
그것을 건다. 커밋·푸시는 10단계가 한다 — 별도 커밋을 만들지 않는다.
"""
import json
import os
import sys

sys.path.insert(
    0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "..", "..", "lib")
)
import report  # noqa: E402
import mcp_config as mc  # noqa: E402


def prune(repo_path, names, claude_json_path=None):
    """names 중 레포에 있고·로컬에 없고·복원 불가인 것만 지운다. 지운 것이 없으면 쓰지 않는다.

    load_backup이 구문 깨짐·미지 스키마에 예외를 던지므로 **쓰기 앞에서** 멈춘다 — 그 갈래의
    skipped("레포 파일은 손대지 않았다")가 참이다. 쓰기는 dump_backup(원자적)이다.
    """
    local = mc.read_local_servers(claude_json_path)
    repo_file = os.path.join(repo_path, mc.BACKUP_RELPATH)
    repo = mc.load_backup(repo_file)
    pruned, not_found, refused = [], [], {}
    for name in dict.fromkeys(names):            # 순서 유지, 중복 무시
        if name not in repo:
            not_found.append(name)
        elif name in local:
            refused[name] = "로컬에 있는 서버는 지우지 않는다 — 사용자의 실제 설정이다"
        elif mc.restorable(name, repo[name]):
            refused[name] = "복원 가능한 항목은 지우지 않는다 — 정리 대상이 아니다"
        else:
            pruned.append(name)
    if pruned:
        mc.dump_backup({n: cfg for n, cfg in repo.items() if n not in pruned}, repo_file)
    return {"status": "ok", "pruned": pruned, "not_found": not_found, "refused": refused}


def main():
    if len(sys.argv) < 3:
        print("사용: prune_mcp.py <레포 경로> <이름> [<이름> ...]", file=sys.stderr)
        sys.exit(1)
    try:
        out = prune(sys.argv[1], sys.argv[2:])
    # 다른 MCP 스크립트 셋과 같은 튜플. 갈리면 한쪽만 traceback으로 죽는다.
    except (mc.LocalConfigUnavailable, mc.UnknownBackupSchema,
            mc.BrokenBackupSyntax, OSError, ValueError) as e:
        out = report.skipped(e)
        print("정리 건너뜀: %s" % e, file=sys.stderr)
    print(json.dumps(out, indent=2, ensure_ascii=False))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS. `test_user_docs.py::test_no_production_code_reads_the_metadata_files_map`의 `production_sources()` 개수 단정(≥10)은 파일이 늘어 여전히 참이다.

- [ ] **Step 4b: 변조 확인** — `plan4-task-09.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 축 분리 | `refused`의 두 사유 문장을 맞바꾼다 | `refuses_local_and_restorable_names` |
| M2 | 훅 호출 계약 | `elif name in local:` 가드 제거 | 같은 테스트(`odd name`이 지워진다) |
| M3 | `{}` vs `None` | `if name not in repo: not_found` → `pruned.append(name)` | `not_found == ["ghost"]` |
| M4 | I/O 층 | `if pruned:` 가드 제거(아무것도 안 지워도 다시 쓴다) | `before ==` 바이트 단정 |
| M5 | 입력 축 | 고정점 테스트의 회차 `range(2)` → `range(1)` | 리뷰가 잡는다(등가) — 대신 `snapshots[0] == snapshots[1]`이 IndexError로 죽는지 확인 |

- [ ] **Step 5: docstring 점검** — `mcp_config.next_base` docstring의 "update_base.py가 파일 단위로 지키는 불변식과 같다" 문단에 "prune_mcp가 지운 이름은 로컬에 없어 이 규칙으로 base에서 저절로 빠진다(spec 4.3)"를 더한다.

- [ ] **Step 6: Commit**

```bash
git add plugins/claude-sync/skills/sync-backup/scripts/prune_mcp.py plugins/claude-sync/lib/mcp_config.py \
        plugins/claude-sync/tests/test_mcp_scripts.py plugins/claude-sync/tests/test_mcp_cycle.py \
        docs/superpowers/mutations/plan4-task-09.json
git commit -m "feat(backup): prune_mcp.py — 어느 기기도 복원할 수 없는 서버를 레포에서 지우는 유일한 경로"
```

---

### Task 10: backup 6.5단계 — 보여주고, 묻고, 지우고, 10단계가 푸시한다

**근거:** spec 4.4 · 4.5 — **등급: 중**(삭제를 권하는 산문)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-backup/SKILL.md` (6단계 표 뒤, **6.5단계 신설**, 12단계 보고)
- Test: `plugins/claude-sync/tests/test_skill_wiring.py` (`COMPAT_WIRING`, 가드 셋)

- [ ] **Step 1: 실패하는 가드**

`COMPAT_WIRING["sync-backup"]["before_calls"]`의 `COLLECT_MCP_CALL` 다음에 `PRUNE_CALL`을 넣고(상수는 `COLLECT_MCP_CALL` 아래 정의), `test_backup_base_gate_covers_both_relpaths` 아래에 더한다:

```python
PRUNE_CALL = 'python3 "$SYNC_SCRIPTS/prune_mcp.py" "$SYNC_REPO"'


def test_backup_prunes_between_mcp_collection_and_the_marker():
    """spec 4.4 — 6.5단계는 6단계(정리 후보를 내는 곳) 뒤, 7단계(표식) 앞이다. 10단계의
    커밋이 그 삭제를 함께 올린다."""
    text = read_skill("sync-backup")
    for anchor in ("### 6. mcp-servers.json", "### 6.5 복원 불가 항목 정리", "### 7. sync-metadata.json"):
        assert anchor in text, anchor
    assert (text.index("### 6. mcp-servers.json") < text.index("### 6.5 복원 불가 항목 정리")
            < text.index("### 7. sync-metadata.json"))
    assert (index_of(text, COLLECT_MCP_CALL, "sync-backup") < index_of(text, PRUNE_CALL, "sync-backup")
            < index_of(text, GENERATE_METADATA_CALL, "sync-backup"))


def test_backup_prune_step_asks_once_with_both_notices_and_suppresses_on_downgrade():
    """spec 4.4·4.5 — 목록 전체에 한 번 묻고, 고지 둘(판정이 버전에 묶여 있다 / 다른 기기가
    실제로 쓰면 묻는다)을 담고, 다운그레이드가 의심되면 정리 선택지를 주지 않는다."""
    sec = section("sync-backup", "6.5 복원 불가 항목 정리")
    assert "`unrestorable`" in sec and "`unrestorable_reasons`" in sec
    assert "레포에서 정리한다" in sec and "이번엔 넘어간다" in sec
    assert "목록 전체에 한 번 묻는다" in sec
    assert "이 버전이 아는 형식" in sec, "판정이 버전에 묶여 있음을 고지하지 않는다"
    assert "묻고" in sec and "조용히 사라지지 않습니다" in sec
    assert '`files["%s"]`' % mc.BACKUP_RELPATH in sec and "`downgrade_suspected`" in sec
    assert "정리 선택지를 주지 않는다" in sec
    assert "`pruned`" in sec and "`refused`" in sec
    assert "기억하지 않는다" in sec
    # 4.6과 같은 자리의 실수를 막는다 — 그 문서 하나만 본다.
    assert '`files["%s"]`' % pc.BACKUP_RELPATH not in sec, "plugins.json 판정으로 억제한다"


def test_backup_mcp_table_excepts_unrestorable_from_the_install_promise():
    """spec 4.2 — `repo_ahead.absent`의 "restore가 설치합니다"를 복원 불가 이름에는 쓰지 않는다."""
    sec = section("sync-backup", "6. mcp-servers.json 생성")
    assert "`unrestorable`에 있는 이름에는 쓰지 않는다" in sec
    assert "6.5단계" in sec


def test_backup_report_lists_what_was_pruned():
    sec = section("sync-backup", "12. 결과 보고")
    assert "`pruned`" in sec and "`refused`" in sec
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_skill_wiring.py -q -k "prune or install_promise or what_was_pruned or before_calls"` → FAIL.

- [ ] **Step 2: 6단계 표 예외와 12단계 보고**

6단계 표 바로 아래 문단("`repo_ahead.present`(케이스 8)에 케이스 2와 같은 문구 …") **앞**에 더한다:

```
`repo_ahead.absent`(케이스 2)의 문구는 **`unrestorable`에 있는 이름에는 쓰지 않는다** — "restore가 이 기기에 설치합니다"가 거짓이다(어느 기기도 설치할 수 없다). 그 이름과 `unrestorable_reasons`의 사유는 6.5단계가 다룬다.
```

12단계의 "백업 완료 후 변경된 파일 목록과 결과를 사용자에게 요약해서 보여준다." 뒤에 더한다:

```
6.5단계에서 정리했으면 **레포에서 지운 서버**(`pruned`)를 이름으로 적고, `refused`가 있었으면 그 이름과 사유도 적는다.
```

- [ ] **Step 3: 6.5단계 신설**

6단계의 마지막 줄("충돌이 있어도 백업 전체를 막지 않는다. 해당 서버만 건너뛴다.") 뒤, `### 7. sync-metadata.json 생성` 앞에 넣는다:

```
### 6.5 복원 불가 항목 정리 — 어느 기기도 되살릴 수 없는 서버를 레포에서 지운다

**7단계보다 앞이다.** 10단계의 커밋이 이 삭제를 함께 올린다 — 별도 커밋을 만들지 않는다.

6단계 출력의 `unrestorable`은 **병합 결과 중 로컬에 없고 복원할 수 없는 서버**다 — 이름이 CLI 규칙을 어겼거나(2.x가 `claude mcp list` 출력을 긁어 넣은 `claude.ai *` 계정 커넥터가 이 형태다) `command`도 `url`+`type`도 없다(v1 승격 항목). 어느 기기의 로컬에도 없으므로 base에 실릴 수 없고, 삭제 전파는 "base에 있고 로컬에서 사라짐"이라야 일어나므로 **이 단계 말고는 레포에서 나갈 경로가 없다**(실측 — 2026-09-01, 7개가 매 실행 거짓 문구를 냈다). `unrestorable_reasons[<이름>]`이 항목마다의 사유다.

**비어 있으면 조용히 지나간다.** 6단계가 `skipped`였으면 목록 자체가 없다 — 역시 지나간다.

**비어 있지 않으면, 먼저 4.5단계가 낸 `files["mcp-servers.json"]`의 `downgrade_suspected`를 본다.** 참이면 레포는 v1 승격 항목투성이라 **전부** 여기 실린다 — 그 자리에서 정리를 제안하면 다른 기기가 실제로 쓰는 서버의 이름까지 지운다. 목록과 사유는 보여주되 **정리 선택지를 주지 않는다**: "먼저 4.5단계의 복구를 하세요. 복구 뒤에는 이 목록이 사라집니다." **그 문서 하나만 본다** — restore 6-5의 억제와 같은 규칙이다.

거짓이면 이름과 사유를 **전부** 보여주고 **두 선택지**를 준다. **목록 전체에 한 번 묻는다** — 항목마다 사용자가 할 일이 같다.

물을 때 반드시 함께 적는다:

> "「복원 불가」는 **이 버전이 아는 형식** 기준입니다. 상위 버전의 플러그인이 더한 형식이라면 그 기기에서 `/sync-backup`을 돌렸을 때 복원 가능한 값으로 돌아오니, 확신이 없으면 넘어가도 됩니다."
>
> "다른 기기가 이 서버를 실제로 쓰고 있다면, 그 기기의 다음 `/sync-restore`가 「다른 기기가 삭제했습니다」로 **묻고**, 「유지」를 고르면 그 기기의 진짜 설정으로 레포에 되돌아옵니다. 조용히 사라지지 않습니다."

첫 문장이 **조용한 자동 삭제를 하지 않는 이유**다 — `restorable()`의 지식은 버전에 묶여 있고, 상위 버전이 새 transport를 더해도 `SCHEMA_VERSION`은 오르지 않으므로 이 버전이 그것을 "복원 불가"로 읽는다. 확인을 받는 것이 그 안전장치다.

- **레포에서 정리한다** —
  ```bash
  SYNC_REPO="${TMPDIR:-/tmp}/claude-sync-repo"
  python3 "$SYNC_SCRIPTS/prune_mcp.py" "$SYNC_REPO" "<이름>" "<이름>" ...
  ```
  `<이름>`은 6단계 출력의 `unrestorable` 목록 **그대로**다 — 다른 이름을 넣지 않는다. 출력의 `pruned`가 지운 것, `not_found`가 레포에 없던 것, `refused`가 거부한 것이다(로컬에 있는 서버·복원 가능한 항목 — 잘못 넣은 이름은 조용히 지워지지 않는다). `refused`가 비어 있지 않으면 그 이름과 사유를 보여준다. `status`가 `"skipped"`면 `reason`을 알리고 넘어간다 — 레포 파일은 손대지 않았다.
- **이번엔 넘어간다** — 아무것도 하지 않는다. 다음 백업에 다시 묻는다. **기억하지 않는다** — 다른 기기가 실제로 쓰는 서버라면 그 기기의 백업이 복원 가능한 값으로 바꿔 놓아 목록에서 사라지므로, 영구히 남는 것은 진짜 쓰레기뿐이다.

지워진 이름은 base·스테이징과 무관하다 — 로컬에 없어 `next_base`에 애초에 실리지 않는다. 10단계의 게이트는 바뀌지 않는다.
```

- [ ] **Step 4: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS. 특히 `test_no_anchor_swallows_another`·`test_each_anchor_witnesses_exactly_one_call_site`·`test_no_skill_branches_on_the_reason_sentence`.

- [ ] **Step 4b: 변조 확인** — `plan4-task-10.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 축 분리 | 6.5단계를 7단계 뒤로 옮긴다 | `prunes_between_mcp_collection_and_the_marker` |
| M2 | 입력 축 | 억제 문단에서 `files["mcp-servers.json"]`을 `files["plugins.json"]`으로 | `suppresses_on_downgrade`의 마지막 단정 |
| M3 | I/O 층 | 고지 인용문 둘째("묻고 … 조용히 사라지지 않습니다") 삭제 | 같은 테스트 |
| M4 | `{}` vs `None` | "정리 선택지를 주지 않는다"를 "정리 선택지를 그대로 준다"로 | 같은 테스트 |
| M5 | 훅 호출 계약 | prune 호출줄의 `"$SYNC_REPO"`를 `"$HOME/.claude"`로 | `before_calls_covers_every_gated_script_call` |

- [ ] **Step 5: Commit**

```bash
git add plugins/claude-sync/skills/sync-backup/SKILL.md plugins/claude-sync/tests/test_skill_wiring.py \
        docs/superpowers/mutations/plan4-task-10.json
git commit -m "feat(backup): 6.5단계 — 복원 불가 서버를 보여주고 묻고 레포에서 지운다"
```

---

### Task 11: `detect_downgrade.py`가 구문 손상을 진단하고, 세 스킬이 그 갈래를 읽는다

**근거:** spec 5.1 · 5.2 · 5.4 — **등급: 중**(backup 4.5단계가 레포 파일을 되돌린다)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-backup/scripts/detect_downgrade.py`
- Modify: `plugins/claude-sync/skills/sync-backup/SKILL.md` (4.5단계 전체), `plugins/claude-sync/skills/sync-restore/SKILL.md` (2.5의 「다운그레이드 탐지 결과」 소절), `plugins/claude-sync/skills/sync-status/SKILL.md` (1.5의 같은 소절)
- Test: `plugins/claude-sync/tests/test_downgrade.py`, `plugins/claude-sync/tests/test_skill_wiring.py`

- [ ] **Step 1: 실패하는 테스트 — 스크립트**

`tests/test_downgrade.py`의 `test_candidate_null_when_history_has_no_v2` 아래에 더한다. `test_corrupt_repo_is_skipped_not_reported_as_no_candidate`의 키 튜플에 `"broken_syntax"`를 더한다:

```python
# --- 구문 손상 (spec 5.1) ---

@pytest.mark.parametrize("relpath,commit,healthy", [
    (mc.BACKUP_RELPATH, commit_mcp, v2({"a": {"command": "a"}})),
    (pc.BACKUP_RELPATH, commit_plugins, p_v2()),
], ids=["mcp", "plugins"])
def test_broken_document_is_diagnosed_with_its_last_v2_commit(tmp_path, relpath, commit, healthy):
    """spec 5.1 — 구문 손상은 base 없이도 사실이고, 후보는 마지막 v2 커밋이며, 두 갈래는 배타다."""
    repo = make_repo(tmp_path)
    commit(repo, healthy, "backup: v2")
    commit(repo, healthy[:len(healthy) // 2], "backup: 잘린 쓰기")      # 정상 문서를 잘라 만든다
    out = dd.detect(str(repo), base_dir=base_dir_with(tmp_path))
    entry = out["files"][relpath]
    assert entry["status"] == "ok"
    assert entry["broken_syntax"] is True
    assert entry["downgrade_suspected"] is False
    assert entry["repo_shape"] == compat.SHAPE_BROKEN
    assert entry["candidate"]["subject"] == "backup: v2"
    other = next(r for r in dd.RELPATHS if r != relpath)
    assert out["files"][other]["broken_syntax"] is False           # 다른 문서는 무관하다


def test_working_tree_only_corruption_points_at_head(tmp_path):
    """커밋되지 않은 손상 — HEAD가 정상이면 HEAD가 후보다."""
    repo = make_repo(tmp_path)
    commit_mcp(repo, v2({"a": {"command": "a"}}), "backup: v2")
    (repo / mc.BACKUP_RELPATH).write_text("{", encoding="utf-8")
    entry = mcp_of(dd.detect(str(repo), base_dir=base_dir_with(tmp_path)))
    assert entry["broken_syntax"] is True
    assert entry["candidate"]["subject"] == "backup: v2"


def test_broken_document_without_any_v2_in_history_has_no_candidate(tmp_path):
    """"판정할 수 없었다"를 "사고가 없다"로 접지 않는다 — 후보가 없어도 손상은 참이다."""
    repo = make_repo(tmp_path)
    commit_mcp(repo, v1(["a"]), "backup: 2.x")
    commit_mcp(repo, "[{", "backup: 잘림")
    entry = mcp_of(dd.detect(str(repo), base_dir=base_dir_with(tmp_path)))
    assert entry["broken_syntax"] is True and entry["candidate"] is None
    assert entry["newer_schema_seen"] is False


def test_a_healthy_document_reports_broken_syntax_false(tmp_path):
    repo = make_repo(tmp_path)
    commit_mcp(repo, v2({"a": {"command": "a"}}), "backup")
    entry = mcp_of(dd.detect(str(repo), base_dir=base_dir_with(tmp_path)))
    assert entry["broken_syntax"] is False and entry["candidate"] is None
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_downgrade.py -q -k "broken or working_tree or healthy_document"` → `KeyError: 'broken_syntax'`.

- [ ] **Step 2: 구현 — 스크립트**

`detect_downgrade.py`: module docstring 첫 줄을 `"""레포 백업 문서 진단 — 다운그레이드 사고와 구문 손상 (읽기 전용).`로 하고, 둘째 단락 뒤에 한 단락을 더한다:

```
**구문 손상도 같은 루프에서 진단한다**(spec 5.1). `repo_shape == broken`이면 `broken_syntax`가
참이고, 그때도 마지막 v2 커밋을 후보로 낸다 — 깨진 문서에 필요한 것이 정확히 그것이다.
두 갈래는 배타다: SHAPE_BROKEN은 어느 문서의 옛 형식과도 다르므로 downgrade_suspected가
거짓이다. 구문 손상은 base 없이도 사실이다. 스크립트 이름은 바꾸지 않는다 — 가드 수십 개가
이름을 부른다(spec 11장).
```

`detect_file`과 `_skipped_file`을:

```python
def detect_file(repo_path, relpath, base_dir):
    """문서 하나의 판정. {"status", "reason", "downgrade_suspected", "broken_syntax",
    "repo_shape", "base_shape", "candidate", "newer_schema_seen"}
    (기존 docstring 본문 유지)
    """
    repo_shape = _shape_of_file(os.path.join(repo_path, relpath), relpath)
    base_shape = _base_shape(relpath, base_dir)
    suspected = compat.downgrade_suspected(repo_shape, base_shape, relpath)
    broken = repo_shape == compat.SHAPE_BROKEN
    out = {
        "status": "ok",
        "reason": None,
        "downgrade_suspected": suspected,
        "broken_syntax": broken,
        "repo_shape": repo_shape,
        "base_shape": base_shape,
        "candidate": None,
        "newer_schema_seen": False,
    }
    if suspected or broken:
        try:
            out["candidate"], out["newer_schema_seen"] = find_last_v2_commit(
                repo_path, relpath)
        except (RuntimeError, OSError) as e:
            # (기존 주석 유지)
            return _skipped_file(str(e), suspected, repo_shape, base_shape, broken)
    return out


def _skipped_file(reason, suspected=False, repo_shape=None, base_shape=None, broken=False):
    """(기존 docstring 유지)"""
    return {
        "status": "skipped",
        "reason": reason,
        "downgrade_suspected": suspected,
        "broken_syntax": broken,
        "repo_shape": repo_shape,
        "base_shape": base_shape,
        "candidate": None,
        "newer_schema_seen": False,
    }
```

- [ ] **Step 3: 통과 확인 — 스크립트만**

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_downgrade.py -q` → 전부 PASS. 이어서 `uv run --with pytest pytest plugins/claude-sync/tests/test_skill_wiring.py -q -k names_every_key` → **세 스킬 전부 FAIL**(`broken_syntax`를 부르지 않는다). 그것이 다음 스텝의 실패 테스트다.

- [ ] **Step 4: 산문 — backup 4.5단계**

제목을 `### 4.5 레포 문서 진단 — 다운그레이드·구문 손상`으로 바꾸고, `tests/test_skill_wiring.py`의 `DOWNGRADE_SECTION["sync-backup"]`을 `"4.5 레포 문서 진단"`으로 바꾼다. 첫 문단 "**수집 단계들보다 먼저 한다.** …"의 끝에 "구문이 깨진 문서도 여기서 잡는다 — 5·6단계가 그 문서를 건너뛰기 전에 되돌릴 기회를 주기 위해서다."를 더한다. 번호 목록 1(`status`가 `"skipped"`) 뒤에 **새 2번**을 넣고 기존 2~5를 3~6으로 민다:

```
2. **`broken_syntax`가 참이면** 레포의 `<relpath>`가 **JSON으로 읽히지 않는다** — 잘린 쓰기나 손으로 고치다 깨진 파일이다. 이 상태로는 5·6단계가 그 문서를 건너뛴다(`broken_syntax` 갈래). `downgrade_suspected`와 동시에 참일 수 없다 — 깨진 문서는 어느 형식도 아니다. 사용자에게 다음을 보여주고 고르게 한다. **문서마다 따로 묻는다.**
   1. 사고 사실: "백업 레포의 `<relpath>`가 JSON으로 읽히지 않습니다. 이 상태로는 이 문서의 백업이 건너뛰어집니다."
   2. `candidate`가 **있으면** 그 커밋의 `sha`·`date`·`subject`, 그리고 `entries`의 버킷별 항목 수 — 5번 갈래와 같은 형식이다. `newer_schema_seen`이 참이면 후보가 그보다 오래된 것임을 명시한다.
   3. 선택지 셋:
      - **복구한다** — 후보 커밋의 **그 문서**를 레포 작업본에 되돌려 놓고 백업을 계속한다. 5·6단계가 그 판본 위에 로컬을 정상적으로 병합한다. 복구 판본 이후 다른 기기가 올린 내용이 있었다면 그것은 손상된 파일 안에 있었고 이미 읽을 수 없다 — 복구가 새로 잃는 것은 없다.
        ```bash
        git -C "$SYNC_REPO" show "<sha>:<relpath>" > "$SYNC_REPO/<relpath>"
        ```
        **`<relpath>`는 지금 보고 있는 항목의 키다.** 한 문서 이름을 리터럴로 박으면 다른 문서의 복구가 엉뚱한 파일을 덮어쓴다.
      - **복구하지 않고 계속한다** — 그 문서만 건너뛴다(5·6단계의 `broken_syntax` 갈래).
      - **중단한다.**
   4. `candidate`가 **없으면**(git 이력에 이 버전이 읽을 수 있는 정상 판본이 없다) 사고는 알리되 복구는 제안하지 않는다 — 그 파일을 정상 JSON으로 되돌린 뒤 다시 실행하도록 안내한다. **후보를 못 찾은 경우에만 남는 마지막 수단이다.** 그냥 지우라고 안내하지 않는다 — 그 문서에만 있던 다른 기기의 항목이 레포에서 사라진다.

   **묻는 이유는 다운그레이드와 다르다.** 거기서는 옛 기기가 의도적으로 지운 항목까지 되살리기 때문이고, 여기서는 손상 직전 커밋과 손상 사이에 무슨 의도가 있었는지 이 기기가 모르기 때문이다 — 다른 기기가 서버 하나를 지우려다 쓰기가 잘렸다면 복구는 그 삭제 의도를 되돌린다.
```

- [ ] **Step 5: 산문 — restore 2.5·status 1.5의 진단 소절**

두 스킬의 소절 제목 `#### 다운그레이드 탐지 결과`를 `#### 레포 문서 진단 결과 — 다운그레이드·구문 손상`으로 바꾸고, `tests/test_skill_wiring.py`의 `DOWNGRADE_RESULT_SUBSECTION`을 `"레포 문서 진단 결과"`로 바꾼다. 두 소절 모두, `status`가 `"skipped"`면 … 문단 **뒤**·`downgrade_suspected`가 `false`면 문단 **앞**에 넣는다.

restore:

```
`broken_syntax`가 참이면 **레포의 `<relpath>`가 JSON으로 읽히지 않는다.** 이 실행에서는 그 문서를 건너뛴다(5·6절의 `broken_syntax` 갈래) — 파일 동기화와 다른 문서는 그대로 진행한다. **막지 않는다 — 알리고 계속한다.** `downgrade_suspected`와 동시에 참일 수 없다.

> "백업 레포의 `<relpath>`가 JSON으로 읽히지 않습니다. 이 실행에서는 그 문서를 건너뜁니다. **복구는 `/sync-backup`에서 합니다** — 4.5단계가 마지막 정상 판본으로 되돌릴지 묻습니다. restore는 리모트에 push하지 않으므로 여기서 레포를 고칠 수 없습니다."

`candidate`가 **있으면** 그 커밋의 `sha`·`date`·`subject`와 `entries`의 버킷별 항목 수를 참고용으로 보여준다("그 판본을 찾았습니다"). **없으면** "git 이력에 이 버전이 읽을 수 있는 정상 판본이 없습니다"까지 말한다. `newer_schema_seen`이 참이면 후보가 그보다 오래된 것임을 명시한다.
```

status는 같은 문단에서 인용문의 둘째 문장만 "**`/sync-backup`이 복구를 제안합니다** — 1.5단계가 아니라 그쪽 4.5단계가 마지막 정상 판본으로 되돌릴지 묻습니다. status는 복구하지 않습니다."로 바꾼다.

- [ ] **Step 6: 가드 갱신**

`tests/test_skill_wiring.py`의 `DOWNGRADE_BRANCHES`에 셋째 행으로 더하고, `test_the_downgrade_branch_table_did_not_shrink`의 `== 6`을 `== 7`로 바꾼다:

```python
    ("`broken_syntax`가 참이면", "JSON으로 읽히지 않",
     "구문 손상은 base 없이도 사실이다. 지워지면 5·6단계의 건너뜀에 이유가 없어진다(spec 5.1)"),
```

그리고 아래에 더한다:

```python
def test_backup_broken_branch_offers_repair_only_when_there_is_a_candidate():
    """spec 5.2 — 후보가 있으면 「복구한다」를 주고, 없으면 손으로 되돌리는 안내가 **거기에만** 남는다."""
    sec = DOWNGRADE_PROSE["sync-backup"]()
    branch = branch_slice(sec, "`broken_syntax`가 참이면")
    assert "**복구한다**" in branch and 'show "<sha>:<relpath>"' in branch
    assert "복구하지 않고 계속한다" in branch and "**중단한다.**" in branch
    assert "후보를 못 찾은 경우에만 남는 마지막 수단이다" in branch
    assert "그냥 지우라고 안내하지 않는다" in branch


@pytest.mark.parametrize("skill,pointer", [
    ("sync-restore", "**복구는 `/sync-backup`에서 합니다**"),
    ("sync-status", "**`/sync-backup`이 복구를 제안합니다**"),
])
def test_read_only_skills_point_the_broken_branch_at_backup(skill, pointer):
    """0장의 결정 — restore·status는 진단하고 backup을 가리킨다. 레포를 고치지 않는다."""
    branch = branch_slice(DOWNGRADE_PROSE[skill](), "`broken_syntax`가 참이면")
    assert pointer in branch
    assert 'show "<sha>' not in branch, "%s가 레포를 되돌린다" % skill
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS. `test_downgrade_prose_names_every_key_of_a_file_entry`·`test_restore_cites_the_backup_step_that_actually_recovers`(제목 첫 토큰 `4.5`는 그대로다)·`test_backup_recovery_command_restores_the_document_it_is_talking_about`(명령이 둘이어도 `in` 검사다)가 초록인지 본다.

- [ ] **Step 4b: 변조 확인** — `plan4-task-11.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | 축 분리 | `broken = repo_shape == compat.SHAPE_BROKEN` → `== compat.SHAPE_UNKNOWN` | `diagnosed_with_its_last_v2_commit` |
| M2 | 훅 호출 계약 | `if suspected or broken:` → `if suspected:` | 같은 테스트(`candidate` None) |
| M3 | `{}` vs `None` | `detect_file`의 `out` dict에서 `"broken_syntax": broken` 줄을 지운다(키 부재 = falsy = "사고 없음") | `test_a_healthy_document_reports_broken_syntax_false`가 `KeyError`로, `_skipped_file` 쪽 키는 `test_skipped_entry_has_exactly_the_keys_of_the_ok_entry`가 짝지어 잡는다 |
| M4 | 입력 축 | 파라미터에서 `plugins` 케이스를 뺀다 | 리뷰가 잡는다(등가) |
| M5 | I/O 층 | backup 4.5의 `broken_syntax` 갈래에서 `git show` 명령 삭제 | `offers_repair_only_when_there_is_a_candidate` |

- [ ] **Step 7: docstring 점검** — `generate_metadata.py` docstring이 "세 SKILL.md는 `newer_schema_seen`만 읽는데"라고 적었다면 Task 3에서 이미 "`newer_schema_seen`·`broken_syntax`"로 고쳤는지 확인한다. `compat.SHAPE_BROKEN` 주변 주석에 "detect_downgrade가 이 형태를 broken_syntax로 보고한다"를 더한다.

- [ ] **Step 8: Commit**

```bash
git add plugins/claude-sync/skills/sync-backup/scripts/detect_downgrade.py plugins/claude-sync/lib/compat.py \
        plugins/claude-sync/skills/sync-backup/SKILL.md plugins/claude-sync/skills/sync-restore/SKILL.md \
        plugins/claude-sync/skills/sync-status/SKILL.md plugins/claude-sync/tests/test_downgrade.py \
        plugins/claude-sync/tests/test_skill_wiring.py docs/superpowers/mutations/plan4-task-11.json
git commit -m "feat(backup): 구문이 깨진 백업 문서를 진단하고 마지막 정상 판본으로 되돌릴지 묻는다"
```

---

### Task 12: `broken_syntax` 문구 여섯 줄 — 행위자를 플러그인으로

**근거:** spec 5.3 — **등급: 중**(사용자에게 행동을 권하는 산문. 옛 문장은 비개발자에게 막다른 길이다)

**Files:**
- Modify: `plugins/claude-sync/skills/sync-backup/SKILL.md` (5단계 324행 · 6단계 373행), `plugins/claude-sync/skills/sync-restore/SKILL.md` (5절 252행 · 6절 456행), `plugins/claude-sync/skills/sync-status/SKILL.md` (2단계 139행 · 169행)
- Test: `plugins/claude-sync/tests/test_skill_wiring.py`

- [ ] **Step 1: 실패하는 가드**

`tests/test_skill_wiring.py`의 `test_no_skill_branches_on_the_reason_sentence` 아래에 더한다:

```python
# `reason_kind`가 broken_syntax인 분기 **줄**. 세 스킬에 여섯 줄이다(backup 5·6, restore 5·6,
# status 2단계의 플러그인·MCP). 손으로 세지 않고 정규식으로 뽑되 개수를 함께 건다.
BROKEN_BRANCH = re.compile(r"`reason_kind`가 \*{0,2}`broken_syntax`\*{0,2}이면")
REPAIR_POINTER = {
    "sync-backup": "4.5단계에서 「복구한다」",
    "sync-restore": "**복구는 `/sync-backup`이**",
    "sync-status": "`/sync-backup`이 복구를 제안합니다",
}
MANUAL_REPAIR = "정상 JSON으로 되돌린 뒤 다시 실행하도록 안내한다"


def test_every_broken_syntax_branch_points_at_the_plugin_repair():
    """spec 5.3 — 여섯 줄 전부가 플러그인의 복구 경로를 가리키고, "손으로 되돌려라"는 backup의
    후보 없음 갈래에만 남는다. restore·status는 그 문장을 아예 쓰지 않는다(0장의 결정).

    앞 판의 여섯 줄은 똑같이 "그 파일을 정상 JSON으로 되돌린 뒤 다시 실행하도록 안내한다"였다
    — 행위자가 사용자이고, 그 상태에서는 세 스킬이 전부 그 문서를 건너뛰어 탈출구가 없었다.
    """
    found = 0
    for skill in SKILLS:
        for line in read_skill(skill).splitlines():
            if not BROKEN_BRANCH.search(line):
                continue
            found += 1
            plain = line.replace("**", "")
            assert REPAIR_POINTER[skill].replace("**", "") in plain, (skill, line[:90])
            if skill == "sync-backup":
                assert MANUAL_REPAIR in plain and plain.index("후보가 없었던 경우") < plain.index(MANUAL_REPAIR), (
                    "backup의 손 복구 안내는 후보 없음 갈래 뒤에만 온다")
            else:
                assert MANUAL_REPAIR not in plain, "%s가 레포 수리를 사용자에게 떠넘긴다" % skill
            assert "그냥 지우라고 안내하지 않는다" in plain, "삭제 금지 경고가 사라졌다"
    assert found == 6, found
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_skill_wiring.py -q -k plugin_repair` → FAIL(여섯 줄 모두 옛 문장).

- [ ] **Step 2: 여섯 줄 교체**

backup 5단계(324행)·6단계(373행)의 "그 파일을 정상 JSON으로 되돌린 뒤 다시 실행하도록 안내한다(레포 git 이력에 정상 판본이 있으면 그것으로 복구한다)."를 **둘 다** 다음으로 바꾼다(문장의 나머지 — "플러그인 업데이트는 소용이 없다", "그냥 지우라고 안내하지 않는다" 이하 — 는 그대로):

```
4.5단계에서 복구하지 않아 이 문서를 건너뛴다 — "복구하려면 `/sync-backup`을 다시 실행해 4.5단계에서 「복구한다」를 고르세요"로 안내한다. 4.5단계에 후보가 없었던 경우에만 그 파일을 정상 JSON으로 되돌린 뒤 다시 실행하도록 안내한다.
```

restore 5절(252행)·6절(456행)의 같은 문장(6절은 "**정상 JSON으로 되돌린 뒤 다시 실행하도록** 안내한다")을 **둘 다** 다음으로:

```
레포의 이 문서가 손상됐습니다(2.5단계 참조) — **복구는 `/sync-backup`이** 마지막 정상 판본으로 되돌릴지 묻는다. restore는 레포에 쓰지 않는다.
```

6절(456행)에는 "그냥 지우라고 안내하지 않는다" 문장이 없다 — 5절과 같은 문장("**그냥 지우라고 안내하지 않는다** — 그 문서에만 있던 다른 기기의 서버는 이 기기의 로컬에 없어서 다음 백업이 되밀 수 없다 — 지우면 그 서버가 레포에서 사라진다.")을 그 줄에 더한다.

status 2단계 플러그인(139행)의 같은 문장을:

```
레포의 이 문서가 손상됐습니다(1.5단계 참조) — `/sync-backup`이 복구를 제안합니다. status는 복구하지 않는다.
```

로, MCP(169행)의 "레포 파일이 손상된 것이므로 **정상 JSON으로 되돌린 뒤 다시 실행하도록** 안내한다(플러그인 업데이트는 소용이 없다)."를:

```
레포 파일이 손상된 것이다(플러그인 업데이트는 소용이 없다) — 1.5단계 참조. `/sync-backup`이 복구를 제안합니다. **그냥 지우라고 안내하지 않는다** — 그 문서에만 있던 다른 기기의 서버가 레포에서 사라진다.
```

로 바꾼다.

- [ ] **Step 3: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS.

- [ ] **Step 4b: 변조 확인** — `plan4-task-12.json`

| id | 축 | 치환 | 잡는 테스트 |
|---|---|---|---|
| M1 | I/O 층 | restore 6절 줄에 옛 문장을 되살린다 | `plugin_repair` |
| M2 | 축 분리 | backup 5단계 줄에서 "후보가 없었던 경우에만"을 지운다(무조건 손 복구) | 같은 테스트(순서 단정) |
| M3 | 입력 축 | status 169행에서 분기 표식 `` `reason_kind`가 `broken_syntax`이면 ``을 문장으로 풀어 쓴다 | `found == 6` |
| M4 | `{}` vs `None` | 한 줄에서 "그냥 지우라고 안내하지 않는다"를 지운다 | 같은 테스트 |
| M5 | 훅 호출 계약 | `REPAIR_POINTER["sync-status"]`가 가리키는 문장을 산문에서 "restore가 복구합니다"로 | 같은 테스트 |

- [ ] **Step 5: Commit**

```bash
git add plugins/claude-sync/skills/sync-backup/SKILL.md plugins/claude-sync/skills/sync-restore/SKILL.md \
        plugins/claude-sync/skills/sync-status/SKILL.md plugins/claude-sync/tests/test_skill_wiring.py \
        docs/superpowers/mutations/plan4-task-12.json
git commit -m "docs(skills): broken_syntax 갈래가 레포 수리를 사용자에게 떠넘기지 않는다"
```

---

### Task 13: 출력 언어 스위치 — `sync-config.json`의 `language`

**근거:** spec 6 — **등급: 경**(산문은 한국어로 남고 가드가 전부 산다)

**Files:**
- Modify: 세 `SKILL.md`의 `## 설정 파일` 절과 `### 1.` 절, `sync-backup/SKILL.md` 2.5단계 164행
- Modify: `README.md`, `README.ko.md` (설정 절 신설)
- Test: `plugins/claude-sync/tests/test_skill_wiring.py`, `plugins/claude-sync/tests/test_user_docs.py`

- [ ] **Step 1: 실패하는 가드**

`tests/test_skill_wiring.py` 끝에 더한다:

```python
# ── 언어 스위치 (spec 6) ─────────────────────────────────────────────────
LANGUAGE_RULE = "**`language`가 있으면 사용자에게 보이는 모든 문장을 그 언어로 낸다.**"
NO_TRANSLATE = re.compile(r"\*\*번역하지 않는 것\*\*: (.+?)\. ")
FIRST_RUN_LANGUAGE = "사용자가 대화에 쓰는 언어로 한다"
STEP1 = {"sync-backup": "1. 설정 확인", "sync-restore": "1. 설정 확인",
         "sync-status": "1. 설정 확인 및 레포 준비"}


@pytest.mark.parametrize("skill", SKILLS)
def test_step1_carries_the_language_rule_and_asks_on_first_run(skill):
    """규칙이 세 스킬에 같은 문장으로 있고, 첫 실행의 질문은 대화 언어로 한다."""
    sec = section(skill, STEP1[skill])
    assert LANGUAGE_RULE in sec, skill
    assert FIRST_RUN_LANGUAGE in sec, skill
    assert "부재가 곧 한국어" in sec, skill


def test_the_do_not_translate_list_is_the_same_in_every_skill():
    """번역하지 않는 것의 목록이 갈리면 한 스킬만 명령이나 키 이름을 번역한다."""
    found = {skill: NO_TRANSLATE.search(section(skill, STEP1[skill])) for skill in SKILLS}
    assert all(found.values()), [s for s, m in found.items() if not m]
    values = {m.group(1) for m in found.values()}
    assert len(values) == 1, values
    only = values.pop()
    for token in ("명령", "JSON 키", "파일 경로", "이름", "판정 값"):
        assert token in only, token


def test_backup_verbatim_contract_allows_language_only():
    """2.5단계의 드리프트 방지 계약이 살아 있고(명령을 직접 타자하지 않는다), 언어만 바꾼다."""
    sec = section("sync-backup", "2.5 호환성 검사")
    assert "`message`의 내용을 그대로 보여준다" in sec
    assert "언어만" in sec and "명령을 직접 타자하지 않는다" in sec
    assert "**`message` 필드를 그대로 보여준다. 명령을 직접 타자하지 않는다**" not in sec
```

`tests/test_user_docs.py`의 `METADATA_WORDING` 블록 아래에 더한다:

```python
# --- 설정 키 (spec 6.4) ---

CONFIG_KEYS = ("repo_url", "git_user_name", "git_user_email", "pull_only", "language")
CONFIG_SECTION = {"README.md": "## Configuration", "README.ko.md": "## 설정"}


def config_section(name):
    text = read_doc(name)
    i = text.index(CONFIG_SECTION[name])
    return text[i:text.index("\n## ", i + 1)]


@pytest.mark.parametrize("name", sorted(CONFIG_SECTION))
def test_readme_documents_every_config_key(name):
    """설정 절이 다섯 키를 전부 적는다 — `language`가 없으면 영어 사용자가 스위치를 모른다."""
    sec = config_section(name)
    for key in CONFIG_KEYS:
        assert "`%s`" % key in sec, (name, key)
    assert "한국어" in sec or "Korean" in sec, "부재의 뜻(한국어)을 적지 않았다"


def test_the_config_key_list_is_what_the_skills_actually_read():
    """목록이 낡지 않게 — 세 SKILL.md가 실제로 읽는 키와 대조한다."""
    text = "".join(open(os.path.join(SKILLS_DIR, s, "SKILL.md"), encoding="utf-8").read()
                   for s in ("sync-backup", "sync-restore", "sync-status"))
    for key in CONFIG_KEYS:
        assert ('"%s"' % key in text) or ("`%s`" % key in text), key
```

실행: `uv run --with pytest pytest plugins/claude-sync/tests/test_skill_wiring.py plugins/claude-sync/tests/test_user_docs.py -q -k "language or translate or verbatim or config_key"` → FAIL.

- [ ] **Step 2: 세 SKILL.md**

(a) `## 설정 파일` 절의 JSON 예시에 `"language": "en"`을 더하고(세 스킬), backup의 불릿 목록에 한 줄을 더한다: `- \`language\`는 선택 사항이다. 있으면 사용자에게 보이는 문장을 그 언어로 낸다(1단계). 없으면 한국어다.` restore·status는 예시 아래 한 문장으로 같은 뜻을 적는다.

(b) 세 스킬의 `### 1.` 절, `cat ~/.claude/sync-config.json` 블록 바로 뒤에 같은 문단을 넣는다:

```
**`language`가 있으면 사용자에게 보이는 모든 문장을 그 언어로 낸다.** 이 문서의 인용문("> …")과 표의 안내 문구, 그리고 스크립트가 만든 문장(`message`·`reason`·`unrestorable_reasons`·`degraded_reason`·`base_staging_reason`)이 대상이다. **번역하지 않는 것**: 명령(`claude …`·`git …`), JSON 키와 버킷 이름, 파일 경로, 서버·플러그인·마켓플레이스 이름, 판정 값. 스크립트 문장은 **내용을 바꾸지 않고 언어만** 바꾼다. 키가 없으면 한국어다 — 부재가 곧 한국어이고, 같은 뜻의 표현을 둘 두지 않는다.
```

(c) 첫 실행 문장. backup의 "파일이 없으면 사용자에게 Git 레포 URL을 물어본다. URL을 받으면:"을

```
파일이 없으면 사용자에게 Git 레포 URL과 **안내 언어**를 물어본다. **이 첫 질문은 사용자가 대화에 쓰는 언어로 한다** — 아직 설정이 없기 때문이다. 한국어를 골랐으면 `language` 키를 쓰지 않는다. 답을 받으면:
```

로 바꾸고 heredoc 예시를

```bash
cat > ~/.claude/sync-config.json << 'EOF'
{
  "repo_url": "<사용자가 입력한 URL>",
  "language": "<선택한 언어 — 한국어면 이 줄을 뺀다>"
}
EOF
```

로 바꾼다. restore의 "파일이 없으면 사용자에게 Git 레포 URL을 물어본다. URL을 받으면 `~/.claude/sync-config.json`에 저장한다."와 status의 "파일이 없으면 사용자에게 Git 레포 URL을 물어보고 저장한다"에도 같은 문장("… URL과 **안내 언어**를 물어본다. **이 첫 질문은 사용자가 대화에 쓰는 언어로 한다** … 한국어를 골랐으면 `language` 키를 쓰지 않는다.")을 쓴다. status는 그 문장이 `## 설정 파일` 절에 있으므로 `### 1.` 절 안에도 한 번 적는다(가드가 절 안에서 찾는다).

(d) backup 2.5단계 164행의 "**`message` 필드를 그대로 보여준다. 명령을 직접 타자하지 않는다** — 안내 문구는 `compat.py`가 만드는 것이 계약이고, SKILL.md가 따로 쓰면 드리프트한다."를

```
**`message`의 내용을 그대로 보여준다** — `language`가 있으면 문장만 그 언어로 옮기되 안의 명령은 그대로 둔다. **명령을 직접 타자하지 않는다** — 안내 문구는 `compat.py`가 만드는 것이 계약이고, SKILL.md가 따로 쓰면 드리프트한다. 언어만 바꾸는 것은 드리프트가 아니다.
```

로 바꾼다.

- [ ] **Step 3: README 두 벌 — 설정 절 신설**

`README.md`의 `### Check before applying changes` 블록 뒤, `## Upgrading to v3.0.0` 앞에:

```
## Configuration

`~/.claude/sync-config.json` is created on the first run and holds these keys:

| Key | Required | Meaning |
|---|---|---|
| `repo_url` | yes | Git URL of the backup repo |
| `git_user_name` / `git_user_email` | no | Local git identity for the backup clone. The clone lives in a temp dir, so `includeIf` rules may not apply |
| `pull_only` | no | `true` makes this machine restore-only — `/sync-backup` refuses to run |
| `language` | no | Language for everything the skills say to you, e.g. `"en"`. Prose is translated at output time; commands, JSON keys, paths and names are never translated. Absent means Korean |
```

`README.ko.md`의 `### 변경 전 확인` 뒤, `## v3.0.0으로 올릴 때` 앞에:

```
## 설정

`~/.claude/sync-config.json`은 첫 실행 때 만들어지고 다음 키를 담습니다:

| 키 | 필수 | 뜻 |
|---|---|---|
| `repo_url` | 예 | 백업 레포의 Git URL |
| `git_user_name` / `git_user_email` | 아니오 | 백업 클론에 적용할 로컬 git 신원. 클론이 임시 디렉토리에 있어 `includeIf` 규칙이 적용되지 않을 수 있습니다 |
| `pull_only` | 아니오 | `true`면 이 기기는 복원 전용입니다 — `/sync-backup`이 실행을 거부합니다 |
| `language` | 아니오 | 스킬이 사용자에게 하는 모든 말의 언어, 예: `"en"`. 문장은 출력 시점에 번역되고 명령·JSON 키·경로·이름은 번역하지 않습니다. 없으면 한국어입니다 |
```

- [ ] **Step 4: 통과 확인** — `uv run --with pytest pytest plugins/claude-sync/tests -q` → 전부 PASS. `test_user_doc_list_covers_every_doc_on_disk`는 문서 수가 그대로라 초록이다.

- [ ] **Step 4b: 변조 확인** — `plan4-task-13.json`: 한 스킬에서 규칙 문단 삭제(CAUGHT — 파라미터화), 한 스킬의 "번역하지 않는 것" 목록에서 "명령"을 뺀다(CAUGHT — 등식), 2.5 옛 문장 되살리기(CAUGHT), README.ko의 `language` 행 삭제(CAUGHT), `CONFIG_KEYS`에 SKILL.md가 읽지 않는 키를 더한다(CAUGHT — `actually_read`).

- [ ] **Step 5: Commit**

```bash
git add plugins/claude-sync/skills/*/SKILL.md README.md README.ko.md \
        plugins/claude-sync/tests/test_skill_wiring.py plugins/claude-sync/tests/test_user_docs.py \
        docs/superpowers/mutations/plan4-task-13.json
git commit -m "feat(skills): sync-config.json의 language — 사용자에게 보이는 문장의 언어를 바꾼다"
```

---

### Task 14: 릴리즈 게이트 — 3.1.0이 나갈 수 있는지 검증한다

**근거:** spec 8 · 10 — **등급: 중**(배포)

**Files:**
- Modify: `plugins/claude-sync/.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` (`3.1.0`)
- Create: `docs/RELEASE-NOTES-3.1.0.md`, `docs/RELEASE-NOTES-3.1.0.en.md`, `docs/superpowers/release-3.1.0-CHECKLIST.md`
- Test: `plugins/claude-sync/tests/test_user_docs.py` (릴리즈 노트 쌍 가드)

- [ ] **Step 1: 실패하는 가드**

`tests/test_user_docs.py` 끝에 더한다:

```python
# --- 릴리즈 노트 두 벌 (spec 8) ---

RELEASE_NOTES = {
    "ko": os.path.join(ROOT, "docs", "RELEASE-NOTES-3.1.0.md"),
    "en": os.path.join(ROOT, "docs", "RELEASE-NOTES-3.1.0.en.md"),
}


def test_release_notes_come_in_two_languages_with_the_same_shape():
    """한 언어만 갱신되는 회귀 — `##` 절의 개수와 순서가 같아야 한다(README 쌍과 같은 처방)."""
    shapes = {}
    for lang, path in RELEASE_NOTES.items():
        with open(path, encoding="utf-8") as f:
            shapes[lang] = [line for line in f.read().splitlines() if line.startswith("## ")]
        assert len(shapes[lang]) >= 4, (lang, shapes[lang])
    assert len(shapes["ko"]) == len(shapes["en"]), shapes
    with open(os.path.join(ROOT, "plugins", "claude-sync", ".claude-plugin", "plugin.json"),
              encoding="utf-8") as f:
        version = json.load(f)["version"]
    for lang, path in RELEASE_NOTES.items():
        assert version in open(path, encoding="utf-8").read(), (lang, version)
```

(`import json`이 없으면 상단에 더한다.) 실행 → FAIL(파일 없음).

- [ ] **Step 2: 버전**

`plugins/claude-sync/.claude-plugin/plugin.json`과 `.claude-plugin/marketplace.json`의 `"version"`을 `"3.1.0"`으로. `lib/compat.py`의 `MIN_READER_VERSION`은 **그대로** `"3.0.0"`이다 — `test_min_reader_major_matches_plugin_json`이 major 일치를, `test_min_reader_minor_and_patch_are_zero`가 나머지를 건다.

- [ ] **Step 3: 릴리즈 노트 두 벌**

`docs/RELEASE-NOTES-3.1.0.md`:

```markdown
# claude-sync 3.1.0

3.0.0을 올린 기기에서 처음 실제 백업을 돌린 뒤 그 결과물을 뜯어보고 찾은 결함 다섯과, 3.0.0이
이월한 둘을 닫습니다. **백업 파일의 형식은 바뀌지 않습니다** — 3.0.0 기기와 섞여 있어도 순서
제약이 없습니다.

## 거짓이었던 문장 다섯 — 이제 어떻게 말하나

- **"리모트가 앞선 변경이 있습니다"** — 비교 기준선이 없는 파일에도 그렇게 말했습니다. 그 안내를 따라 복원에서 「백업 채택」을 고르면 로컬 변경이 사라집니다. 이제 기준선이 없으면 "어느 쪽이 앞선 것인지 판단할 수 없습니다. 「백업 채택」은 로컬 변경을 버립니다"라고 말합니다. `/sync-status`도 그 파일을 "양쪽 변경"이 아니라 "기준선 없음"으로 찍습니다.
- **백업만 하는 기기에는 파일 기준선이 영영 생기지 않았습니다.** 로컬과 레포가 이미 같은 파일의 기준선을 쓰지 않았기 때문입니다. 이제 씁니다 — 그래서 위 문구가 재발하지 않고, 나중에 레포만 바뀌면 충돌이 아니라 조용한 내려받기가 됩니다.
- **`sync-metadata.json`이 레포에 없는 내용을 레포 것이라 적었습니다.** 이 기기의 로컬을 걷었기 때문입니다. 이제 레포 작업 트리를 걷습니다 — 다른 기기가 올린 파일도 함께 실립니다. 이 파일을 읽는 코드는 없고 README의 "3-way 충돌 판단에 활용"도 고쳤습니다.
- **"다른 기기가 추가했습니다. restore가 설치합니다" ×7, "복원할 수 없습니다" ×7.** 2.x가 `claude mcp list` 출력을 긁어 넣은 계정 커넥터가 레포에 영구히 남아 매 실행 거짓 문구를 냈습니다. 어느 기기의 로컬에도 없어 병합으로는 지워질 수 없었습니다. 이제 `/sync-backup`이 그 목록과 사유를 보여주고 **레포에서 정리할지 묻습니다.** 고르면 플러그인이 지우고 커밋·푸시까지 합니다. `/sync-status`·`/sync-restore`는 그 항목에 "설치합니다"라고 말하지 않습니다.
- **"그 파일을 정상 JSON으로 되돌린 뒤 다시 실행하세요."** 레포 문서의 구문이 깨졌을 때 다섯 곳이 그렇게 안내했습니다 — 행위자가 사용자이고, 그 상태에서는 세 스킬이 전부 그 문서를 건너뛰어 탈출구가 없었습니다. 이제 `/sync-backup`이 git 이력에서 마지막 정상 판본을 찾아 **되돌릴지 묻고**, 고르면 플러그인이 되돌립니다. `/sync-restore`·`/sync-status`는 진단하고 그곳을 가리킵니다.

## 새로 생긴 것

- **복원 불가 항목 정리** (`/sync-backup` 6.5단계). 목록 전체에 한 번 묻습니다. 판정이 이 버전이 아는 형식 기준이라는 것과, 다른 기기가 실제로 쓰는 서버라면 그 기기의 다음 복원이 묻고 되돌린다는 것을 함께 알립니다. 다운그레이드가 의심되는 레포에서는 제안하지 않습니다.
- **구문 손상 복구** (`/sync-backup` 4.5단계). 다운그레이드 복구와 같은 자리, 같은 모양입니다. 후보를 못 찾은 경우에만 손으로 되돌리는 안내가 남습니다.
- **출력 언어** — `~/.claude/sync-config.json`에 `"language": "en"`을 두면 스킬이 사용자에게 하는 모든 말이 그 언어가 됩니다. 명령·JSON 키·경로·이름은 번역하지 않습니다. 첫 실행의 질문은 사용자가 대화에 쓰는 언어로 합니다. 없으면 한국어입니다.

## 그 밖의 변경

- MCP 복원 계획 JSON이 플러그인 계획과 같은 두 층(`sections[<섹션>]` + 최상위 실행 재료)이 됐습니다. 사용자에게 보이는 변화는 없습니다.
- README에 설정 키 다섯을 적는 절이 생겼습니다.

## 3.0.0에서 올릴 때

순서 제약이 없습니다. 기기마다 `claude plugin marketplace update claude-sync && claude plugin update claude-sync` 후 재시작하면 됩니다. 첫 `/sync-backup`이 파일 기준선을 만들고, 레포에 남은 복원 불가 항목이 있으면 정리를 제안합니다.
```

`docs/RELEASE-NOTES-3.1.0.en.md`는 같은 다섯 절(`# claude-sync 3.1.0` · `## Five sentences that were false — and what they say now` · `## New` · `## Other changes` · `## Upgrading from 3.0.0`)을 영어로 같은 내용으로 쓴다. 절 수가 같아야 위 가드가 통과한다.

- [ ] **Step 4: 전체 검증**

```bash
uv run --with pytest pytest plugins/claude-sync/tests -q
grep -rn '"version"' .claude-plugin/marketplace.json plugins/claude-sync/.claude-plugin/plugin.json
for spec in docs/superpowers/mutations/plan4-task-*.json; do
  python3 ~/.claude/suberpowers/tools/mutate.py --repo /Users/bran/personal/claude-sync --spec "$spec" --jobs 4
done
```

기대: 0 failed · 둘 다 `3.1.0` · 열세 명세 전부 `SURVIVED`·`APPLY_FAIL` 0건(등가 변조로 명세에 적은 것은 제외). 결과를 `docs/superpowers/mutations/README.md`의 표에 plan ④ 행으로 더한다(커밋 SHA와 함께 — "변조는 실행을 공유한다").

- [ ] **Step 5: 실기기 스모크 체크리스트 — `docs/superpowers/release-3.1.0-CHECKLIST.md`**

3.0.0 체크리스트와 같은 형식으로 쓴다. 담을 것: (1) 브랜치 흐름과 PR·태그 명령(3.0.0 것 그대로, 버전만), (2) **이 기기가 픽스처다** — spec 10.2의 여섯 단계를 순서대로. 특히:

- 설치 전 `/sync-status` 한 번(대조군) → 3.1.0 설치·재시작 → **`/sync-backup` 한 번**: 4단계 `reject.no_base == ["agents/code-reviewer.md"]`이고 "리모트가 앞선"이 나가지 않는다 / 6.5단계에 `claude.ai *` 7개가 사유와 함께 뜨고 「정리한다」→ `pruned` 7개·`refused` 비어 있음 / 10단계는 커밋 경로이고 `~/.claude/.sync-state/base/`에 `in_sync` 8개의 파일 기준선이 생기며 `agents/code-reviewer.md`의 기준선은 **없다** / 레포의 `sync-metadata.json`에서 `agents/code-reviewer.md`의 해시가 레포 판본(`1be61088…`)이다.
- `/sync-backup` 한 번 더: `unrestorable`·`repo_ahead`·`deleted` 비어 있음, "변경사항이 없습니다" 경로, 파일 기준선 8개 불변.
- `/sync-restore`: 계획이 `sections["servers"]` 층이다. `agents/code-reviewer.md`가 충돌로 뜨면 **「로컬 유지」**(「백업 채택」은 41줄을 버린다). 그 뒤 `/sync-backup`이 그 파일을 `push`로 올리고 기준선이 생긴다.
- `"language": "en"`을 넣고 `/sync-status`: 문장은 영어, 명령·키·이름은 그대로.
- ⑤는 실기기 레포를 깨뜨리지 않는다 — 임시 클론을 복제한 버리는 레포에서 `mcp-servers.json`을 잘라 4.5단계의 세 선택지를 한 번씩 돈다.

- [ ] **Step 6: Commit**

```bash
git add plugins/claude-sync/.claude-plugin/plugin.json .claude-plugin/marketplace.json \
        docs/RELEASE-NOTES-3.1.0.md docs/RELEASE-NOTES-3.1.0.en.md docs/superpowers/release-3.1.0-CHECKLIST.md \
        docs/superpowers/mutations/README.md plugins/claude-sync/tests/test_user_docs.py
git commit -m "release: 3.1.0 — 버전·릴리즈 노트 두 벌·실기기 체크리스트"
```

**그 뒤는 사용자가 한다** — 푸시, `release/3.1.0` PR, `main` PR, `claude plugin tag plugins/claude-sync --push`, 기기별 업데이트, 체크리스트의 스모크.

---

## Self-review (작성 직후 — 2026-09-02)

**Spec coverage.** spec 절마다 task를 가리킬 수 있는가.

| spec | task | | spec | task |
|---|---|---|---|---|
| 3.1 | 1 | | 5.1 · 5.2 · 5.4 | 11 |
| 3.2 | 2 | | 5.3 | 12 |
| 3.3 | 3 | | 5.5 | 11 (4.5단계 산문 "묻는 이유") |
| 3.4 | 4 | | 6.1 ~ 6.5 | 13 |
| 3.5 | 5 | | 7.1 ~ 7.3 | 6 |
| 4.1 | 7 | | 8 | 14 |
| 4.2 | 8 | | 9 (영향 파일) | 각 task의 Files |
| 4.3 · 4.6 | 9 | | 10.1 (회귀 표) | 각 task의 Step 1 — 표의 행 전부에 대응 테스트가 있다 |
| 4.4 · 4.5 | 10 | | 10.2 (실기기) · 10.3 (변조) | 14 · 각 task의 Step 4b |
| 11 (닫은 것) | 없음 — 할 일이 아니다 | | 12 (오픈 이슈) | 1·6은 닫혔다, 2는 Task 3 Step 4가, 3은 Task 10 Step 2가, 4는 Task 10 Step 4가, 5는 Task 13 Step 1이 확정한다 |

**Placeholder scan.** "TBD"·"TODO"·"나중에"·"비슷하게" 없음. 코드 step은 전부 코드 블록을 갖는다. Task 14 Step 3의 영어판은 절 구조와 내용을 한국어판에 묶어 두었다 — 원문 자체가 plan에 있으므로 자리표시자가 아니다.

**Type consistency.** `bucket(plan, name)` — Task 6이 정의하고 8·9가 쓴다(test_mcp_scripts·test_mcp_cycle 각각). `mc.unrestorable_report`·`mc.route_new_keys` — Task 7이 정의하고 8·9가 쓴다. `GENERATE_METADATA_CALL` — Task 3이 정의하고 10이 쓴다. `PRUNE_CALL` — Task 10. `DOWNGRADE_SECTION["sync-backup"]`·`DOWNGRADE_RESULT_SUBSECTION` — Task 11이 값을 바꾸고 같은 task의 가드가 그 값을 쓴다. `branch_slice` — 기존. `GARBAGE` — test_mcp_scripts(Task 8)와 test_mcp_cycle(Task 9)에 각각 정의한다(두 파일이 서로 import하지 않는다). `every_rel` — Task 1의 test_reconcile.py 안에서만 쓴다. 출력 키 이름은 `unrestorable`·`unrestorable_reasons`·`broken_syntax`·`no_base`·`remote_ahead`·`pruned`·`not_found`·`refused` — 모든 task에서 같다.
