# plan ③ 실행 진행 기록

- plan 본문: `docs/superpowers/plans/2026-08-31-plugins-downgrade-compat.md`
- 분할 파일: `~/.claude/suberpowers/plan3-tasks/` (`split-plan.py --check` 초록)
- 착수: `e7831ba`, **963 passed**
- 방식: 갈래 ⓐ는 subagent-driven (task마다 implementer → spec review → quality review).
  **갈래 ⓑ부터는 직접 구현 + 변조 하네스** — 근거는 「갈래 ⓑ 완결」 절

## 상태

| Task | 상태 | 커밋 | spec | quality |
|---|---|---|---|---|
| 1 `lib/compat.py`의 relpath별 형태 판정 | **완료** | `e3f6200`·`af09464`·`e39f88f`·`c82f6e1` | ✅ (r3) | **Yes** (r2) — Critical 0 / Important 0 / Minor 1 |
| 2 `detect_downgrade.py` relpath 맵 | **완료** | `d543feb`·`ea4c136`·`e92f721`·`687913b` | ✅ (1라운드) | **With fixes → 해소** |
| 3 v1→v2 승격이 사고를 삼키지 않는다 | **완료** | `a721e79` | ✅ (1라운드) | 〃 (2·3 합동) |
| 4 `generate_metadata.py`의 `schema` 맵 | **완료** | `957524f` | — (직접 검증) | — (변조 여섯, S5는 등가) |
| 5 세 `SKILL.md`의 다운그레이드 대화 | **완료** | `2e3f4a5`~`8dbf305` | ✅ (2라운드) | **With fixes → 해소** |
| 6 2.x 배포 순서 경고 + `.bak` 정리 안내 | **완료** | `761d2e0`~`c40bbb2` | ❌→✅ (2라운드) | **With fixes → 해소** |
| 7 3차 스모크 반영 | **완료** | `26c3e6e` | — (직접 검증) | — (변조 여섯 CAUGHT) |
| 8 4단계가 로컬 확장 값을 평탄화하지 않는다 | **완료** | `f46489a` | — (직접 검증) | — (변조 여덟 CAUGHT) |
| 9 비원자적 로컬 쓰기 + `.tmp` 위생 | **완료** | `3922e44` | — (직접 검증) | — (변조 아홉 CAUGHT) |

현재 **1199 passed**(14 tasks 전부). **갈래 ⓐ가 닫혔다** — Task 6이 배포 순서 경고 세 곳(파일로는 다섯)을 두 문서 모두 말하게 고쳤고, 그것이 이 plan의 유일한 **예방**이다(2.x는 표식을 읽지 못하므로 사고를 막을 코드가 없다). 리뷰 보고서는 `~/.claude/suberpowers/reviews/2026-08-31-claude-sync-task-1-*.md` 여섯 벌.

## Task 1이 남긴 것 — Task 2가 이어받을 자리

**Task 1의 리뷰 라운드가 실제 결함 셋을 잡았고 셋 다 닫혔다.** 형태별로 기록해 둔다 —
같은 형태가 남은 task에서 반복된다.

| 라운드 | 결함 | 형태 |
|---|---|---|
| spec r1 | 옛 arity 서술 둘(`version-compat-design.md:653`·`:975-976`) | **(b) 문장이 코드와 어긋난다** |
| spec r2 | 완전성 단정이 **선택자가 비면 루프 0회로 초록**(`VERSION_MARKED_BUT_NOT_V2 = {}` SURVIVE) | 공허해지는 형태 **②** |
| quality r1 | `detect_downgrade.py`의 relpath 인자 **둘이 테스트로 고정되지 않음** — 틀린 relpath를 꽂아도 1045 passed. 실제 다운그레이드가 조용히 "사고 없음"이 된다 | **훅 호출 계약** 축 + (c) |
| quality r1 | `compat.py`의 "순환 import" 근거가 **정적·실측 모두 거짓**(`lib/`는 순환 없는 DAG) | **(b)**, 결론은 맞고 근거가 틀림 |

**quality r1의 relpath 결함이 Task 2에 직접 걸린다.** 원인은 `tests/test_downgrade.py`의 픽스처가
전부 `{"version":2,"scope":"user","servers":{…}}`라 **두 규칙 모두에서 `v2_object`**여서 갈리지
않는 것이었다. Task 1이 갈리는 픽스처 둘을 넣어 닫았다:

| 픽스처 | mcp 규칙 | plugins 규칙 |
|---|---|---|
| `v2_without_version({…})` = `{"servers":{…}}` | `v2_object` | `v1_object` |
| `version_without_servers()` = `{"version":2}` | `unknown` | `v2_object` |

**Task 2는 이 둘을 그대로 relpath 맵 위로 옮겨 재사용한다.** 그리고 **Step 4b에 "호출부 relpath
오배선" 변조를 명시적으로 넣을 것** — plan 머리의 축 표가 "훅 호출 계약"을 1번 축으로 적어
두었는데 Task 1의 Step 4b 목록이 그 축을 세우지 않아 리뷰가 대신 잡았다.


## Task 1의 시간과 그 귀결 — plan 공통 절이 바뀐 이유

Task 1은 **1시간 53분**이 걸렸고 산출물은 프로덕션 코드 **+102/−22줄**, 테스트 +429/−39줄,
문서 +15/−7줄이다. 배분은 subagent 실행 100분 27초 + 오케스트레이션 12분 29초였고,
그 100분은 implementer 48분 · 리뷰어 다섯 52분으로 갈린다.

**라운드별 발견을 보면 무엇이 수렴하지 않았는지가 드러난다:**

| 라운드 | 프로덕션 로직 결함 | 그 외 |
|---|---|---|
| spec r1 | **0** | 문서 arity 2 |
| spec r2 | **0** | 완전성 단정이 공허함 1 |
| spec r3 | **0** | 0 |
| quality r1 | **0** | 테스트 공백 1, 거짓 근거 주석 1, Minor 5 |
| quality r2 | **0** | Minor 1 (선재) |

**로직은 첫 시도에 수렴했다. 수렴하지 않은 것은 「증명」이다.** 그래서 plan 공통 절에 세 절을
넣었다(`00-shared-context.md`로 배포되어 남은 task의 subagent가 읽는다):

1. **검증을 언제 멈추는가 — 명시적 기준 셋**(커버리지: 다섯 축 전부 CAUGHT / 깊이: 가드는
   스스로를 검사하고 가드의 가드를 만들지 않는다 / 라운드: ⑴⑵ 충족 + 로직 결함 0건이면 종료)
2. **검증 강도는 위험도로 나눈다** — 축은 「코드냐 문서냐」가 아니라 「조용히 틀리면 되돌릴 수
   없는 행동이 나가는가」다. 이 저장소에서는 `SKILL.md` 산문이 실행물이다
3. **변조는 실행을 공유하고 설계는 공유하지 않는다** — 리뷰어의 값은 전부 자기가 고안한 변조에서
   나왔고, implementer의 spec을 재실행한 데서는 새 정보가 0건이었다

**가장 값싼 절감은 ⑴이다.** quality r1이 잡은 실제 결함은 다섯 축 중 1번 축인데 Task 1의
Step 4b가 그 축을 세우지 않았다 — 세웠으면 1라운드에 나오고 두 라운드가 줄었다.

**Task 2부터는 각 task의 Step 4b가 다섯 축을 전부 인스턴스화해야 한다.**

## Task 1이 남긴 둘 — 이월이 아니라 **지금 닫는다**

사용자 결정(2026-08-31)으로 이 plan에는 이월이 없다. Task 1 리뷰가 남긴 둘은 **Task 2·3이
`test_downgrade.py`·`test_compat.py`를 손댈 때 함께 닫는다.** 둘 다 다운그레이드 탐지의 가드이므로
Goal에 직접 방해가 된다.

| 항목 | 왜 방해가 되는가 |
|---|---|
| **m6** — `test_compat.py`의 `parsable_rows`에서 `rp == relpath` 필터를 지우면 1047 전부 초록. 다른 변조와 겹치면 완전성 단정 셋이 함께 넓어진다 | 그 단정들이 **형태 판정표의 자기 축소**를 막는 유일한 장치다. 넓어지면 판정표가 줄어도 초록이 되고, 그러면 다운그레이드 탐지가 조용히 약해진다 |
| **R14** — mcp `{"servers":[]}` 행이 mcp v2 규칙(`servers`가 dict)을 재는 **유일한 입력**인데 짝지어진 완전성 단정이 없다 | 그 행이 사라지면 `isinstance(…, dict)` → `"servers" in obj` 약화가 **새어 나간다**. mcp 쪽 다운그레이드 판정이 헐거워진다 |

**m3·m4·m5**(가독성·이름·테스트 분할)는 **닫는다** — Goal에 방해가 되지 않는다. 특히 m4는 이 파일의
헬퍼 관례가 밑줄 없음이라 **바꾸면 오히려 파일 안에서 갈린다**(실측 확인).

---

## Task 2·3·5 완료 — 깨졌던 세 `SKILL.md` 계약은 **닫혔다** (아래는 그 이력)

Task 2가 `detect()`의 출력을 relpath 맵으로 바꾸면서 **세 `SKILL.md`의 계약을 깼다.**
plan(task-02.md)이 산문 수정을 Task 5로 미루라고 명시적으로 금지했으므로 **의도된 순서**지만,
**그 키를 산문에 묶는 테스트가 하나도 없어 아무 앵커도 빨개지지 않는다**(실측 재확인).
그래서 이 기록이 유일한 안전장치다.

**무엇이 깨졌나** — ① 최상위 `downgrade_suspected`·`newer_schema_seen`·`status`·`repo_shape`·
`base_shape`·`candidate`가 사라졌다(이제 전부 `files[relpath]` 아래) ② `candidate.server_count`·
`server_names`가 `entries: {버킷: [이름…]}`으로 대체됐다.

| 파일 | 아직 옛 계약을 읽는 자리 |
|---|---|
| `sync-backup/SKILL.md` | `:256`·`:258`·`:260`·`:263`·`:272` |
| `sync-status/SKILL.md` | `:92`·`:94`·`:96`·`:100` |
| `sync-restore/SKILL.md` | `:140`·`:142`·`:144`·`:154`·`:155` |
| `sync-restore/SKILL.md` (6-5) | `:490`·`:492`·`:497` — 2.5단계의 `downgrade_suspected`를 **최상위 값으로** 참조 |

**`version-compat` spec §9.3**(복구 UX — *"후보 커밋의 날짜·서버 수·서버 이름"*)도 같은 이유로
낡았고, 산문 결정과 붙어 있어 Task 5에 남겼다.

> **닫혔다 (Task 5, `2e3f4a5`~`bbc8008`).** 네 자리를 파일 맵 위에 다시 세웠고, **다섯째 자리**를
> 새로 발견해 세웠다 — `sync-restore` **5-5**절에도 같은 거짓(*"다른 기기가 지웠습니다"* → `uninstall`
> 권유)이 플러그인 쪽에 생기므로 `files["plugins.json"]`로 억제한다. `grep -rn "server_count\|server_names"
> plugins/` → 출력 없음.

**Task 5에 넘기는 추가 입력:** 「전역 skipped + 파일별 ok」 조합(git을 못 쓰는데 어느 문서도
suspected가 아닌 경우)을 산문이 어떻게 렌더링할지 정해야 한다. Task 2·3의 quality 리뷰 m5가
그 조합을 산출하는 테스트를 더한다.

---

## plan 개정 (2026-08-31, 사용자 결정)

- **Goal을 릴리즈 기준으로 다시 썼다** — *"3.0.0을 배포 가능한 상태로 만든다"*. 네 갈래(ⓐ 다운그레이드 · ⓑ 남은 로컬 손실 경로 · ⓒ 검증되지 않은 자리 · ⓓ 보고·산문 드리프트)로 묶었고 **task 번호는 재배열하지 않았다**(근거 참조가 깨진다).
- **이월 표를 폐지했다.** plan ②가 넘긴 14행을 한 줄씩 판정해 **10개를 Task 10~13으로 흡수**, **5개를 「닫은 것」 절에 이유와 함께 닫았다.**
- **Task 14(릴리즈 게이트)를 더했다** — 릴리즈 계획 5장의 범위가 코드에서 실제로 닫혔는지 **plan ②의 선언이 아니라 코드와 테스트로** 검증한다. 푸시·배포는 사용자가 실행한다.
- task가 9 → **14**가 됐다. Task 1 완료, **13 남음.**


---

# 세션 종료 인계 (2026-08-31)

## 어디까지 왔나 — **갈래 ⓐ 완결**

| 갈래 | task | 상태 |
|---|---|---|
| **ⓐ 다운그레이드 사고** (예방·탐지·안내) | 1·2·3·5·6 | **전부 완료** |
| ⓑ 남은 로컬 손실 경로 | 8·9 | **전부 완료** |
| ⓒ 검증되지 않은 자리 | 7·13 | **전부 완료** |
| ⓓ 보고·산문 드리프트 | 4·10·11·12 | **전부 완료** |
| 릴리즈 게이트 | 14 | **완료** |

**963 → 1145 passed.** 착수(`ea53122`) 이후 20 files, +2018/−269. 작업 트리 clean.

**세 스킬(`/sync-backup`·`/sync-restore`·`/sync-status`)은 한 번도 실행하지 않았다.** 이 기기 캐시는 여전히 `2.0.0`이다.

## ⓐ가 실제로 무엇을 바꿨나

2.x 기기 한 번의 백업이 타 기기 항목을 지우는 사고가 **조용하고 영구적**이던 것에서 **예방·탐지·안내가 있는 것**으로 바뀌었다.

- **예방(Task 6)** — 배포 순서 경고 세 곳(다섯 파일, 두 언어)이 이제 `plugins.json`도 말하고 *"MCP를 하나도 쓰지 않아도 해당된다"*를 앞세운다. `backup-readme`의 경고는 MCP 절 **밖 독립 절**로 옮겼다(절 안에 두면 지키려는 독자가 절째로 건너뛴다)
- **탐지(Task 1·2·3)** — 형태 판정이 relpath별로 갈리고(`relpath`는 **기본값 없는 필수 인자**), 탐지 출력이 `{"files": {relpath: …}}` 맵이 됐다. `find_last_v2_commit`의 v2 판정이 `shape_of`다 — `parse_base`를 쓰면 2.x가 쓴 v1 커밋을 "마지막 정상 판본"으로 제시해 **탐지가 사고를 고착시킨다**
- **안내(Task 5)** — 대화 **다섯 자리**가 파일 맵 위에 섰다. 네 자리는 plan이 알던 것이고 **다섯째(`sync-restore` 5-5)는 이 작업이 발견**했다 — 플러그인 쪽에도 *"다른 기기가 지웠습니다"* → `uninstall` 권유라는 같은 거짓이 생긴다

## 다음 세션이 바로 이어받는 방법

1. `~/.claude/suberpowers/plan3-tasks/` — 14개 task 파일 + `00-shared-context.md`(변조 다섯 축, **검증을 언제 멈추는가**, 검증 강도 등급, 이월 금지 규칙). `split-plan.py --check` 초록
2. 이 문서 + `docs/superpowers/plans/2026-08-31-plugins-downgrade-compat.md`
3. 리뷰 보고서 열둘: `~/.claude/suberpowers/reviews/2026-08-31-claude-sync-task-*.md`

**추천 순서: Task 9 → 8 (갈래 ⓑ)**. 9는 `ks.dump_bytes`가 이미 있어 두 줄 교체이고, **잘린 파일이 레포의 온전한 사본을 덮어 모든 기기로 퍼지는 진짜 손실 경로**다. 그다음 ⓒⓓ, 마지막이 Task 14.

> **그대로 실행했다 (2026-09-01). 아래 「갈래 ⓑ」 절을 볼 것.** 다음은 **ⓒ(7·13) → ⓓ(4·10·11·12) → 14**다.

## 이 세션에서 확립된 것 (공통 절에 반영돼 있다)

- **검증을 언제 멈추는가 — 기준 셋:** ⑴ 다섯 축 전부 CAUGHT면 끝 ⑵ 가드는 자기 입력이 비면 스스로 실패한다, **가드의 가드를 만들지 않는다** ⑶ ⑴⑵ 충족 + 프로덕션 결함 0이면 라운드 종료
- **검증 강도는 위험도로 나눈다** — 축은 「코드냐 문서냐」가 아니라 **「조용히 틀리면 되돌릴 수 없는 행동이 나가는가」**. 이 저장소에서는 `SKILL.md` 산문이 실행물이다
- **변조는 실행을 공유하고 설계는 공유하지 않는다** — 리뷰어의 값은 전부 자기가 고안한 변조에서 나왔다
- **이월 표를 만들지 않는다** — Goal에 방해되면 지금, 아니면 「닫은 것」에 이유를 적는다. **이 세션에서 넘긴 것 0건**

## 효과 (Task 1 대비)

| | Task 1 | Task 2·3 | Task 5 | Task 6 |
|---|---|---|---|---|
| task당 시간 | 100분 | **30분** | 55분 | 60분 |
| spec 라운드 | 3 | 1 | 2 | 2 |

Task 5·6이 다시 늘어난 것은 **산문 task라 리뷰가 실제 결함을 계속 찾았기 때문**이다 — Task 6 quality가 잡은 I1(경고 몇 줄 아래 문단이 *"never drops"*로 정반대를 말하던 것)과 I2(`min_reader_version`이 3.0.0 기기를 막지 못하는데 막는다고 적혀 있던 것)는 둘 다 **예방 문서의 사실 오류**였다. 라운드가 는 것이 낭비가 아니었다.


---

# 갈래 ⓑ 완결 (2026-09-01)

**1145 → 1161 passed.** Task 9 `3922e44`, Task 8 `f46489a`. 작업 트리 clean. 세 스킬은 이 세션에서도 **한 번도 실행하지 않았다**(이 기기 캐시는 여전히 `2.0.0`이다).

## 검증 방식이 갈래 ⓐ와 다르다 — 리뷰 subagent를 띄우지 않았다

**근거는 사용자 지시와 「검증 강도는 위험도로 나눈다」 절이다.** 두 task 모두 **한 파일당 수십 줄**의 델타이고, 변조 하네스가 다섯 축을 전부 실측으로 덮었다. 리뷰 라운드를 대신한 것은 **변조 열일곱 벌**(Task 9 아홉 · Task 8 여덟)이고, 그중 **둘이 실제 공백을 열었다** — 아래 참조. Task 1의 실측(리뷰 다섯 라운드에서 프로덕션 로직 결함 0건, 발견은 전부 검증 계층)이 이 선택의 근거다.

## Task 9 — 잘린 파일이 레포로 퍼지는 경로를 닫았다

`reconcile_restore`의 `open(local,"wb")` 둘이 **선-truncate**했다. ENOSPC로 중간에 죽으면 로컬이 잘린 채 남고, 예외가 traceback으로 서서 `write_base`가 실행되지 않아 base는 옛 값 그대로다 — 다음 판정이 `local_ahead`가 되어 **다음 백업이 잘린 로컬을 레포의 온전한 사본 위로 push한다.**

- 두 갈래(`add`/`overwrite`, clean `merge`) 모두 `ks.dump_bytes`를 거친다
- `write_base`의 삭제 분기가 `<path>.tmp`까지 지운다. **현재 영향은 없다**(base 디렉토리를 walk하는 코드도, `data=None` 프로덕션 호출자도 없다 — 전수 grep). 위생이라고 주석에 적었다
- `bootstrap.sh`가 백업 레포에 `.gitignore`(`*.tmp`)를 만든다. 기존 파일은 **덮지 않고 이어 붙이며**, 마지막 줄에 개행이 없으면 먼저 채운다(`echo >>`가 사용자 패턴에 `*.tmp`를 붙여 **둘 다** 망가뜨린다). 소급되지 않는 한계를 주석·`backup-readme` 양쪽에 적었다
- `.gitignore`가 값싼 보험이 아니라 실재하는 보험인 근거: `dump_backup → dump_json → dump_bytes`가 **`<레포>/plugins.json.tmp`를 잠깐 만든다**. `os.replace` 전에 SIGKILL이면 남고 10단계의 `git add -A`가 그것을 커밋한다

**실패 주입은 디스크를 채우지 않는다.** `builtins.open`을 감싸 HOME 아래 쓰기에서만 "절반 쓰고 ENOSPC"를 흉내낸다 — 옛 코드와 새 코드가 **같은 지점**에서 실패하므로 갈리는 것은 **로컬 파일이 잘렸는가** 하나다. `os.replace`만 표적으로 삼으면 옛 코드는 그 줄에 닿지도 않아 "실패가 주입되지 않았다"와 "원자적이다"가 같은 초록이 된다.

## Task 8 — 계획이 다루지 않은 상호작용 하나를 사용자에게 물었다

`install --config k=v`는 그것도 `install`이라 enabledPlugins 값을 함께 쓴다. 로컬 값이 객체면 평탄화되고 되돌릴 CLI가 없다. 계획 Step 1은 **「로컬 비불리언이면 전부 뺀다」** 였는데, 구현 중 실측으로 다음이 드러났다:

> **레포 값이 불리언이면 5-4(3단계)가 `enable`/`disable`을 내고 그 명령이 확장 값을 어차피 지운다**(`value_command`의 실측 표 — 스모크 3장). 즉 그 갈래에서 4단계를 건너뛰어도 **최종 상태가 같고** 설정만 못 채운다 — 순수한 손해다.

**사용자 결정(2026-09-01): 「지킬 값이 있을 때만 건너뛴다」.** 판정을 `value_command(로컬, 레포)`가 `None`인 키로 좁혔다 — 레포 값이 비불리언(H3)이거나 레포 `enabledPlugins`에 키가 없을 때다. 이것은 2026-08-31 결정의 **사유**(*"사용자가 요청하지 않은 로컬 상태 변경"*)를 그대로 적용한 것이다: 레포와 갈린 값을 맞추는 3단계는 **요청된 수렴**이지 부수 효과가 아니다.

**설치 여부로는 여전히 좁히지 않았다.** 좁히면 bare install이 **보존하는** 배열 값까지 함께 잃는다. 대신 그 경계(`install` 목록에도 있고 로컬 값이 객체면 이 보류가 지키는 것이 없다)를 코드 주석과 5-3 산문에 적었다.

**spec 9.3.1에 예외를 적었다** — spec에 없는 동작을 코드가 하면 다음 감사가 결함으로 읽는다.

## 변조가 연 공백 둘 — 리뷰어가 아니라 하네스가 잡았다

| 변조 | 무엇이 드러났나 | 조치 |
|---|---|---|
| **M4**(Task 9) `if data is None:` → `if not data:` | **SURVIVED.** 빈 base 블롭(`b""`)을 "삭제"로 접어도 스위트 전체가 초록이었다. 동기화 대상 중 **빈 파일**이 있으면 base가 지워지고, 그 파일이 다음에 갈릴 때 `changed_local`·`changed_remote`가 둘 다 참이 되어 fast_forward여야 할 것이 **`no_base` 충돌**로 남는다 | 테스트를 보강(`write_base(rel, b"")`가 빈 블롭을 기록한다). 재실행 CAUGHT |
| **N8**(Task 8) 가드를 `k in local_masked` 하나로 넓히기 | 불리언 행이 `local=False, repo=True`뿐이라 **둘째 절(`value_command`)이 대신 막아** `isinstance` 절이 하중을 받지 않았다 | 행을 `[True, False]`로 갈랐다. `local=True, repo=True`는 `value_command`가 `None`이라 **첫째 절만이** 막는다 |

## 입력 축을 두 번 실증했다 (⑸의 다섯째 축)

- **Task 9.** `merge` 시나리오를 빼고 merge 호출부만 되돌리면 **1152 전부 초록**이다 — 그 시나리오가 그 호출부의 **유일한** 가드다. 두 갈래를 각각 걸어야 하는 이유가 이것이다
- **Task 8.** 불리언 `[True]` 행을 빼고 가드를 넓혀도 **다섯이 빨개진다**(종단 cycle 테스트 셋 포함) — 그 행은 단독 가드가 아니다. 두 결과가 다른 것이 정상이고, 어느 쪽인지는 **재 봐야 안다**

## 남은 것 — 다음 세션이 이어받는 자리

| 갈래 | task | 상태 |
|---|---|---|
| ⓐ 다운그레이드 사고 | 1·2·3·5·6 | 완료 |
| ⓑ 남은 로컬 손실 경로 | 8·9 | **완료** |
| ⓒ 검증되지 않은 자리 | 7·13 | **전부 완료** |
| ⓓ 보고·산문 드리프트 | 4·10·11·12 | **전부 완료** |
| 릴리즈 게이트 | 14 | **완료** |

**전부 끝났다 — 아래 「plan ③ 완결」 절을 볼 것.** 남은 것은 사용자의 배포 실행뿐이다.

---

# 갈래 ⓒ 완결 (2026-09-01)

**1161 → 1173 passed.** Task 7 `26c3e6e`, Task 13 `d746a10`. 작업 트리 clean.
**세 스킬은 여전히 한 번도 실행하지 않았다** — 측정은 전부 임시 HOME에서 했고 측정 뒤
실제 홈의 마켓플레이스 셋이 그대로임을 확인했다.

## Task 7 — 틀린 것은 프로덕션이 아니라 에뮬레이터 하나였다

`_marketplace_source`가 url·git 인자에 `NotImplementedError`를 던지고 있었다. 3차 스모크
14장이 그 둘을 쟀으므로 다섯 모양을 판별하게 고쳤고, **표 밖의 인자에는 여전히 죽는다.**

- **순서가 실측이 아니라 선택이다** — github 정규화가 `.git`보다 먼저이므로
  `https://github.com/o/r.git`는 github이 된다. 그 모양은 미측정이고 프로덕션에 도달
  경로가 없다(github 출처에 내는 인자는 `repo` 필드다). 그 사실을 docstring에 적었다
- 모듈 docstring **열셋이 전부 실측**이 됐다. 남은 것은 항목 **안**의 좁은 미측정 둘 —
  `a@b@m`(CLI 규칙으로는 지워지는데 `marketplace_of`는 못 알아본다)과 위의 `.git` URL
- `git`의 둘째 후보 `repo`는 **근거를 바꿔** 남겼다. 앞 근거(*"어느 쪽이 옳은지 모른다"*)는
  사라졌고 새 근거는 비대칭이다 — 지우면 `repo`만 가진 값이 조용히 unrestorable이 되고
  남기는 비용은 0이다
- **전수 grep이 규약 위반 하나를 잡았다**: 모듈 docstring 8번이 2차 스모크로 승격될 때
  사용처(`test_installing_at_user_scope_keeps_other_scope_entries`)가 함께 고쳐지지 않아
  한쪽만 *"실측 없음 — 추정"*으로 남아 있었다. 표시 규약이 요구하는 양방향 갱신이 그 자리다

## Task 13 — 측정이 계획보다 큰 것을 열었다

`defaultEnabled`를 어느 파일에서 되읽는지(#4)를 재려고 임시 HOME에 CLI 2.1.252를 세웠는데,
**답이 "되읽지 않는다"** 였다. install 시점에 한 번 쓰여 `settings.json`으로 물질화될 뿐이다.
그래서 *"부재는 매니페스트 기본값에 위임하는 상태"* 라는 이 저장소의 전제가 무너졌고,
통합 규칙이 나왔다 — **부재 = 꺼짐**(설치 여부와 무관하다).

| `enabledPlugins` 값 | 현재 상태 | `enable` | `disable` |
|---|---|---|---|
| `true` | 켜짐 | exit 1 | exit 0 → `false` |
| `false` · 비불리언 · **부재** | 꺼짐 | exit 0 → `true` | exit 1 |

**그 전제 위에 있던 두 자리가 틀려 있었다.** `get(k, True)`가 ⑴ 레포 `true` + 로컬 키 부재인
**설치된** 플러그인에 아무 명령도 내지 않아 **꺼진 채로 남기고**(조용한 미복원) ⑵ 레포
`false`에는 `disable`을 내 거짓 실패를 만들었다. `False`로 읽으면 **둘 다 옳아진다.**
`assumed` 표식과 SKILL.md 10.2의 그 갈래는 함께 사라졌다.

**층을 갈랐다.** 「동기화 층의 부재」(다른 기기가 지웠다 ≠ 껐다 — 9.3.3)와 「CLI 층의
부재」(꺼짐)는 다른 질문이다. 섞으면 3단계가 틀린다. `value_command`의 docstring과 9.3.3이
그 구별을 적는다.

## 판본 드리프트 — 1차의 기록이 재현되지 않았다

1차 스모크(2.1.250)는 *"미설치 id에 `disable` → exit 0이고 `{id: false}` 키를 만든다"*로
쟀는데 **2.1.252에서는 exit 1이고 키를 만들지 않는다**(`enable`은 그대로 만든다).
원인이 판본 변화인지 1차 픽스처의 차이인지는 **이 측정으로 가릴 수 없다** — 1차의 그 시점
`enabledPlugins` 상태가 기록돼 있지 않다.

**결정: 에뮬레이터는 1차 동작을 유지한다.** 함대에 옛 판본이 섞일 수 있고 그쪽이 더
위험하며, **재현하는 쪽이 검증의 상위집합**이다(유령 키를 만드는 갈래를 재현해야 그것을
막는 방어 9.3.2가 시험된다). 프로덕션은 4차 실측을 따른다 — 두 층이 다른 판본을 향하는
것이 의도이고, 그 이유를 `_set_value`와 모듈 docstring 4번 양쪽에 적었다.

## 새로 발견하고 **닫은 것**

**「레포 `false` + 로컬 키 부재」는 2.1.252에서 한 명령으로 수렴시킬 수 없다.** `disable`이
exit 1이므로 로컬에 `false` 키를 만들 수 없고, 키가 부재인 채 남으면 다음 백업이 그것을
**삭제로 읽어 레포에서 그 항목을 지운다.**

**닫는다 — 이 plan의 Goal에 방해가 되지 않는다.** 근거 셋이다. ⑴ **이번 변경이 만든 것이
아니다** — 2.1.252에서는 앞 판(`disable`을 내던 판)도 exit 1로 죽어 결과가 같았다.
⑵ 최종 상태는 **레포와 일치한다**(둘 다 꺼짐) — 잃는 것은 값이 아니라 표현이다.
⑶ 고치려면 `enable` → `disable` 두 명령을 내야 하는데 **사용자가 끄기를 원한 플러그인을
잠깐 켠다** — 처방이 병보다 나쁘다. 도달 조건도 좁다(그 id가 이미 설치돼 있고, 로컬 키가
사람 손이나 옛 판본으로 지워진 상태여야 한다).

## corpus가 레포에 들어왔다 (Step 3)

`docs/superpowers/mutations/`에 task 7·8·9·13의 명세 넷. **전부 재실행해 SURVIVED·APPLY_FAIL
0건**을 확인했다 — 명세가 커밋된 트리에 실제로 적용된다는 뜻이다. Task 1~6의 명세는
회수하지 못했고, **지금 새로 쓰면 "그때 돌린 것"이 아니므로 쓰지 않았다**(그 사실을 README에
적었다). 그 판정의 근거는 이 문서와 리뷰 보고서 열둘이다.

**그 과정에서 하네스의 두 함정을 실측했다** — Task 7의 `P5`는 앵커가 두 자리에 매치해
`APPLY_FAIL`이었고, Task 13의 `T6`은 **쓰지 않는 인자를 더한 등가 변조**라 CAUGHT도 SURVIVED도
의미가 없었다. 둘 다 `SURVIVED`로 읽지 않고 고쳐서 다시 돌렸다.

## 남은 것

| 갈래 | task | 상태 |
|---|---|---|
| ⓐ·ⓑ·ⓒ | 1·2·3·5·6·7·8·9·13 | **완료** |
| ⓓ 보고·산문 드리프트 | 4·10·11·12 | **전부 완료** |
| 릴리즈 게이트 | 14 | **완료** |

spec 14.5의 미측정은 **#5·#6·#7** 셋이다(#1~#4는 닫혔다). 셋 다 프로덕션 도달 경로가
없거나 좁고, 해당 자리에 표식이 붙어 있다.

---

# plan ③ 완결 (2026-09-01) — 14 tasks 전부

**963 → 1199 passed.** 작업 트리 clean. **세 스킬은 한 번도 실행하지 않았다** — 이 기기
캐시는 여전히 `2.0.0`이고, 실행한 것은 임시 HOME의 `claude plugin`뿐이다(4차 스모크).

| 갈래 | task | 상태 |
|---|---|---|
| ⓐ 다운그레이드 사고 | 1·2·3·5·6 | 완료 |
| ⓑ 남은 로컬 손실 경로 | 8·9 | 완료 |
| ⓒ 검증되지 않은 자리 | 7·13 | 완료 |
| ⓓ 보고·산문 드리프트 | 4·10·11·12 | **완료** |
| 릴리즈 게이트 | 14 | **완료** |

## 갈래 ⓓ — 네 task

- **Task 4** (`957524f`) `sync-metadata`의 `schema` 맵이 백업 문서 둘을 말한다. 변조가
  **SKILL.md 예시 JSON이 산출물에 묶여 있지 않다**는 것을 열었고(S6), 그 자리를 닫았다
- **Task 12** (`52f04dc`·`3c33d5c`) `restore_action`을 `lib/`로 **순수 이동**한 뒤 제외 파일에
  항목별 복원 처방을 붙였다. 앞 판 주석의 *"어느 분류에도 해당하지 않는다"* 는 backup
  방향에만 참이고 **restore 방향을 빠뜨린 문장**이었다
- **Task 10** (`7067551`) 손으로 센 개수 둘을 원천에서 뽑는다 — `CORRECTIONS`는 spec 13장
  첫 표에서, `SYNCIGNORE_MEANING`은 `syncignore` 정본의 번호 목록에서. 옛 앵커 둘을
  재확인해 하나는 고치고(헤딩 개명), 하나는 **그 자리에 앵커가 없음**을 확인한 뒤 같은
  장에서 진짜 낡은 것(13장 표의 행 번호)을 찾아 닫았다
- **Task 11** (`5b19e6b`) `reason` 문장 분기 여덟 자리를 `reason_kind`로 옮기고 보고 층
  `lib/report.py`를 신설했다. 축자 중복(`conflicts`·`repo_ahead` 분할)도 같은 자리로 접었고,
  `/sync-status`의 `unrestorable`에 사유를 실었다(spec 9.2를 먼저 고쳤다)

## Task 14 — 릴리즈 게이트: **셋 다 짚었다**

| 릴리즈 계획 5장 | 그것을 고정하는 테스트 |
|---|---|
| 결함 A 통째 덮어쓰기 | `test_collect_keeps_other_devices_entries` |
| 결함 B 켬/끔 미감지 | `test_compare_reports_value_changes_not_just_key_sets` · `test_compare_says_which_way_an_on_off_change_went` |
| 결함 C 예외 처리 부재 | `test_a_broken_repo_document_is_a_document_level_skip_from_the_cli`(두 파일 × 세 스크립트) |
| 코어 추출 | `test_recognize_adapter_list_covers_every_keyed_sync_importer` · `test_adapter_passes_one_recognize_hook_to_all_three` |

버전 둘 다 `3.0.0`, 배포 순서 경고가 다섯 문서에 도달(가드 둘이 고정). 사용자가 실행할
절차는 **`docs/superpowers/2026-09-01-release-3.0.0-CHECKLIST.md`** 에 있다.
**배포·푸시는 하지 않았다** — 외부 동작이므로 사용자가 실행한다.

## 완료 정의 실행 결과

| 항목 | 결과 |
|---|---|
| 0 failed | **1199 passed** |
| 코어(`keyed_sync.py`) 판정 로직 불변 | **참** — diff는 `dump_bytes` **docstring 6줄뿐**이고 로직 0줄이다(Task 9가 소비자 둘을 적었다) |
| `shape_of`의 `relpath`가 필수 인자 | `TypeError` ✓ |
| `server_count`/`server_names` | 0건 |
| `NotImplementedError` 유지 | 2건 |
| 이월 표 부재 | 남은 "이월"은 규칙 설명·「닫은 것」·**plan ②의 이월을 근거로 인용한 task 머리** 셋뿐이다 |
| 변조 명세가 저장소에 | `docs/superpowers/mutations/` 9파일 |
| 세 스킬 미실행 | 참 |

**착수 시점의 「제외」 표 네 행이 실행 중에 거짓이 됐다** — Task 10·11이 실제로 했다.
표에 취소선으로 표시했다. 그대로 두면 다음 감사가 *"하지 않기로 한 것"* 으로 읽는데
코드에는 있다.

## 이 plan이 남긴 방법론 — 실측 셋

1. **변조가 리뷰어보다 싸고 자주 맞았다.** 갈래 ⓑ~ⓓ에서 리뷰 subagent 없이 변조 **마흔여섯**
   벌을 돌렸고, 그중 **여섯이 실제 공백을 열었다**(M4·N8·S6·W2·W5·W7). 리뷰 라운드가 잡던
   것과 같은 종류이고, 커밋 SHA에 묶여 재실행 가능하다
2. **`SURVIVED`를 읽는 법이 세 가지다** — 진짜 공백(테스트를 보강한다) · **등가 변조**(오늘
   동작이 같다. S5·초판 T6) · **가드의 가드**(만들지 않는다). 셋을 섞으면 corpus가 소음이 된다
3. **`APPLY_FAIL`과 `CONTROL_BROKEN`이 두 번씩 났고 둘 다 판정을 무효로 만들었다.** P5·U5는
   앵커가 낡았고, Task 11의 C0은 **새 테스트 자신의 결함**을 먼저 잡았다 — 대조군이 없었으면
   그 라운드의 CAUGHT 판정 일곱이 전부 거짓이었다

## 다음 세션이 할 일

**코드 작업은 없다.** `docs/superpowers/2026-09-01-release-3.0.0-CHECKLIST.md`를 열고
사용자가 절차를 실행한다. 그 문서 맨 앞의 ⛔ 경고를 먼저 읽을 것 — **이 기기에서
`/sync-backup`을 실행하면 레포가 파괴된다.**
